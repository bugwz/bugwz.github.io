<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title>Ghost博客成功&quot;更新&quot;至0.11.9</title>
      <link href="/2017/05/27/ghost-0-11-9/"/>
      <url>/2017/05/27/ghost-0-11-9/</url>
      
        <content type="html"><![CDATA[<p>Ghost真的是一个让我又爱又恨的博客平台，界面的简洁之美，写作的流畅与舒适，还有那烦人的安装与更新操作。在差不多一年之前吧，我安装了版本是 <code>0.7.4</code> 的Ghost平台，那时候用的是<a href="http://www.ghostchina.com/" target="_blank">Ghost中文网</a>中的那个 <code>Ghost 中文集成版</code> ，当时也是费了很多周折，使用到现在也没有出现什么问题。也是在最近几天，闲着没事去Ghost中文网看了看，竟然还是 <code>0.7.4</code> 的版本！心想难道Ghost一直没更新？于是去英文官网看了看，才发现原来Ghost有两个路线，一个是 <code>Alpha</code> 版本，已经更新到了 <code>Alpha.21</code> ，一个是正式版，也已经更新到了 <code>0.11.9</code> ；而Ghost中文网的内容就有点滞后了，希望可以尽快更新。之后我就想体验新版本的Ghost。开始吧~</p><h3 id="0-尝试更新Ghost"><a href="#0-尝试更新Ghost" class="headerlink" title="0.尝试更新Ghost"></a>0.尝试更新Ghost</h3><p>根据Ghost英文官网的 <a href="https://support.ghost.org/how-to-upgrade" target="_blank">How-To-Upgrade</a>更新指导步骤，我整理如下：</p><ul><li>备份：在后台中的”实验室”栏目中导出一个 <code>.json文件</code> ，并且将Ghost根目录中的 <code>content目录</code> 进行备份(这里存放着博客站点的所有上传图片和主题)。而我直接是把 <code>整个Ghost目录和数据库</code> 进行了备份。记住：一定要备份！无论你做什么一定要备份！！！</li><li>检查Ghost平台是否可以重启：因为在更新Ghost完成之后，或者更新出错进行调试的时候，需要重启Ghost，所以我们必须确保Ghost可以重启；</li><li>关于跨版本更新的问题：源版本与目标版本如果跨度较大可能会出现问题，所以更新升级的建议如下：<ul><li><code>0.7.1</code> 或更高版本应该可以直接升级到 <code>0.11.9</code> ；</li><li><code>0.5.0</code> 或更高版本应该可以直接升级到 <code>0.7.x</code> ；</li><li><code>0.4.2</code> 的版本必须升级到版本 <code>0.7.1</code>（<code>0.7.0</code>有几个升级错误，在<code>0.7.1</code>中被解决），然后升级到更高版本的<code>0.7.x</code>；</li><li><code>0.4.2</code>之前的版本必须按照版本号由低到高升级，直到达到<code>0.4.2</code>；</li></ul></li><li>下载新版本并解压：可以使用 <code>wget</code> 或者 <code>curl -LOk</code> 下载<br> <code>https://ghost.org/zip/ghost-latest.zip</code> 中的最新版本文件，然后可以使用 <code>unzip Ghost-*.*.*.zip -d ghost-*.*.*</code> 解压。</li><li>删除旧文件/复制新文件：删除Ghost更目录下的 <code>core目录</code> 、<code>index.js</code> 、<code>*.md</code> 和 <code>*.json</code> ；复制新版本Ghost目录中的<br> <code>core目录</code> 、 <code>index.js</code> 、 <code>package.json</code> 和 <code>npm-shrinkwrap.json</code> 到目前的Ghost根目录中。</li><li>开始升级：在目前的Ghost目录中执行升级命令 <code>npm install --production --unsafe-perm</code> ，如果期间报错，先删除 <code>node_modules文件夹</code> ，再运行 <code>npm cache clean</code> 并重试。最后运行 <code>npm start --production</code> 进行调试。</li></ul><blockquote><p> 使用 <code>npm install --production --unsafe-perm</code> 需要注意的问题：</p></blockquote><ul><li>添加 <code>--unsafe-perm</code> 的参数的原因是：如果我们不添加这个参数，会出现类似于 <code>npm WARN cannot run in wd test@0.0.0 echo something (wd=/Users/Lloyd/Documents/test)</code> 的错误，原因是如果使用root权限调用 <code>npm</code> ，那么它会将 <code>uid</code> 更改为用户配置指定的 <code>uid</code> ，默认为 <code>nobody</code> ，设置<code>--unsafe-perm</code> 参数以使用root权限运行脚本；</li><li>因为GFW的原因，使用 <code>npm install --production --unsafe-perm</code> 可能会出现很多错误，建议修改一下 <code>npm</code> 的镜像源，这里推荐<a href="http://npm.taobao.org/" target="_blank">淘宝 NPM 镜像</a>，具体使用命令为（其他修改方法见<a href="https://cnodejs.org/topic/4f9904f9407edba21468f31e" target="_blank">这里</a>）：</li></ul><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm config <span class="built_in">set</span> registry https://registry.npm.taobao.org </span><br><span class="line">npm info underscore（如果上面配置正确这个命令会有字符串response）</span><br></pre></td></tr></table></figure><ul><li>由于我这里不使用 <code>sqlite3</code> ，所以我吧 <code>package.json</code> 中的 <code>sqlite3</code> 依赖包给删除了，否则的话安装会卡住不动。</li></ul><h3 id="1-全新安装Ghost平台"><a href="#1-全新安装Ghost平台" class="headerlink" title="1.全新安装Ghost平台"></a>1.全新安装Ghost平台</h3><p>因为之前安装Ghost也出现了不少问题，但是目前都忘了，为了回顾一下，也为了照顾第一次使用Ghost的用户，这里再说明一下全新安装 <code>Ghost 0.11.9</code> 的方法。下文中部分参考于<a href="https://snowz.me/how-to-install-ghost/" target="_blank">手把手教你搭建一个属于自己的Ghost博客</a></p><h4 id="1-1-安装-Node-环境"><a href="#1-1-安装-Node-环境" class="headerlink" title="1.1 安装 Node 环境"></a>1.1 安装 <code>Node</code> 环境</h4><p>由于 <code>Ghost</code> 是基于 <code>Node</code> 的，所以 <code>Node</code> 的环境是必须第一步要安装的，而 <code>Ghost</code> 对于 <code>Node</code> 版本的要求也是十分苛刻的，具体要求可以去<a href="http://support.ghost.org/supported-node-versions/" target="_blank">官方的说明</a>中去查看，我整理如下：</p><ul><li>Ghost目前支持的Node版本只有 <code>0.12.x</code> 、 <code>4.2+</code> 、 <code>6.9+</code>；</li><li>官方推荐的版本是 <code>4.2+</code> ，经实测使用 <code>4.5</code> 会报错，所以这里的<br> <code>4.2+</code> 应该是指 <code>4.2.*</code> ；</li></ul><p>我在这里使用的 <code>Node</code> 版本是4.2.5，安装Node的命令如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget http://nodejs.org/dist/v4.2.5/node-v4.2.5.tar.gz</span><br><span class="line">tar zxvf node-v4.2.5.tar.gz</span><br><span class="line"><span class="built_in">cd</span> node-v4.2.5</span><br><span class="line">./configure</span><br><span class="line">make &amp;&amp; make install</span><br></pre></td></tr></table></figure></p><h4 id="1-2-安装Nginx并配置"><a href="#1-2-安装Nginx并配置" class="headerlink" title="1.2 安装Nginx并配置"></a>1.2 安装Nginx并配置</h4><p>首先我们安装Nginx：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install nginx</span><br></pre></td></tr></table></figure></p><p>安装之后可以使用IP访问，检查 <code>Nginx</code> 是否安装成功，然后我们需要为Ghost平台在 <code>/etc/nginx/conf.d</code> 目录下创建一个配置文件<br> <code>ghost.conf</code> ：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/nginx/conf.d/ghost.conf</span><br></pre></td></tr></table></figure></p><p>写入的内容如下所示：<br><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="comment">#http访问</span></span><br><span class="line"><span class="section">server</span> &#123;  </span><br><span class="line">    <span class="attribute">listen</span> <span class="number">80</span>;</span><br><span class="line">    <span class="attribute">server_name</span> example.com; <span class="comment">#将example.com改为你的域名或ip</span></span><br><span class="line">    <span class="attribute">location</span> / &#123;</span><br><span class="line">        <span class="attribute">proxy_set_header</span>   X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span>   Host      <span class="variable">$http_host</span>;</span><br><span class="line">        <span class="attribute">proxy_pass</span>         http://127.0.0.1:2368;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">#https访问，可以配置也可以不配置：</span></span><br><span class="line"><span class="section">server</span> &#123;</span><br><span class="line"><span class="attribute">listen</span> <span class="number">443</span>;</span><br><span class="line"><span class="attribute">server_name</span> example.com; <span class="comment">#将example.com改为你的域名或ip</span></span><br><span class="line"><span class="attribute">ssl</span> <span class="literal">on</span>;</span><br><span class="line"><span class="attribute">ssl_certificate</span>/etc/crt.crt;<span class="comment">#证书crt的绝对路径;</span></span><br><span class="line"><span class="attribute">ssl_certificate_key</span>    /etc/private.key;<span class="comment">#私钥key的绝对路径;</span></span><br><span class="line"><span class="attribute">location</span> / &#123;</span><br><span class="line"><span class="attribute">proxy_set_header</span>   X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line"><span class="attribute">proxy_set_header</span>   Host      <span class="variable">$http_host</span>;</span><br><span class="line"><span class="attribute">proxy_set_header</span> X-Forwarded-Proto <span class="variable">$scheme</span>;</span><br><span class="line"><span class="attribute">proxy_pass</span> http://127.0.0.1:2368;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>保存退出，然后重启 <code>Nginx</code> 生效配置：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/etc/init.d/nginx restart</span><br></pre></td></tr></table></figure></p><h4 id="1-3-安装-MySQL-并配置"><a href="#1-3-安装-MySQL-并配置" class="headerlink" title="1.3 安装 MySQL 并配置"></a>1.3 安装 <code>MySQL</code> 并配置</h4><p>Ghost 默认使用 <code>sqlite3 数据库</code> ， <code>sqlite3 数据库</code> 功能简约，小型化，追求最大磁盘效率，而 <code>MySQL</code> 是完善的服务器数据库，功能全面，综合化，追求最大并发效率，所以我这里使用的是 <code>MySQL</code> ，下面是操作命令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-get install mysql-server <span class="comment"># 安装Mysql  </span></span><br><span class="line">/etc/init.d/mysql restart <span class="comment"># 启动/重新启动Mysql</span></span><br><span class="line">mysql_secure_installation <span class="comment">#配置Mysql</span></span><br></pre></td></tr></table></figure></p><p>输入 <code>mysql_secure_installation</code> 回车后，系统可能会询问一些 <code>MySQL</code> 的用户密码安全措施，建议在设置时选择 <code>1 or MEDIUM</code> 即可，这是说明你的 <code>MySQL</code> 用户密码必须包括”数字、大写字母、小写字母、特殊字符”，部分配置的解释为：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Set root password? [Y/n] <span class="comment"># 设置root密码  </span></span><br><span class="line">anonymous users? [Y/n] <span class="comment"># 删除匿名用户  </span></span><br><span class="line">Disallow root login remotely? [Y/n] <span class="comment"># 禁止root用户远程登录  </span></span><br><span class="line">Remove <span class="built_in">test</span> database and access to it? [Y/n] <span class="comment"># 删除默认的 test 数据库  </span></span><br><span class="line">Reload privilege tables now? [Y/n] <span class="comment"># 刷新授权表使修改生效</span></span><br></pre></td></tr></table></figure></p><p>为了防止在 <code>MySQL</code> 数据库中出现中文乱码，需要设置 <code>MySQL</code> 的编码，编辑 <code>/etc/mysql/my.cnf</code> 写入如下信息后保存退出，并重启 <code>MySQL</code> 服务：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[client]</span><br><span class="line">default-character-set=utf8mb4</span><br><span class="line">[mysql]</span><br><span class="line">default-character-set=utf8mb4</span><br><span class="line">[mysqld]</span><br><span class="line">character-set-server=utf8mb4</span><br><span class="line">collation-server=utf8mb4_unicode_ci</span><br><span class="line">skip-character-set-client-handshake = <span class="literal">true</span></span><br></pre></td></tr></table></figure></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/etc/init.d/mysql restart <span class="comment">#重启MySQL服务</span></span><br></pre></td></tr></table></figure><p>然后我们新建一个数据库用来存放博客中的文章等信息，并且建立一个用户管理该数据库，由于我们之前设置的 <code>MySQL</code> 数据库用户密码策略，所以密码必须包含”数字、大写字母、小写字母、特殊字符”：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -u root -p <span class="comment"># 输入设置好的密码  </span></span><br><span class="line">create database ghost; <span class="comment"># 创建ghost数据库  </span></span><br><span class="line"><span class="comment"># 新建一个用户ghost，密码为123456Aa!并将ghost数据库授权给它</span></span><br><span class="line">grant all privileges on ghost.* to <span class="string">'ghost'</span>@<span class="string">'%'</span> identified by <span class="string">'123456Aa!'</span>; </span><br><span class="line">flush privileges <span class="comment"># 重新读取权限表中的数据到内存，不用重启mysql就可以让权限生效</span></span><br></pre></td></tr></table></figure></p><h4 id="1-4-安装-Ghost-并配置"><a href="#1-4-安装-Ghost-并配置" class="headerlink" title="1.4 安装 Ghost 并配置"></a>1.4 安装 <code>Ghost</code> 并配置</h4><p>下载并解压Ghost：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /var/www</span><br><span class="line">wget https://ghost.org/zip/ghost-latest.zip</span><br><span class="line">unzip Ghost-0.11.9.zip -d ghost</span><br><span class="line"><span class="built_in">cd</span> ghost</span><br></pre></td></tr></table></figure></p><p>修改配置：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cp config.example.js config.js  </span><br><span class="line">vi config.js</span><br></pre></td></tr></table></figure></p><p> <code>Ghost</code> 有生产模式、开发模式和测试模式等多种运行模式，这里我们需要在配置文件中找到 <code>production</code> 模式：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 生产模式</span></span><br><span class="line">production: &#123;  </span><br><span class="line">    url: <span class="string">'http://snowz.me'</span>, <span class="comment"># 修改为你的域名或者IP，注意加上http://</span></span><br><span class="line">    mail: &#123;&#125;,</span><br><span class="line">    database: &#123;</span><br><span class="line">        client: <span class="string">'mysql'</span></span><br><span class="line">        connection: &#123;</span><br><span class="line">            host     : <span class="string">'127.0.0.1'</span>,</span><br><span class="line">            user     : <span class="string">'ghost'</span>, <span class="comment"># 数据库连接的用户</span></span><br><span class="line">            password : <span class="string">'123456Aa!'</span>, <span class="comment"># 先前创建的密码</span></span><br><span class="line">            database : <span class="string">'ghost'</span>, <span class="comment"># 先前创建的数据库</span></span><br><span class="line">            charset  : <span class="string">'utf8'</span></span><br><span class="line">        &#125;,</span><br><span class="line">    server: &#123;</span><br><span class="line">            host: <span class="string">'127.0.0.1'</span>,</span><br><span class="line">            port: <span class="string">'2368'</span> <span class="comment"># 若修改该端口记得在nginx中做相应改变</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><p>接下来下载 <code>Ghost</code> 所需要的依赖包：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm config <span class="built_in">set</span> registry https://registry.npm.taobao.org </span><br><span class="line">npm info underscore（如果上面配置正确这个命令会有字符串response）</span><br><span class="line">npm install --production --unsafe-perm</span><br></pre></td></tr></table></figure></p><blockquote><p> 上面代码的解释如下：</p></blockquote><ul><li><code>npm config set registry https://registry.npm.taobao.org</code> 是修改一下 <code>npm</code> 的镜像源，避免因为 <code>GFW</code> 而下载依赖包失败，其他修改方法见<a href="https://cnodejs.org/topic/4f9904f9407edba21468f31e" target="_blank">这里</a>；</li><li>添加 <code>--unsafe-perm</code> 的参数的原因是：如果我们不添加这个参数，会出现类似于 <code>npm WARN cannot run in wd test@0.0.0 echo something (wd=/Users/Lloyd/Documents/test)</code> 的错误，原因是如果使用 <code>root</code> 权限调用 <code>npm</code> ，那么它会将 <code>uid</code> 更改为用户配置指定的 <code>uid</code> ，默认为 <code>nobody</code> ，设置 <code>--unsafe-perm</code> 参数以使用 <code>root</code> 权限运行脚本；</li></ul><h4 id="1-5-开机后博客自动运行"><a href="#1-5-开机后博客自动运行" class="headerlink" title="1.5 开机后博客自动运行"></a>1.5 开机后博客自动运行</h4><p>安装 <code>PM2</code> 让 <code>Ghost</code> 可以在开机后自动后台运行<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g cnpm --registry=https://registry.npm.taobao.org  </span><br><span class="line">cnpm install pm2 -g  </span><br><span class="line">NODE_ENV=production pm2 start index.js --name <span class="string">"ghost"</span>  </span><br><span class="line">pm2 startup ubuntu </span><br><span class="line">pm2 save</span><br></pre></td></tr></table></figure></p><p>我们需要打开 <code>/etc/rc.local</code> 文件在其中添加如下代码，让 <code>MySQL</code> 和 <code>Nginx</code> 开机自动运行：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/etc/init.d/mysql restart</span><br><span class="line">/etc/init.d/nginx restart</span><br></pre></td></tr></table></figure></p><h3 id="2-出现的一些问题"><a href="#2-出现的一些问题" class="headerlink" title="2.出现的一些问题"></a>2.出现的一些问题</h3><h4 id="2-1-config-js文件中的的URL配置https后，访问是浏览器提示重定向次数过多问题："><a href="#2-1-config-js文件中的的URL配置https后，访问是浏览器提示重定向次数过多问题：" class="headerlink" title="2.1 config.js文件中的的URL配置https后，访问是浏览器提示重定向次数过多问题："></a>2.1 config.js文件中的的URL配置https后，访问是浏览器提示重定向次数过多问题：</h4><p>之前就曾在安装完运行过程中发现网站的针对谷歌的复合搜索卡中的URL是http的，如果改成https，浏览器提示重定向次数过多。由于不妨碍使用，当时就没注意。</p><p>今天(<code>2017/06/03</code>) 在 Search Console 中发现 <code>我们在您的网站上未找到复合搜索卡的任何结构化数据。</code> 的提示信息。先通过谷歌的<a href="https://search.google.com/structured-data/testing-tool" target="_blank">结构化数据测试工具</a>检测了一番，发现我的复合搜索卡结构语法并没有错误，然后谷歌提供的<a href="https://support.google.com/webmasters/answer/6381755?#debugging" target="_blank">问题排查</a>，我发现是由于config.js文件中配置的是<a href="http://bugwz.com" target="_blank" rel="noopener">http://bugwz.com</a> ，而我在谷歌的Search Console中配置的是<a href="https://bugwz.com" target="_blank" rel="noopener">https://bugwz.com</a> ，导致Search Console 资源必须与托管网站不匹配，所以最后查阅一番，修正方法为在nginx的配置文件中的 <code>location /</code> 配置中加入如下代码：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">proxy_set_header X-Forwarded-Proto <span class="variable">$scheme</span>; //其中<span class="variable">$scheme</span>也可以直接写为https</span><br></pre></td></tr></table></figure></p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Ghost </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>利用Ngrok搭建私有的内网穿透服务器</title>
      <link href="/2017/04/27/ngrok-secure-tunnels-to-localhost/"/>
      <url>/2017/04/27/ngrok-secure-tunnels-to-localhost/</url>
      
        <content type="html"><![CDATA[<h3 id="0-关于Ngrok"><a href="#0-关于Ngrok" class="headerlink" title="0. 关于Ngrok"></a>0. 关于Ngrok</h3><p>Ngrok 是用Go语言写的一个反向代理软件，Ngrok 服务可以分配给你一个域名让你本地的web项目可供外网访问，Ngrok解决了内网穿透这十分棘手的问题，可以让我们不需要公网IP的情况下，轻松向他人展示本机的Web Demo等信息。Ngrok 官网本身还提供了公共服务，只需要注册一个帐号，运行它的客户端，就可以快速把内网映射出去,不过这么好的服务，没多久就被墙了,幸运的是，Ngrok的1.x的源码被公布了出来，我们可以利用它的源码去构建属于我们自己的Ngrok内网穿透服务器。</p><p>据官方所说由于 Ngrok 存在一些已知的问题，例如内存泄漏等对稳定使用影响较大，原文内容如下：</p><blockquote><p>DO NOT RUN THIS VERSION OF NGROK (1.X) IN PRODUCTION. Both the client and server are known to have serious reliability issues including memory and file descriptor leaks as well as crashes. There is also no HA story as the server is a SPOF. </p></blockquote><p>不过，当你使用了Ngrok之后，你绝对会对他赞不绝口，当你内网中的一台机器开着Ngrok时，即使那台机器突然断网了，只要电脑之后连接上了网络，Ngrok就会立马自动开始工作，很人性化。</p><h3 id="1-Linux下Ngrok服务器的搭建及客户端的生成"><a href="#1-Linux下Ngrok服务器的搭建及客户端的生成" class="headerlink" title="1. Linux下Ngrok服务器的搭建及客户端的生成"></a>1. Linux下Ngrok服务器的搭建及客户端的生成</h3><ul><li>服务器环境：CentOS 7.2 64位/Ubuntu Server 16.04.1 LTS 64位</li><li>客户端环境：Windows 10 版本10.0.14393</li></ul><h4 id="1-1-：搭建Ngrok服务器所必需的环境条件"><a href="#1-1-：搭建Ngrok服务器所必需的环境条件" class="headerlink" title="1.1 ：搭建Ngrok服务器所必需的环境条件"></a>1.1 ：搭建Ngrok服务器所必需的环境条件</h4><ul><li><p>CentOS下的命令：<br>更新系统软件环境，并安装所需要的mercurial git gcc golang软件包：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum update</span><br><span class="line">yum install mercurial git gcc golang</span><br></pre></td></tr></table></figure></li><li><p>Ubuntu Server下的命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install build-essential golang mercurial git</span><br></pre></td></tr></table></figure></li></ul><h4 id="1-2-：下载Ngrok的源码"><a href="#1-2-：下载Ngrok的源码" class="headerlink" title="1.2 ：下载Ngrok的源码"></a>1.2 ：下载Ngrok的源码</h4><p>假如我们当前处于/root目录，然后我们需要从GitHub上面下载下来Ngrok的源码文件，下面提供三个link，一个官方地址(可能会报错)，一个第三方地址，一个是我Fork别人的。由于访问 github 不太顺畅，有可能下载的时候链接会中断，所以如果出错了就再运行一遍。执行完<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/inconshreveable/ngrok.git</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/tutumcloud/ngrok.git</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/CUBEGWZ/ngrok.git</span><br></pre></td></tr></table></figure></p><p>执行完上面的代码后，我们 <code>ls</code> 就可以发现/root/目录下存在一个ngrok目录。</p><h4 id="1-3-：生成证书"><a href="#1-3-：生成证书" class="headerlink" title="1.3 ：生成证书"></a>1.3 ：生成证书</h4><p>我们在此之前需要确定已经有一个域名可以成功的泛解析映射到目标服务器上了。由于我们下载的Ngrok源码是官方提供的，所以其中的SSL证书当然不是针对目前我们所拥有的域名的，所以我们需要为当前域名配置SSL证书，并把信息写入服务器文件中去，还有一点，我们之后编译客户端的时候也会将这些信息编译进客户端，客户端内的信息必须与服务器端的信息相一致。</p><p>此处假设我想让使用我的Ngrok服务的人们那里获得的域名形似为”*.ngrok.testbug.top”，那么接下来我的设置如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /root/ngrok</span><br><span class="line"><span class="comment">#这里修改为自己想要设置的域名变量，便于后面使用</span></span><br><span class="line">NGROK_DOMAIN=<span class="string">"ngrok.testbug.top"</span></span><br><span class="line"></span><br><span class="line">openssl genrsa -out rootCA.key 2048</span><br><span class="line">openssl req -x509 -new -nodes -key rootCA.key -subj <span class="string">"/CN=<span class="variable">$NGROK_DOMAIN</span>"</span> -days 5000 -out rootCA.pem</span><br><span class="line">openssl genrsa -out device.key 2048</span><br><span class="line">openssl req -new -key device.key -subj <span class="string">"/CN=<span class="variable">$NGROK_DOMAIN</span>"</span> -out device.csr</span><br><span class="line">openssl x509 -req -<span class="keyword">in</span> device.csr -CA rootCA.pem -CAkey rootCA.key -CAcreateserial -out device.crt -days 5000</span><br></pre></td></tr></table></figure></p><p>然后我们会发现/root/ngrok目录下会多出六个文件，他们分别是”rootCA.key”,”rootCA.pem”,”rootCA.srl”,”device.key”,”device.csr”,”device.crt”，然后我们将”rootCA.pem”,”device.crt”,”device.key”这三个文件复制替换/root/ngrok/<a href="http://cdn.bugwz.com/client/tls/目录下的三个文件，代码如下所示：" target="_blank" rel="noopener">http://cdn.bugwz.com/client/tls/目录下的三个文件，代码如下所示：</a><br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">\cp rootCA.pem http://cdn.bugwz.com/client/tls/ngrokroot.crt -f</span><br><span class="line">\cp device.crt http://cdn.bugwz.com/server/tls/snakeoil.crt  -f</span><br><span class="line">\cp device.key http://cdn.bugwz.com/server/tls/snakeoil.key -f</span><br></pre></td></tr></table></figure></p><h4 id="1-4-：生成服务器端-ngrokd-与编译客户端的ngrok"><a href="#1-4-：生成服务器端-ngrokd-与编译客户端的ngrok" class="headerlink" title="1.4 ：生成服务器端 ngrokd 与编译客户端的ngrok"></a>1.4 ：生成服务器端 ngrokd 与编译客户端的ngrok</h4><p>进入/root/ngrok目录后创建服务器端的 ngrokd，代码如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /root/ngrok</span><br><span class="line">make release-server</span><br></pre></td></tr></table></figure></p><p>如果提示下载失败，可能是因为链接国外的服务器会断线的问题，可重新运行一遍 make release-server 。执行成功后，我们可以在/root/ngrok/bin/目录下看到 ngrokd 这个文件，这个就是我们后面要开启的服务器端，现在先不要运行。</p><p>然后我们在/root/ngrok/目录中使用交叉编译，编译出几个常用平台的客户端软件，代码如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">GOOS=linux GOARCH=amd64 make release-client</span><br><span class="line">GOOS=windows GOARCH=amd64 make release-client</span><br><span class="line">GOOS=linux GOARCH=arm make release-client</span><br></pre></td></tr></table></figure></p><p>编译后的文件存在于/root/ngrok/bin/目录中，他们分别在以自己的操作系统名命名的目录中。我们把生成的文件下载下来存在客户端即可。</p><p>需要注意的是，不同平台使用不同的 GOOS 和 GOARCH，其中GOOS是指编译出来的操作系统 (windows,linux,darwin) ；GOARCH是指对应的构架 (386,amd64,arm)，列表如下：</p><ul><li>Linux 平台 32 位系统：GOOS=linux GOARCH=386</li><li>Linux 平台 64 位系统：GOOS=linux GOARCH=amd64</li><li>Windows 平台 32 位系统：GOOS=windows GOARCH=386</li><li>Windows 平台 64 位系统：GOOS=windows GOARCH=amd64</li><li>MAC 平台 32 位系统：GOOS=darwin GOARCH=386</li><li>MAC 平台 64 位系统：GOOS=darwin GOARCH=amd64</li><li>ARM 平台：GOOS=linux GOARCH=arm</li></ul><h4 id="1-5-：服务器端-ngrokd-与客户端的ngrok的运行测试"><a href="#1-5-：服务器端-ngrokd-与客户端的ngrok的运行测试" class="headerlink" title="1.5 ：服务器端 ngrokd 与客户端的ngrok的运行测试"></a>1.5 ：服务器端 ngrokd 与客户端的ngrok的运行测试</h4><p>首先我们让服务器端的ngrok开始运行，代码如下所示（三选一即可）：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#cd /root/ngrok</span></span><br><span class="line"><span class="comment">#NGROK_DOMAIN="ngrok.testbug.top"</span></span><br><span class="line"><span class="comment">#只启用http连接</span></span><br><span class="line">/root/ngrok/bin/ngrokd -domain=<span class="string">"ngrok.testbug.top"</span> -httpAddr=<span class="string">":6060"</span> -httpsAddr=<span class="string">":6061"</span> -tunnelAddr=<span class="string">":6062"</span></span><br><span class="line"><span class="comment">#启用http和https连接</span></span><br><span class="line">/root/ngrok/bin/ngrokd -domain=<span class="string">"ngrok.testbug.top"</span> -httpAddr=<span class="string">":6060"</span> -httpsAddr=<span class="string">":6061"</span> -tunnelAddr=<span class="string">":6062"</span> -tlsKey=/root/ngrok/device.key -tlsCrt=/root/ngrok/device.crt</span><br><span class="line"><span class="comment">#如果想让服务器端在我们关闭了终端后依旧可以后台运行，选择下面这句代码，后台运行，启用http和https连接</span></span><br><span class="line">/usr/bin/nohup /root/ngrok/bin/ngrokd -domain=<span class="string">"ngrok.testbug.top"</span> -httpAddr=<span class="string">":6060"</span> -httpsAddr=<span class="string">":6061"</span> -tunnelAddr=<span class="string">":6062"</span> -tlsKey=/root/ngrok/device.key -tlsCrt=/root/ngrok/device.crt &gt; /root/ngrok/out.file 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure></p><p>上面代码中的一些说明：</p><ul><li>httpAddr 是访问普通的http使用的端口号，客户端用 <a href="#">http://*.ngrok.testbug.top:6060</a> 来访问服务</li><li>httpsAddr 是访问的https使用的端口号,客户端用 <a href="#">https://*.ngrok.testbug.top:6060</a> 来访问服务</li><li>tunnelAddr 是通道的端口号，这个端口是Ngrok用来通信的，所以这个端口在服务器上和客户端上设置必须要对应才可以正常的链接，默认不填写好像是4433</li></ul><p>如果想要开机启动Ngrokd服务，并开启http和https连接服务，可以执行下面的操作：</p><ul><li>针对于CentOS中，在”/etc/rc.d/rc.local”文件最后加入下面的代码：</li><li>针对于Ubuntu Server，在”/etc/rc.local”文件的最后加入下面的代码:<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/bin/nohup /root/ngrok/bin/ngrokd -domain=<span class="string">"ngrok.testbug.top"</span> -httpAddr=<span class="string">":6060"</span> -httpsAddr=<span class="string">":6061"</span> -tunnelAddr=<span class="string">":6062"</span> -tlsKey=/root/ngrok/device.key -tlsCrt=/root/ngrok/device.crt &gt; /root/ngrok/out.file 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure></li></ul><p>接下来我们配置客户端，以便于我们在客户端可以成功建立与服务器的连接，并且最终完成内网穿透的需求；</p><p>我们客户端的环境是Windows 10 版本10.0.14393，把下载下来的ngrok.exe文件放在绝对路径中没有中文的文件夹中，然后在该文件夹中新建一个”ngrok.cfg”，页面编码选择为”UTF-8 无BOM格式”，然后里面输入如下内容并保存：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">server_addr: <span class="string">"ngrok.testbug.top:6062"</span></span><br><span class="line">trust_host_root_certs: <span class="literal">false</span></span><br></pre></td></tr></table></figure></p><ul><li><p>如果想要在外网访问内网中的Web服务，则可以使用下面的代码：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ngrok.exe -<span class="built_in">log</span>=ngrok_log.txt -subdomain=<span class="built_in">test</span> -config=<span class="string">"ngrok.cfg"</span> 80</span><br></pre></td></tr></table></figure></li><li><p>如果想要在外网访问内网中的TCP服务，则可以使用下面的代码(例如访问内网中机器的TCP协议的3389端口)：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ngrok.exe -<span class="built_in">log</span>=ngrok_log.txt -subdomain=<span class="built_in">test</span> -config=<span class="string">"ngrok.cfg"</span> -proto=tcp 3389</span><br></pre></td></tr></table></figure></li></ul><p>上面代码的解释为：</p><ul><li>日志： -log=ngrok_log.txt 是记录ngrok的日志，如果前期调试的时候加上这个参数，如果不能访问就可以查看到底是什么问题</li><li>子域名： -subdomain=test 是定义访问的时候的子域名，现在访问 ngrok.testbug.top:6060 就可以访问到这一台机器上80端口的服务</li></ul><p><strong><em>备注  常用的工作在TCP协议上的端口列表如下：</em></strong></p><ul><li>53 ：MTP，邮件传输协议</li><li>80 ：HTTP（超文本传输协议）- 用于传输网页</li><li>110 ：POP3，“邮局协议”，第3版 - 用于接收电子邮件</li><li>115 ：SFTP，简单文件传输协议</li><li>3389 ：远程桌面协议（RDP）</li><li>8008/8080 ：HTTP 替代端口</li><li>8080 : Apache Tomcat</li></ul><h3 id="2-一些基于的Ngrok的免费内网穿透服务"><a href="#2-一些基于的Ngrok的免费内网穿透服务" class="headerlink" title="2. 一些基于的Ngrok的免费内网穿透服务"></a>2. 一些基于的Ngrok的免费内网穿透服务</h3><h4 id="2-1-：NATAPP-基于ngrok高速内网穿透"><a href="#2-1-：NATAPP-基于ngrok高速内网穿透" class="headerlink" title="2.1 ：NATAPP 基于ngrok高速内网穿透"></a>2.1 ：<a href="https://natapp.cn/" target="_blank">NATAPP 基于ngrok高速内网穿透</a></h4><p>第一次使用需要注册，竟然只能用手机号注册，注册后可以在个人页面看到存在免费隧道可用，阿里云国内服务器，不支持绑定自己的域名，不支持Https，1M的带宽(估计也是共享带宽)，一分钟60个连接数的限制，最大TCP连接数五个，分配的三级域名以及端口还不定时强制更换，更恶心的是使用这个免费隧道还需要实名认证，不推荐！极不推荐！</p><h4 id="2-2-：Ngrok国内免费服务器"><a href="#2-2-：Ngrok国内免费服务器" class="headerlink" title="2.2 ：Ngrok国内免费服务器"></a>2.2 ：<a href="http://qydev.com/" target="_blank">Ngrok国内免费服务器</a></h4><p>这是我使用的第一个Ngrok内网穿透服务，这应该是一个学生免费提供的的Ngrok内网穿透服务，当初应该是为了贡献闲置资源，现在我感觉用起来越来越舒服，一不需要注册，二不需要实名，三不需要花钱，简直就是Ngrok内网穿透的福音呀！并且还十分稳定，十分良心，建议大家如果觉的好用，给人家捐赠点维护费用，利人利己。强烈推荐这款服务！</p><h4 id="2-3-：Sunny-Ngrok内网转发"><a href="#2-3-：Sunny-Ngrok内网转发" class="headerlink" title="2.3 ：Sunny-Ngrok内网转发"></a>2.3 ：<a href="https://www.ngrok.cc/" target="_blank">Sunny-Ngrok内网转发</a></h4><p>这也是一个体验性挺好的Ngrok内网穿透服务，第一个看到这网站的界面我就猜到了他应该是基于ThinkCMF写的，虽然如果你要使用这个也需要注册，但是人家的注册就不想某些人那样，人家直接使用邮箱就可以注册，还不用邮箱验证，估计邮箱也就是为了找回密码用的，后台界面很简洁，提供两种隧道供选择，一种是香港100M服务器，10元/月，一种是香港10M免费的，它的免费隧道可以固定自己的三级域名，也可以把自己的域名CNAME解析到server.ngrok.cc从而使用自定义域名，还有一个有意思的是，它可以设置在http映射的时候是否需要访问认证，这有加了一层安全保障，关键是这些都是免费的，更好的是，似乎我们可以建很多的免费的隧道，这就很好了，也是值得推荐的！</p><h4 id="2-4-：魔法隧道"><a href="#2-4-：魔法隧道" class="headerlink" title="2.4 ：魔法隧道"></a>2.4 ：<a href="http://mofasuidao.cn/" target="_blank">魔法隧道</a></h4><p>据说(知乎上一个人说的)是所谓的最稳定的Ngrok，注册也是需要手机号码的，好吧，我不计较是不是需要用手机号了，登进去管理界面才发现，好嘛~签到送流量，我点击了签到，好嘛~送了我 0.90M ，果断关了。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Ngrok </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>读《Web性能优化与HTTP/2》有感笔记</title>
      <link href="/2017/04/26/web-performance-http2/"/>
      <url>/2017/04/26/web-performance-http2/</url>
      
        <content type="html"><![CDATA[<h3 id="0-前言"><a href="#0-前言" class="headerlink" title="0.前言"></a>0.前言</h3><p>前段时间本着尝鲜与想释放看书的欲望的初衷入手了Kindle PaperWhite 3，买来后便把之前一直想看但迫于书籍的沉重与携带的不便而没看的书籍塞了进去，其中有一本叫做《Web性能优化与HTTP/2》，这是从看云上找到的一本书籍，被题目所吸引，但是放入后才发现这本书中并没有多少字，但是牵扯出的东西却太多了，所以打算写这么一篇，记录一下自己的感受与学习。</p><blockquote><p>下面以书中所提及的知识点为主线，记录我对于各个知识点的学习与感受</p></blockquote><h3 id="1-Http-304"><a href="#1-Http-304" class="headerlink" title="1. Http 304"></a>1. Http 304</h3><p>304 Not Modified是一个在网页浏览过程中不会直接发现的一个提示，在正常浏览网页的时候用户不可见，只有当我们打开Console控制台的时候才会发现，请求列表中存在304响应状态码。</p><p>如果客户端发送了一个带条件的GET请求且该请求已被允许，而文档的内容（自上次访问以来或者根据请求的条件）并没有改变，则服务器应当返回这个状态码。304响应禁止包含消息体，因此始终以消息头后的第一个空行结尾。</p><h4 id="1-1-Http-304的响应状态的资源更新机制："><a href="#1-1-Http-304的响应状态的资源更新机制：" class="headerlink" title="1.1 Http 304的响应状态的资源更新机制："></a>1.1 Http 304的响应状态的资源更新机制：</h4><ul><li>可能请求一：当客户端缓存了目标资源但不确定该缓存资源是否是最新版本的时候, 就会发送一个条件请求，这样就可以辨别出一个请求是否是条件请求，在进行条件请求时,304请求的响应头信息里面有两个比较重要的请求头字段：If-Modified-Since【其值为服务器上次返回的Last-Modified响应头中的Date日期值】和 If-None-Match【其值为服务器上次返回的ETag响应头的值】，这两个字段表示发送的是一个条件请求。 <ul><li>结果一：服务器会读取到这两个请求头中的值,判断出客户端缓存的资源是否是最新的,如果是的话,服务器就会返回HTTP/304 Not Modified响应头, 但没有响应体。客户端收到304响应后,就会从本地缓存中读取对应的资源. </li><li>结果二：服务器认为客户端缓存的资源已经过期了,那么服务器就会返回HTTP/200 OK响应,响应体就是该资源当前最新的内容。客户端收到200响应后,就会用新的响应体覆盖掉旧的缓存资源。</li></ul></li><li>可能请求二：如果客户端第一次请求该资源或者请求该资源的响应头不存在了Last-Modified和ETag请求头字段,则必须无条件(unconditionally)请求该资源,服务器也就必须返回完整的资源数据。</li></ul><h4 id="1-2-使用条件请求机制的原因："><a href="#1-2-使用条件请求机制的原因：" class="headerlink" title="1.2 使用条件请求机制的原因："></a>1.2 使用条件请求机制的原因：</h4><ul><li>因为可以省去传输整个响应体的时间，所以条件请求可以加速网页的打开时间，但仍然会有网络延迟，因为浏览器还是得为每个资源生成一条条件请求，并且等到服务器返回HTTP/304响应，才能读取缓存来显示网页。</li></ul><h4 id="1-3-其他可用策略："><a href="#1-3-其他可用策略：" class="headerlink" title="1.3 其他可用策略："></a>1.3 其他可用策略：</h4><ul><li>如果服务器在响应上指定Cache-Control或Expires指令，这样客户端就能知道该资源的可用时间为多长，也就能跳过条件请求的步骤，直接使用缓存中的资源了。</li></ul><h3 id="2-gzip压缩Http-body"><a href="#2-gzip压缩Http-body" class="headerlink" title="2. gzip压缩Http body"></a>2. gzip压缩Http body</h3><p>gzip 是 GNUzip 的缩写，最早用于 UNIX 系统的文件压缩。HTTP 协议上的 gzip 编码是一种用来改进 web 应用程序性能的技术，web 服务器和客户端（浏览器）必须共同支持 gzip。目前主流的浏览器，Chrome,firefox,IE 等都支持该协议。常见的服务器如 Apache，Nginx，IIS 同样支持 gzip。gzip压缩比率在3到10倍左右，可以大大节省服务器的网络带宽。而在实际应用中，并不是对所有文件进行压缩，通常只是压缩静态文件。</p><h4 id="2-1-Web服务器处理HTTP压缩的过程图解："><a href="#2-1-Web服务器处理HTTP压缩的过程图解：" class="headerlink" title="2.1 Web服务器处理HTTP压缩的过程图解："></a>2.1 Web服务器处理HTTP压缩的过程图解：</h4><p><img src="http://cdn.bugwz.com/006qpCDTly1ff2bvzib3oj30vr0g40u6.jpg" alt="Web服务器处理HTTP压缩的过程"></p><h3 id="3-HSTS策略"><a href="#3-HSTS策略" class="headerlink" title="3. HSTS策略"></a>3. HSTS策略</h3><p>HTTP严格传输安全（英语：HTTP Strict Transport Security，缩写：HSTS）是一套由互联网工程任务组发布的互联网安全策略机制。网站可以选择使用HSTS策略，来让浏览器强制使用HTTPS与网站进行通信，以减少会话劫持风险。</p><h4 id="3-1-HSTS策略的作用以使用说明"><a href="#3-1-HSTS策略的作用以使用说明" class="headerlink" title="3.1 HSTS策略的作用以使用说明"></a>3.1 HSTS策略的作用以使用说明</h4><p>HSTS的作用是强制客户端（如浏览器）使用HTTPS与服务器创建连接。服务器开启HSTS的方法是，当客户端通过HTTPS发出请求时，在服务器返回的超文本传输协议响应头中包含Strict-Transport-Security字段。非加密传输时设置的HSTS字段无效。</p><p>比如，<a href="https://www.bugwz.com" target="_blank" rel="noopener">https://www.bugwz.com</a> 的响应头含有Strict-Transport-Security: max-age=31536000; includeSubDomains。这意味着两点：</p><ul><li>在接下来的一年（即31536000秒）中，浏览器只要向example.com或其子域名发送HTTP请求时，必须采用HTTPS来发起连接。比如，用户点击超链接或在地址栏输入 <a href="https://www.bugwz.com" target="_blank" rel="noopener">https://www.bugwz.com</a> ，浏览器应当自动将 http 转写成 https，然后直接向 <a href="https://www.bugwz.com" target="_blank" rel="noopener">https://www.bugwz.com</a> 发送请求。</li><li>在接下来的一年中，如果 <a href="https://www.bugwz.com" target="_blank" rel="noopener">https://www.bugwz.com</a> 服务器发送的TLS证书无效，用户不能忽略浏览器警告继续访问网站。</li></ul><h4 id="3-2-HSTS策略的一些问题"><a href="#3-2-HSTS策略的一些问题" class="headerlink" title="3.2 HSTS策略的一些问题"></a>3.2 HSTS策略的一些问题</h4><p>HSTS策略在它看到STS头部声明的max-age的期间内保护了客户端从Http到https跳转的过程中的可能的被拦截。然而，HSTS并不是http回话劫持的完美解决方案。用户在访问HSTS保护的网站时，在以下情况下仍然容易受到攻击：</p><ul><li>以前从未访问过该网站</li><li>最近重新安装了其操作系统</li><li>最近重新安装了其浏览器</li><li>切换到新的浏览器</li><li>切换到一个新的设备如移动电话</li><li>删除浏览器的缓存</li><li>最近没访问过该站并且max-age过期了</li></ul><p>为了解决这个问题，Google坚持维护了一个”HSTS preload list”的站点域名和子域名，并通过<a href="https://hstspreload.appspot.com/" target="_blank">https://hstspreload.appspot.com/</a>【需要额外的手段才可以顺畅访问】提交其域名。该域名列表被分发和硬编码到主流的web浏览器。客户端访问此列表中的域名将主动的使用HTTPS，并拒绝使用HTTP访问该站点。<br>一旦设置了STS头部或者提交了你的域名到HSTS预加载列表，这是不可能将其删除的。这是一个单向不可逆的决定了你的域名必须通过Https进行访问的方法。</p><h3 id="4-资源预加载"><a href="#4-资源预加载" class="headerlink" title="4. 资源预加载"></a>4. 资源预加载</h3><p>当我们访问一个页面的时候，该页面可能有一些资源存在很大的几率被用户点击查看，那么我们就可能需要对这些资源进行预加载，例如《Web性能优化与HTTP/2》这本书中所说的DNS预解析,这就可以减少一些DNS解析时间，提升用户访问的体验。资源预加载这种做法曾经被称为<code>prebrowsing</code>，但这并不是一项单一的技术，可以细分为几个不同的技术：<code>DNS-prefetch</code>、<code>subresource</code> 和标准的 <code>prefetch</code>、<code>preconnect</code>、<code>prerender</code>。</p><h4 id="4-1-DNS-预解析-DNS-Prefetch"><a href="#4-1-DNS-预解析-DNS-Prefetch" class="headerlink" title="4.1 DNS 预解析 DNS-Prefetch"></a>4.1 DNS 预解析 DNS-Prefetch</h4><p>当你浏览一个网页的时候，浏览器会在加载网页时对网页中包含的域名进行解析缓存，这样在你单击当前已经加载完成的网页中的链接时就无需再进行DNS 回源解析，减少用户的等待时间，提高用户体验。</p><p>操作方法跟简单，只需要在文档顶部的 <head><meta name="generator" content="Hexo 3.8.0"> 标签中加入以下代码(例如：其中的host可以为bugwz.com)：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"dns-prefetch"</span> <span class="attr">href</span>=<span class="string">"//host/"</span> /&gt;</span></span><br></pre></td></tr></table></figure></head></p><p>这似乎是一个非常微小的性能优化，显得也并非那么重要，但事实并非如此 – <a href="https://docs.google.com/presentation/d/18zlAdKAxnc51y_kj-6sWLmnjl6TLnaru_WH0LJTjP-o/present?slide=id.g120f70e9a_041" target="_blank" rel="noopener">Chrome 一直都做了类似的优化</a>。实际上，单纯执行 DNS-Prefetch 只能够微小的提升浏览性能，因为大部分现代浏览器也都内置了预解析的功能，甚至在你在地址栏输入域名时就完成了预解析。通过阅读Chormium 的文档，得到以下信息：</p><ul><li>不用对超链接做手动 dns prefetching，因为 chrome 会自动做 dns prefetching</li><li>chrome 会自动把当前页面的所有带 href 的 link 的 dns 都 prefetch 一遍</li><li>对于一些需要跳转的域名做好预解析，最多可以减少 300~500ms 的加载时间</li></ul><p>兼容性展示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ff2bxe3v6cj30zb0hcgmb.jpg" alt="DNS-Prefetch"></p><h4 id="4-2-预连接-Preconnect"><a href="#4-2-预连接-Preconnect" class="headerlink" title="4.2 预连接 Preconnect"></a>4.2 预连接 Preconnect</h4><p>与 DNS 预解析类似，<code>preconnect</code> 不仅完成 DNS 预解析，同时还将进行 TCP 握手和建立传输层协议。预先建立 socket 连接，从而消除昂贵的 DNS 查找、TCP 握手和 TLS 往返开销。使用方法是在文档顶部的 <head><meta name="generator" content="Hexo 3.8.0"> 标签中加入以下代码：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"preconnect"</span> <span class="attr">href</span>=<span class="string">"https://bugwz.com"</span> /&gt;</span></span><br></pre></td></tr></table></figure></head></p><p>兼容性展示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ff2bywyn6pj30za0ir0ti.jpg" alt="预连接 Preconnect"></p><h4 id="4-3-预获取-Prefetching"><a href="#4-3-预获取-Prefetching" class="headerlink" title="4.3 预获取 Prefetching"></a>4.3 预获取 Prefetching</h4><p>如果我们确定某个资源将来一定会被使用到，我们可以让浏览器预先请求该资源并放入浏览器缓存中。例如，一个图片和脚本或任何可以被浏览器缓存的资源，使用方法是在文档顶部的 <head><meta name="generator" content="Hexo 3.8.0"> 标签中加入以下代码：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"prefetch"</span> <span class="attr">href</span>=<span class="string">"image.png"</span> /&gt;</span></span><br></pre></td></tr></table></figure></head></p><p>Prefetching 有两种用法。其中 prefetch 为将来的页面提供了一种低优先级的资源预加载方式，而 subresource 为当前页面提供了一种高优先级的资源预加载。所以，如果资源是当前页面必须的，或者资源需要尽快可用，那么最好使用 subresource。用法如下：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"subresource"</span> <span class="attr">href</span>=<span class="string">"styles.css"</span> /&gt;</span></span><br></pre></td></tr></table></figure></p><p>注意：与 DNS 预解析不同，预获取真正请求并下载了资源，并储存在缓存中。但预获取还依赖于一些条件，某些预获取可能会被浏览器忽略，例如从一个非常缓慢的网络中获取一个庞大的字体文件。并且，Firefox 只会在浏览器闲置时进行资源预获取。目前，字体文件必须等到 DOM 和 CSS 构建完成之后才开始下载，使用预获取就可以轻松绕过该瓶颈。</p><p>兼容性展示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ff2bywyn6pj30za0ir0ti.jpg" alt="预连接 Preconnect"><br><img src="http://cdn.bugwz.com/006qpCDTly1ff2c0ao4mrj30za0hsq3m.jpg" alt="预获取 subresource"></p><h4 id="4-4-预渲染-Prerender"><a href="#4-4-预渲染-Prerender" class="headerlink" title="4.4 预渲染 Prerender"></a>4.4 预渲染 Prerender</h4><p>这是一个核武器，因为 prerender 可以预先加载文档的所有资源，代码如下：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"prerender"</span> <span class="attr">href</span>=<span class="string">"https://bugwz.com/"</span> /&gt;</span></span><br></pre></td></tr></table></figure></p><p>这类似于在一个隐藏的 tab 页中打开了某个链接 – 将下载所有资源、创建 DOM 结构、完成页面布局、应用 CSS 样式和执行 JavaScript 脚本等。当用户真正访问该链接时，隐藏的页面就切换为可见，使页面看起来就是瞬间加载完成一样。Google 搜索在其即时搜索页面中已经应用该技术多年了，微软也宣称在 IE11 中支持该特性。</p><p>需要注意的问题：</p><ul><li>不要滥用该特性，当你知道用户一定会点击某个链接时才可以进行预渲染，因为预加载的开销（抢占 CPU 资源，消耗电池，浪费带宽等）是高昂的，所以必须谨慎行事</li><li>使用 <a href="https://www.w3.org/TR/page-visibility/" target="_blank" rel="noopener">Page Visibility API</a> 可以防止页面真正可见前被执行</li></ul><p>兼容性展示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ff2c0v902ij30z90i0t9e.jpg" alt="预渲染 Prerender"></p><h4 id="4-5-Preload"><a href="#4-5-Preload" class="headerlink" title="4.5 Preload"></a>4.5 Preload</h4><p>preload 是一个新规范，与 prefetch 不同（可能被忽略）的是，浏览器一定会预加载该资源,使用代码如下：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"preload"</span> <span class="attr">href</span>=<span class="string">"image.png"</span> /&gt;</span></span><br></pre></td></tr></table></figure></p><p>兼容性展示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ff2c1f6rb0j30z90hu750.jpg" alt="Preload"></p><h3 id="5-手动管理缓存localStorage"><a href="#5-手动管理缓存localStorage" class="headerlink" title="5. 手动管理缓存localStorage"></a>5. 手动管理缓存localStorage</h3><p>localStorage是HTML5中的特性，来实现手动控制缓存。大概的思路是，在定义模块时，同时将模块的代码和版本号分别储存到localStorage，在下一次打算请求模块之前，我们先判断模块的最新版本是不是在localStorage中，将不存在的模块组合在一起，请求动态合并的资源。</p><h4 id="5-1-Cookie-LocalStorage-与-SessionStorage"><a href="#5-1-Cookie-LocalStorage-与-SessionStorage" class="headerlink" title="5.1 Cookie, LocalStorage 与 SessionStorage"></a>5.1 Cookie, LocalStorage 与 SessionStorage</h4><ul><li>Cookie：Cookie的大小限制为4KB左右，是网景公司的前雇员 Lou Montulli 在1993年3月的发明。它的主要用途有保存登录信息，比如你登录某个网站市场可以看到“记住密码”，这通常就是通过在 Cookie 中存入一段辨别用户身份的数据来实现的。</li><li><p>LocalStorage：LocalStorage 是 HTML5 标准中新加入的技术，它并不是什么划时代的新东西,早在 IE 6 时代，就有一个叫 userData 的东西用于本地存储，而当时考虑到浏览器兼容性，更通用的方案是使用 Flash。而如今，localStorage 被大多数浏览器所支持。创建的代码实例如下：</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">localStorage.lastname=<span class="string">"Smith"</span>;</span><br><span class="line"><span class="built_in">document</span>.write(localStorage.lastname);</span><br></pre></td></tr></table></figure></li><li><p>sessionStorage：sessionStorage 与 localStorage 的接口类似，但保存数据的生命周期与 localStorage 不同。做过后端开发的同学应该知道 Session 这个词的意思，直译过来是“会话”。而 sessionStorage 是一个前端的概念，它只是可以将一部分数据在当前会话中保存下来，刷新页面数据依旧存在。但当页面关闭后，sessionStorage 中的数据就会被清空。创建的代码实例如下：</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">sessionStorage.lastname=<span class="string">"Smith"</span>;</span><br><span class="line"><span class="built_in">document</span>.write(sessionStorage.lastname);</span><br></pre></td></tr></table></figure></li><li><p>三者对比详情如下所示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ff2c1zus6xj30t809e0tk.jpg" alt="sessionStorage：sessionStorage 与 localStorage对比"></p></li></ul><h3 id="5-2-需要注意的一些地方"><a href="#5-2-需要注意的一些地方" class="headerlink" title="5.2 需要注意的一些地方"></a>5.2 需要注意的一些地方</h3><ul><li>严禁将一些敏感数据放置在Cookie、localStorage 和 sessionStorage 中，因为只要打开Console控制台我就可以查看并修改这些存储在本地的值。</li><li>假如同域下的其他页面被XSS攻击，攻击者就可以篡改localStorage的内容，可能导致原来的页面代码被植入恶意程序。</li><li>在执行每个网页模块之前，需要计算一下代码摘要，对比下服务器给的该模块的摘要，再决定是否使用，也可以使用SRI策略(关于SRI策略的详解信息，可移步<a href="https://imququ.com/post/subresource-integrity.html" target="_blank">这里</a>)，由浏览器帮你做校验。</li></ul><h3 id="6-HTTP持久连接-keep-alive和persistent"><a href="#6-HTTP持久连接-keep-alive和persistent" class="headerlink" title="6. HTTP持久连接 keep alive和persistent"></a>6. HTTP持久连接 keep alive和persistent</h3><p>HTTP持久连接可以避免每次都经历缓慢的连接建立阶段，减少三次握手的RTT延迟，以及每次都执行关闭操作，节省耗时和带宽；避免TCP连接慢启动特性的拥塞适应阶段，从而利用重用TCP连接这一措施加速数据传输。一个客户端对任何服务器或代理最多只能维护两条持久连接，以防服务器过载。HTTP持久连接的两种类型为：</p><ul><li>HTTP/1.0+ “keep-alive”连接</li><li>HTTP/1.1 “persistent”连接</li></ul><h4 id="6-1-HTTP-1-0-keep-alive连接"><a href="#6-1-HTTP-1-0-keep-alive连接" class="headerlink" title="6.1 HTTP/1.0+ keep-alive连接"></a>6.1 HTTP/1.0+ keep-alive连接</h4><p>HTTP/1.0+中支持的是keep-alive连接，keep-alive握手过程如下所示：</p><ul><li><p>HTTP/1.0+支持keep-alive连接，但默认并未激活。客户端通过发送一个包含Connection: Keep-Alive首部的请求来请求服务器激活keep-alive连接，即将这条连接保持在打开状态。</p></li><li><p>如果服务器愿意为下一条请求重用此连接，就会在响应中包含相同的首部。若没有，服务器就会在发回响应报文后关闭连接。客户端就是通过检测响应中是否包含Connection: Keep-Alive响应首部来判断服务器是否会在发送响应后关闭连接</p></li><li><p>假如服务器同意使用keep-alive连接，那么接下来客户端必须在所有希望保持持久连接的请求中包含Connection: Keep-Alive首部。如果没有发送该首部，服务器会在那条请求后关闭连接。</p></li><li><p>注意，Connection: Keep-Alive首部只是请求将连接保持在活跃状态。即使服务器和客户端都同意建立持久连接了，它们仍可以在任意时刻关闭空闲的keep-alive连接，且可随意限制keep-alive连接所处理事务的数量。我们可以通过Keep-Alive选项调节它们的行为，具体请看下一部分。</p></li></ul><p>Keep-Alive选项解释说明：<br><figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Connection</span>: Keep-Alive</span><br><span class="line"><span class="attribute">Keep-Alive</span>: max=5, timeout=120</span><br></pre></td></tr></table></figure></p><ul><li>参数timeout：在Keep-Alive响应首部中发送，告诉客户端服务器估计会在打开状态保持到连接空闲多长时间后关闭连接。</li><li>参数max：在Keep-Alive响应首部中发送，告诉客户端服务器还会为另外几个http事务将连接保持在打开状态。</li><li>注意，这两个参数值仅仅是估计，并非承诺。</li></ul><h4 id="6-2-HTTP-1-1的persistent连接"><a href="#6-2-HTTP-1-1的persistent连接" class="headerlink" title="6.2 HTTP/1.1的persistent连接"></a>6.2 HTTP/1.1的persistent连接</h4><ul><li><p>HTTP/1.1逐渐停止了对keep-alive连接的支持，用persistent连接替代了它，与keep-alive连接不同，HTTP/1.1中persistent连接默认就是激活的，除非特别指明，否则HTTP/1.1认为所有连接都是持久的。</p></li><li><p>HTTP/1.1的客户端假定在收到的响应后，除非报文包含了Connection: Close首部，否则客户端就认为连接仍为维持在打开状态。如果客户端要建立一个非持久连接，则需要在请求中包含Connection: Close首部；服务器在处理完该事务后，就会在响应中包含Connection: Close首部以告知客户端连接已关闭。如果客户端不想在一条persistent连接上发送更多请求了，就应该在最后一条请求中包含Connection: Close首部。</p></li><li><p>只要服务器决定在事务处理结束后关闭连接，就必须在响应中包含Connection: Close首部。但不发送Connection: Close首部也并不意味着服务器承诺永远将连接保持在打开状态。同样地，不管连接是否维持在打开状态，或Connection首部取了什么值，客户端和服务器仍然可以随时关闭空闲连接。</p></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>用C#编写的一个IP地址修改器</title>
      <link href="/2017/01/05/ip-address-modifier/"/>
      <url>/2017/01/05/ip-address-modifier/</url>
      
        <content type="html"><![CDATA[<h3 id="编写初衷："><a href="#编写初衷：" class="headerlink" title="编写初衷："></a>编写初衷：</h3><p>在学校的时候很多时候需要更改自己电脑的IP地址，比如机房课程设计的时候，拔掉机房的网线插到自己电脑上的时候，每次都得配上机房的IP地址，下午下课回去后还得自己更改为自动获取IP地址，很是烦人。之后我曾经用过BAT的方式去修改电脑的IP地址等信息，之前用起来效果也十分不错，但是毕竟添加IP地址等信息还得去编辑BAT，也不是十分方便，对于一些小白用户来说多少也是个麻烦事，并且之前的那个BAT需要手动以管理员方式运行，也比较麻烦，为此打算用C#写一个小程序，方便的来改变电脑的IP地址等信息。</p><h3 id="软件详细介绍："><a href="#软件详细介绍：" class="headerlink" title="软件详细介绍："></a>软件详细介绍：</h3><p>该IPAddressModifier软件使用Microsoft Visual Studio 2015这款IDE使用C#进行编写，整体上只设计了两个窗体，一个是当前网络适配器详情以及预览预设置IP地址等信息的窗体，还有一个是针对预选IP地址等信息的操作窗体，整体的耗时大概一周左右，其实时间应该是两周左右，因为中间有一些考试，我还需要好好复习一下，所以一共做的时间应该是一周左右，因为本人C#的技能并不是很好，也想把这次当作C#的一次复习，所以就是边查边做了。开始进入界面，并详细介绍。</p><h5 id="第一个窗体截图如下："><a href="#第一个窗体截图如下：" class="headerlink" title="第一个窗体截图如下："></a>第一个窗体截图如下：</h5><p><img src="http://cdn.bugwz.com/006qpCDTgw1fbfwky13tnj30970ck0t3.jpg" alt="Main Interface"></p><p>打开这个窗体的时候需要使用管理员权限打开，打开时会有该软件需要管理员权限等的提示，然后就针对“管理员权限”这一点详细说明一下；之前我是在<code>app.manifest</code>中修改后的如下代码启用管理员权限：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;requestedExecutionLevel  level=&quot;requireAdministrator&quot; uiAccess=&quot;false&quot; /&gt;</span><br></pre></td></tr></table></figure><p>因为我当初的想法是将这个软件打包发行，但是后来出现的问题是VS无法在这种情况下打包，总是出现类似于“ClickOnce”之类的错误，我尝试按照网络上一些人的建议进行修改，最后仍然会出现这种错误，最后使用在<code>Program.cs</code>文件中的设置修改如下代码完成管理员权限的赋予：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">static void Main()</span><br><span class="line">        &#123;</span><br><span class="line">            //Application.EnableVisualStyles();</span><br><span class="line">            //Application.SetCompatibleTextRenderingDefault(false);</span><br><span class="line">            //Application.Run(new IPAddressModifier());</span><br><span class="line">            Application.EnableVisualStyles();</span><br><span class="line">            Application.SetCompatibleTextRenderingDefault(false);</span><br><span class="line"></span><br><span class="line">            /**</span><br><span class="line">             * 当前用户是管理员的时候，直接启动应用程序</span><br><span class="line">             * 如果不是管理员，则使用启动对象启动程序，以确保使用管理员身份运行</span><br><span class="line">             */</span><br><span class="line">            //获得当前登录的Windows用户标示</span><br><span class="line">            System.Security.Principal.WindowsIdentity identity = System.Security.Principal.WindowsIdentity.GetCurrent();</span><br><span class="line">            System.Security.Principal.WindowsPrincipal principal = new System.Security.Principal.WindowsPrincipal(identity);</span><br><span class="line">            //判断当前登录用户是否为管理员</span><br><span class="line">            if (principal.IsInRole(System.Security.Principal.WindowsBuiltInRole.Administrator))</span><br><span class="line">            &#123;</span><br><span class="line">                //如果是管理员，则直接运行</span><br><span class="line">                Application.Run(new IPAddressModifier());</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                //创建启动对象</span><br><span class="line">                System.Diagnostics.ProcessStartInfo startInfo = new System.Diagnostics.ProcessStartInfo();</span><br><span class="line">                startInfo.UseShellExecute = true;</span><br><span class="line">                startInfo.WorkingDirectory = Environment.CurrentDirectory;</span><br><span class="line">                startInfo.FileName = Application.ExecutablePath;</span><br><span class="line">                //设置启动动作,确保以管理员身份运行</span><br><span class="line">                startInfo.Verb = &quot;runas&quot;;</span><br><span class="line">                try</span><br><span class="line">                &#123;</span><br><span class="line">                    System.Diagnostics.Process.Start(startInfo);</span><br><span class="line">                &#125;</span><br><span class="line">                catch</span><br><span class="line">                &#123;</span><br><span class="line">                    return;</span><br><span class="line">                &#125;</span><br><span class="line">                //退出</span><br><span class="line">                Application.Exit();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><h5 id="第二个窗体截图如下："><a href="#第二个窗体截图如下：" class="headerlink" title="第二个窗体截图如下："></a>第二个窗体截图如下：</h5><p><img src="http://cdn.bugwz.com/006qpCDTjw1fbfw62ys31j309k0akt8z.jpg" alt="IP Info Interface"></p><p>这个窗体中主要就是对于网络适配器的预选信息进行增加删除修改操作，需要说明的是，这些信息全部存放在本机的<code>C:\Windows\IPInfo.txt</code>中，也可以自己手动去更改其中的信息，这其中的知识点就是C#对于文件的读写操作。</p><p>需要说的一点是，在这个窗体中所做的修改会同时同步到第一个窗体中的下面的网络适配器目标信息的combox中，这里使用的方法如下：</p><p>第一个窗体中的代码如下：（重点代码以作注释标注）<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static IPAddressModifier ipWindow = null;//重点</span><br><span class="line">        public IPAddressModifier()</span><br><span class="line">        &#123;</span><br><span class="line">            InitializeComponent();</span><br><span class="line">            ipWindow = this;//重点</span><br><span class="line">        &#125;</span><br><span class="line">        string[,] adapterinfo=new string[20,10];//存放网卡信息的二维数组</span><br><span class="line">        string[,] toadapterinfo = new string[20, 10];//存放目标网卡信息的二维数组</span><br><span class="line">        int adapter_i=0;</span><br><span class="line">        string adapterid=&quot;&quot;;</span><br><span class="line">        string path= &quot;C:\\Windows\\IPInfo.txt&quot;;//存放ip信息的路径</span><br><span class="line">        adapterFile adapterF;</span><br><span class="line">        //adapterFile窗体通过此方法更新adapterInfoToText的combox的items      [重点方法]</span><br><span class="line"></span><br><span class="line">        public void update_adapterInfoToText(int i,int j,int inum,string str)</span><br><span class="line">        &#123;</span><br><span class="line">            //MessageBox.Show(i.ToString()+&quot;dadad&quot;);</span><br><span class="line">            if (i == 0 &amp;&amp; j==0)</span><br><span class="line">            &#123;</span><br><span class="line">                adapterInfoToText.Items.Clear();//先清空，添加items之前必须清空一次</span><br><span class="line">                adapterInfoToText.Items.Add(toadapterinfo[0, 0]);</span><br><span class="line">                toadapterinfo[i + 1, 0] = str;</span><br><span class="line">                adapterInfoToText.Items.Add(toadapterinfo[i + 1, 0]);</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                toadapterinfo[i + 1, j] = str;</span><br><span class="line">                if (j == 0)</span><br><span class="line">                &#123;</span><br><span class="line">                    adapterInfoToText.Items.Add(toadapterinfo[i + 1, 0]);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            if (i == inum-1)</span><br><span class="line">            &#123;</span><br><span class="line">                adapterListText.SelectedIndex = 0;</span><br><span class="line">                adapterInfoToText.SelectedIndex = 0;</span><br><span class="line">                changeColor(&quot;black&quot;);</span><br><span class="line">                try</span><br><span class="line">                &#123;</span><br><span class="line">                    for (int n = 0; n &lt; 7; n++)</span><br><span class="line">                    &#123;</span><br><span class="line">                        changeText(n, adapterinfo[0, n]);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;catch &#123; &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure></p><p>第二个窗体中的调用代码如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">IPAddressModifier.ipWindow.update_adapterInfoToText(i, j, ipnum, adapter_info[i, j].ToString());</span><br></pre></td></tr></table></figure><h3 id="结束语："><a href="#结束语：" class="headerlink" title="结束语："></a>结束语：</h3><p>其实最初的想法并没有那么复杂，只是想写一个软件来修改IP地址等信息就好了，但是出于本人的有点强迫症，这个坑感觉越挖越有点大，截止到目前为止，其实还有好多坑没填上，比如挖坑的时候还想要这个软件能够获取出所有禁用的和启用的网卡，但是目前为止只能获取到已经启用的网卡；当初还想要可以对网卡进行禁用或启用操作，现在也还没实现，但是说实话我感觉现在已经弄得很不错了，代码我已经放在<a href="https://github.com/CUBEGWZ/IPAddressModifier" target="_blank" rel="noopener">GitHub</a> 上了，希望大家能够多提意见，改进一下这款软件。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP笔记-7-JSP标准动作</title>
      <link href="/2016/12/14/jsp-7/"/>
      <url>/2016/12/14/jsp-7/</url>
      
        <content type="html"><![CDATA[<h3 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h3><h5 id="JavaBean的含义："><a href="#JavaBean的含义：" class="headerlink" title="JavaBean的含义："></a>JavaBean的含义：</h5><ol><li>JavaBean是一种java语言写成的可复用组件。</li><li>它是一种特殊的java类，特殊性如下：<ul><li>类必须是具体的和公共的；</li><li>具有无参数的构造方法；</li><li>勒种方法的访问属性必须是public的，并且方法的命名也必须遵守一定的命名规范。</li></ul></li></ol><h5 id="JavaBean的优点："><a href="#JavaBean的优点：" class="headerlink" title="JavaBean的优点："></a>JavaBean的优点：</h5><ol><li>提高代码的可复用性；</li><li>程序易于开发维护；</li><li>可以跨平台；</li></ol><h5 id="JavaBean的使用："><a href="#JavaBean的使用：" class="headerlink" title="JavaBean的使用："></a>JavaBean的使用：</h5><ol><li>标准动作用于：<ul><li>将JavaBean嵌入JSP页面；</li><li>设置和获取JavaBean的属性</li><li>将用户请求转发给其他页面</li><li>将其他用户的内容嵌入当前页面</li></ul></li><li>标准动作中的属性区分大小写。</li><li>JSP中的标准动作使用<jsp>作为前缀。</jsp></li><li>JSP可使用JSP标准动作调用JavaBean组件并访问属性。</li></ol><h3 id="Java标准动作包括："><a href="#Java标准动作包括：" class="headerlink" title="Java标准动作包括："></a>Java标准动作包括：</h3><ol><li><code>&lt;jsp:useBean&gt;</code></li><li><code>&lt;jsp:setProperty&gt;</code></li><li><code>&lt;jsp:getProperty&gt;</code></li><li><code>&lt;jsp:forward&gt;</code></li><li><code>&lt;jsp:include&gt;</code></li></ol><h5 id="useBean动作："><a href="#useBean动作：" class="headerlink" title="useBean动作："></a>useBean动作：</h5><ol><li><a href="jsp:useBean" target="_blank" rel="noopener">jsp:useBean</a>标签用于在指定的域范围内查找指定名称的JavaBean对象：<ul><li>如果存在则直接返回该JavaBean对象的引用；</li><li>如果不存在则实例化一个新的JavaBean对象并将它以制定的名称存储到指定的与范围内。</li></ul></li><li>常用语法：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;jsp:useBean id=<span class="string">"beanName"</span> <span class="class"><span class="keyword">class</span></span>=<span class="string">"package.class"</span> scope=<span class="string">"page|request|session|application"</span> /&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">//id属性用于指定JavaBean实话对象的引用名称和气存储在域范围中的名称。</span></span><br><span class="line"><span class="comment">//class属性用于指定JavaBean的完整类名(即必须带有包名)。</span></span><br><span class="line"><span class="comment">//scope属性用于指定JavaBean实例对象所存储的域范围，其值只能是page、request、session和application等四个值中的一个，其默认值是page。</span></span><br></pre></td></tr></table></figure></li></ol><p>关于<code>scope</code>属性中的域范围的描述如下：</p><ol><li>Page – Bean只能自啊使用页面时使用(仅涵盖使用JavaBean的页面)。当加载新页面，就会将其销毁(pageBean.jsp/MyBean.java)</li><li>Request – 有效范围仅限于使用JavaBean的请求(requestBean.jsp/MyBean.java)</li><li>Session – 有效范围在用户整个连接过程中(整个会话过程均有效)(sessionBean.jsp/MyBean.java)</li><li>Application – 有效范围涵盖整个应用程序。也就是对整个网站均有效。(applicationBean1.jsp applicationBean2.jsp/MyBean.java)</li></ol><p>#####补充：Get()和Set()方法：<br>Get()和Set()方法用于访问JavaBean的属性</p><ol><li>Get()方法：定义了共有方法，Get()方法返回值；</li><li>Set()方法：定义了共有方法，Set()方法给属性赋值；</li></ol><h5 id="setProperty动作："><a href="#setProperty动作：" class="headerlink" title="setProperty动作："></a>setProperty动作：</h5><ol><li><a href="jsp:setProperty" target="_blank" rel="noopener">jsp:setProperty</a>标签用于设置JavaBean对象的属性。</li><li>语法结构如下：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;jsp:setProperty name=<span class="string">"beanName"</span></span><br><span class="line">&#123;</span><br><span class="line">property=<span class="string">"propertyName"</span> value=<span class="string">"&#123;string | &lt;%= expression %&gt;&#125;"</span> | property=<span class="string">"propertyName"</span> [param=<span class="string">"parameterName"</span>] | property=<span class="string">"*"</span></span><br><span class="line">&#125;/&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">//name 属性用于指定JavaBean对象的名称。</span></span><br><span class="line"><span class="comment">//property 属性用于指定JavaBean实例对象的属性名</span></span><br><span class="line"><span class="comment">//value 属性用于指定JavaBean对象的某个属性的值，value的值可以是字符串，也可以是表达式。为字符串时，该值会自动转化为JavaBean属性相应的类型，如果value的值是一个表达式，那么该表达式的计算结果必须与所要设置的JavaBean属性的类型一致。</span></span><br><span class="line"><span class="comment">//param属性用于将JavaBean实例对象的某个属性值设置为一个请求参数值，该属性值同样会自动转换成要设置的JavaBean属性的类型。</span></span><br></pre></td></tr></table></figure></li></ol><h5 id="getProperty动作："><a href="#getProperty动作：" class="headerlink" title="getProperty动作："></a>getProperty动作：</h5><ol><li><a href="jsp:getProperty" target="_blank" rel="noopener">jsp:getProperty</a>标签用于读取JavaBean对象的属性，也就是调用JavaBean对象的getter方法，然后将读取的属性值转换成字符串后插入进输出的响应正文中。</li><li><p>语法格式为：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;jsp:getProperty name=<span class="string">"beanInstanceName"</span> property=<span class="string">"PropertyName"</span> /&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">//name 属性用于指定JavaBean实例对象的名称，其值应该与&lt;jsp:useBean&gt;标签的id属性值相同。</span></span><br><span class="line"><span class="comment">//property属性用于指定JavaBean实力对象的属性名。</span></span><br></pre></td></tr></table></figure></li><li><p>如果一个JavaBean实例对象的某个属性的值为null，那么，使用<code>&lt;jsp:getProperty&gt;</code>标签输出该属性的结果将是一个内容为”null”的字符串。</p></li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP笔记-6-JSP隐式对象</title>
      <link href="/2016/12/11/jsp-6/"/>
      <url>/2016/12/11/jsp-6/</url>
      
        <content type="html"><![CDATA[<h3 id="关于JSP隐式对象"><a href="#关于JSP隐式对象" class="headerlink" title="关于JSP隐式对象"></a>关于JSP隐式对象</h3><h5 id="1-JSP饮食对象是web容器加载的一组类的实例。"><a href="#1-JSP饮食对象是web容器加载的一组类的实例。" class="headerlink" title="1. JSP饮食对象是web容器加载的一组类的实例。"></a>1. JSP饮食对象是web容器加载的一组类的实例。</h5><h5 id="2-它不像一般的java对象那样用”new”去获取实例，而是可以直接在jsp页面的java程序片和表达式部分使用对象。"><a href="#2-它不像一般的java对象那样用”new”去获取实例，而是可以直接在jsp页面的java程序片和表达式部分使用对象。" class="headerlink" title="2. 它不像一般的java对象那样用”new”去获取实例，而是可以直接在jsp页面的java程序片和表达式部分使用对象。"></a>2. 它不像一般的java对象那样用”new”去获取实例，而是可以直接在jsp页面的java程序片和表达式部分使用对象。</h5><h5 id="3-jsp使用java定义的隐式对象来访问网页的动态内容。"><a href="#3-jsp使用java定义的隐式对象来访问网页的动态内容。" class="headerlink" title="3. jsp使用java定义的隐式对象来访问网页的动态内容。"></a>3. jsp使用java定义的隐式对象来访问网页的动态内容。</h5><h5 id="4-隐式对象的名称是jsp的保留字。"><a href="#4-隐式对象的名称是jsp的保留字。" class="headerlink" title="4. 隐式对象的名称是jsp的保留字。"></a>4. 隐式对象的名称是jsp的保留字。</h5><h5 id="5-jsp提供了一些隐式对象可简化开发。"><a href="#5-jsp提供了一些隐式对象可简化开发。" class="headerlink" title="5. jsp提供了一些隐式对象可简化开发。"></a>5. jsp提供了一些隐式对象可简化开发。</h5><blockquote><p>注：这里我们主要体验这些JSP隐式对象的作用范围。</p></blockquote><ol><li>对象的作用域是可以访问对象的部分；</li><li>JSP页面中的隐式对象的作用域包括：<ul><li>page – 再引用对象的JSP页面中提供对象。</li><li>Request – 提供在所有请求页面中可用的对象。</li><li>Session – 提供对象已访问给顶应用程序中的所有网页。例如，用户访问一个网站，并通过访问其他链接打开网站中的其他页面。网站中的所有网页形成一个应用程序作用域。</li></ul></li><li>作用于通信对象和控制对象用于访问给定作用域中可用的所有对象。</li></ol><h4 id="四种隐式对象："><a href="#四种隐式对象：" class="headerlink" title="四种隐式对象："></a>四种隐式对象：</h4><ol><li>输入/输出对象：<ul><li>request</li><li>response</li><li>out</li></ul></li><li>作用域通信对象：<ul><li>session</li><li>application</li><li>pageContext</li></ul></li><li>Servlet对象<ul><li>page</li><li>config</li></ul></li><li>错误对象<ul><li>exception</li></ul></li></ol><h4 id="输入-输出对象："><a href="#输入-输出对象：" class="headerlink" title="输入/输出对象："></a>输入/输出对象：</h4><ol><li>控制页面的输入和输出</li><li>访问与所有请求和响应有关的数据</li><li>输入和输出对象包括request，response和out</li></ol><h4 id="request对象"><a href="#request对象" class="headerlink" title="request对象"></a>request对象</h4><p>客户端请求，此请求会包含来自GET/POST请求的参数；用户输入的数据用来保存在Request对象中，用javax.servlet.HttpServlet来执行。<code>request对象的作用范围只在本页</code>，跳转页面后之前的设置信息失效。部分代码如下所示：<br><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;body&gt;</span><br><span class="line">    &lt;%</span><br><span class="line">request.setAttribute(<span class="string">"name"</span>,<span class="string">"zhangsan"</span>) ;</span><br><span class="line">    request.setAttribute(<span class="string">"password"</span>,<span class="string">"123456"</span>) ;</span><br><span class="line">    <span class="comment">//request.removeAttribute("name");</span></span><br><span class="line">    %&gt;</span><br><span class="line">    &lt;jsp:forward page=<span class="string">"request1.jsp"</span>/&gt;</span><br><span class="line">&lt;a href="&lt;%=path %&gt;/first/request1.jsp"&gt;request1.jsp&lt;/a&gt;</span><br><span class="line">  &lt;/body&gt;</span><br></pre></td></tr></table></figure></p><h4 id="response对象"><a href="#response对象" class="headerlink" title="response对象"></a>response对象</h4><p>处理jsp生成的响应；响发送给客户端；实现javax.servlet.http.HttpServletResponse接口；jsp引擎吧存放在request对象中的数据发到服务器端的组件，组件收到这些个数据后进行相应处理；返回一个response对象给jsp引擎，jsp引擎再把response对象传给jsp页面，这时的页面包含了定好的格式和从服务器端得到的数据。<code>response的作用范围仍然局限于本页面</code>。</p><h4 id="out对象"><a href="#out对象" class="headerlink" title="out对象"></a>out对象</h4><p>表示输出流(注意不是response.getWriter(),因为这种是PrintWriter类型)；javax.servlet.jsp.JspWriter类的实例；使用write()、print()、和println()方法；Write()和print()方法的区别是<code>Write只能输出和字符相关的东西，而print()可以输出各种那个数据类型</code>。</p><h3 id="作用域通信对象"><a href="#作用域通信对象" class="headerlink" title="作用域通信对象"></a>作用域通信对象</h3><h4 id="session对象"><a href="#session对象" class="headerlink" title="session对象"></a>session对象</h4><p>Web服务器为单个用户发送的多个请求创建会话；存储有关用户会话的所有信息；javax.servlet.http.HttpSession接口的实例；<code>session对象的作用范围在跳转页面后仍然有效</code>。</p><h4 id="application对象"><a href="#application对象" class="headerlink" title="application对象"></a>application对象</h4><p>表示jsp页面所属的应用程序；应用程序的jsp页面组合起来形成一个应用程序，适用范围最广的上下文状态。它允许jsp页面的servlet与包括在同一应用程序中的任何web组件共享信息；javax.servlet.ServletContext接口实例。<code>application对象的作用范围在跳转页面后失效</code>。</p><h4 id="pageContext对象"><a href="#pageContext对象" class="headerlink" title="pageContext对象"></a>pageContext对象</h4><p>使用户可以访问页面作用域中定义的所有隐式对象；他的作用范围是在同一页面；javax.servlet.jsp.PageContext类的实例；是jsp页面本身的上下文；提供唯一一个API来管理具有不同作用域的属性；在实现jsp自定义标记处理程序时使用的非常多；<code>pageContext对象的作用范围在跳转页面后仍然有效</code>。</p><h3 id="Servlet对象"><a href="#Servlet对象" class="headerlink" title="Servlet对象"></a>Servlet对象</h3><p>JSP引擎为每个JSP生成一个Servlet；Servlet的对象提供了访问Servlet信息的方法和变量；Servlet对象包括：page和config。</p><h4 id="page对象"><a href="#page对象" class="headerlink" title="page对象"></a>page对象</h4><p>使用page对象可以访问Servlet类的所有变量和方法；java.lang.Object类的</p><h4 id="page对象-1"><a href="#page对象-1" class="headerlink" title="page对象"></a>page对象</h4><p>存储在便宜JSP页面的过程中刚创建的信息；javax.servlet.ServletConfig接口的实例；提供了检索Servlet的初始化参数的方法；</p><h3 id="Exception对象"><a href="#Exception对象" class="headerlink" title="Exception对象"></a>Exception对象</h3><p>exception对象用于处理JSP页面中的错误；exception对象用于访问执行JSP的过程中引发的异常；exception对象是java.lang.Throwable类的实例；</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP笔记-4-JSP页面执行过程</title>
      <link href="/2016/12/02/jsp-4/"/>
      <url>/2016/12/02/jsp-4/</url>
      
        <content type="html"><![CDATA[<h3 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h3><ol><li>Eclipse Java EE IDE for Web Developers.<br>Version: Neon.1a Release (4.6.1)</li><li>Apache Tomcat version: 8.0.39</li></ol><h3 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h3><blockquote><p>整体过程感受：</p></blockquote><p>当我们请求WEB容器中的JSP页面时，WEB容器便将被访问的页面交给JSP引擎去处理。Tomcat中的JSP引擎就是一个Servlet程序，它负责解释和执行JSP页面。</p><p>每个JSP页面在第一次被访问时，JSP引擎先将它翻译成一个Servlet源程序，接着再把这个Servlet源程序编译成Servlet的class类文件，然后再由WEB容器像调用普通Servlet程序一样的方式来装载和解释执行这个由JSP页面翻译成的Servlet程序。</p><p>由于每台服务器的环境不同，具体的由Tomcat中的JSP引擎转译的源码地址也就不一样。在我本机的环境中，我可以通过查看控制台的相关提示，就可以快速找到与我项目中jsp文件对应的java文件和class文件。示例图如下。(当然如果你实在想了很多办法也没能找到jsp转译的相关文件位置，那就用最笨的方法，来个具体盘搜索，或者直接来个全盘搜索，搜索格式为：jsp文件名+_jsp+.java)</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fade9ok91ij30pc0csdii.jpg" alt="Console Infos"></p><blockquote><p>JSP的具体执行过程如下：</p></blockquote><ol><li>客户端发出请求。</li><li>Web容器将JSP转译成Servlet源代码。</li><li>Web容器将产生的源代码进行编译。</li><li>把执行结果响应至客户端。</li></ol><blockquote><p>总结如下：</p></blockquote><p>JSP执行过程：</p><ol><li>首先，客户端发出请求(request )，请求访问JSP网页</li><li>接着，JSP Container将要访问的.JSP文件 转译成Servlet的源代码（.java文件）</li><li>然后，将产生的Servlet的源代码（.java文件）经过编译，生成.class文件，并加载到内存执行</li><li>最后把结果响应(response )给客户端</li></ol><p>补充：执行JSP网页文件时，需要经过两个时期：转译时期(TranslationTime)和请求时期(RequestTime)。</p><ul><li><p>转译时期：JSP转译成Servlet类(.class文件)。</p><ul><li>将JSP网页转译为Servlet源代码(.java)，此段称为转译时期(Translation time)；将JSP网页转译为Servlet源代码(.java)，此段称为转译时期(Translation time)；</li><li>将Servlet源代码(.java)编译成Servlet类(.class)，此阶段称为编译时期(Compilation time)。<br>其实，JSP就是一个Servlet。</li></ul></li><li><p>请求时期：Servlet类(.class文件)执行后，响应结果至客户端。</p></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP笔记-5-JSP脚本元素指令与动作</title>
      <link href="/2016/12/02/jsp-5/"/>
      <url>/2016/12/02/jsp-5/</url>
      
        <content type="html"><![CDATA[<h3 id="JSP脚本元素："><a href="#JSP脚本元素：" class="headerlink" title="JSP脚本元素："></a>JSP脚本元素：</h3><h4 id="简介："><a href="#简介：" class="headerlink" title="简介："></a>简介：</h4><p>由于都是在一定的格式里起纳入Java代码，因此经常把“表达式”、Scriptlet、“声明”都称为脚本元素。</p><h4 id="JSP表达式："><a href="#JSP表达式：" class="headerlink" title="JSP表达式："></a>JSP表达式：</h4><p>JSP表达式是对数据的表示，系统将其作为一个值进行计算和显示。示例代码如下所示：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">"java"</span> <span class="keyword">import</span>=<span class="string">"java.util.*"</span> pageEncoding=<span class="string">"UTF-8"</span>%&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">  &lt;head&gt;</span><br><span class="line">    &lt;title&gt;jspDemo1.jsp&lt;/title&gt;</span><br><span class="line">  &lt;/head&gt;</span><br><span class="line">  &lt;body&gt;</span><br><span class="line">    &lt;h1&gt;JSP表达式 示例&lt;/h1&gt;</span><br><span class="line">    &lt;!-- 常量,方法值,算术运算,关系运算 --&gt;</span><br><span class="line">    &lt;b&gt;PI 的值： &lt;/b&gt;&lt;%=Math.PI %&gt;&lt;br /&gt;</span><br><span class="line">    &lt;b&gt;100,99中最大的值 :&lt;/b&gt;&lt;%=Math.max(100,99) %&gt;&lt;br /&gt;</span><br><span class="line">    &lt;b&gt;100,99中最小的值 :&lt;/b&gt;&lt;%=Math.min(100,99) %&gt;&lt;br /&gt;</span><br><span class="line">    &lt;b&gt;3+2-5的值 :&lt;/b&gt;&lt;%=3+2-5 %&gt;&lt;br /&gt;</span><br><span class="line">    &lt;b&gt;(3+2)==5的值 :&lt;/b&gt;&lt;%=(3+2)==5 %&gt;&lt;br/&gt;</span><br><span class="line">    &lt;b&gt;(3+2)!=5的值(表达式输出) :&lt;/b&gt;&lt;%=(3+2)!=5 %&gt;&lt;br /&gt;</span><br><span class="line">    &lt;!-- out.print() 与表达式作用类似 --&gt;</span><br><span class="line">    &lt;b&gt;&lt;%out.println(<span class="string">"(3+2)!=5的值(out.print输出) :"</span>+((<span class="number">3</span>+<span class="number">2</span>)!=<span class="number">5</span>)); %&gt;&lt;br/&gt;</span><br><span class="line">  &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h4 id="JSP-Scriptlet："><a href="#JSP-Scriptlet：" class="headerlink" title="JSP Scriptlet："></a>JSP Scriptlet：</h4><p>JSP Scriptlet就是在JSP页面里面嵌入一段Java代码，也成为脚本代码段，它在JSP页面中的表现形式为:&lt;% Java代码 %&gt;，代码示例如下:</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">"java"</span> <span class="keyword">import</span>=<span class="string">"java.util.*"</span> pageEncoding=<span class="string">"UTF-8"</span>%&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">  &lt;head&gt;</span><br><span class="line">    &lt;title&gt;jspDemo2.jsp&lt;/title&gt;</span><br><span class="line">  &lt;/head&gt;</span><br><span class="line">  &lt;body&gt;</span><br><span class="line">    &lt;h1&gt;java 程序片 示例&lt;/h1&gt;</span><br><span class="line">    &lt;%</span><br><span class="line">      <span class="comment">//java程序片 定义一个变量 (结论:局部变量,反复声明,数值不变)</span></span><br><span class="line">      <span class="keyword">int</span> i=<span class="number">0</span>;</span><br><span class="line">      i++;</span><br><span class="line">     %&gt;</span><br><span class="line">     &lt;h1&gt;你是第&lt;%=i %&gt;个访问本站的用户&lt;/h1&gt;</span><br><span class="line">     &lt;br&gt;</span><br><span class="line">     &lt;!-- java 程序片 写具体代码 --&gt;</span><br><span class="line">     &lt;h1&gt;以直角三角形的形式显示数字&lt;/h1&gt;</span><br><span class="line">     &lt;%</span><br><span class="line">     <span class="keyword">for</span>(<span class="keyword">int</span> k=<span class="number">1</span>;k&lt;<span class="number">10</span>;k++) &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;=k;j++) &#123;</span><br><span class="line">           out.println(j);</span><br><span class="line">        &#125;</span><br><span class="line">     out.println(<span class="string">"&lt;br/&gt;"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">      %&gt;</span><br><span class="line">      &lt;h1&gt;计算1到100的和&lt;/h1&gt;</span><br><span class="line">      &lt;%</span><br><span class="line">        <span class="comment">//计算1到100的和</span></span><br><span class="line">    <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> k=<span class="number">1</span>;k&lt;=<span class="number">100</span>;k++)&#123;</span><br><span class="line">    sum+=k;</span><br><span class="line">    &#125;</span><br><span class="line">    out.println(<span class="string">"sum="</span>+sum);</span><br><span class="line">      %&gt;</span><br><span class="line">  &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h4 id="JSP声明："><a href="#JSP声明：" class="headerlink" title="JSP声明："></a>JSP声明：</h4><p>JSP声明就是在JSP页面中声明Java方法或变量等(用于定义JSP代表的Servlet类的成员变量和方法)。JSP声明在JSP页面中的表现形式为：&lt;%! Java 代码 %&gt;，实例代码如下：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">"java"</span> <span class="keyword">import</span>=<span class="string">"java.util.*"</span> pageEncoding=<span class="string">"UTF-8"</span>%&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;title&gt;jspDemo3.jsp&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;h1&gt;java 声明变量和方法 示例&lt;/h1&gt;</span><br><span class="line">&lt;%!<span class="comment">//声明变量 变量类型为java允许的任何类型 (结论:全局变量即类的成员变量,只声明一次,i数值递增)</span></span><br><span class="line"><span class="keyword">int</span> i, a, b = <span class="number">10</span>, c;</span><br><span class="line">String tom = <span class="keyword">null</span>, jerry = <span class="string">"love JSP"</span>;</span><br><span class="line">Date date;</span><br><span class="line"><span class="comment">//声明方法</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">sayHello</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="string">"Hello,"</span> + name + <span class="string">"!"</span>;</span><br><span class="line">&#125;%&gt;</span><br><span class="line">&lt;%</span><br><span class="line">i++;</span><br><span class="line"><span class="comment">//给变量进行赋值操作</span></span><br><span class="line">date = <span class="keyword">new</span> Date();</span><br><span class="line">%&gt;</span><br><span class="line">&lt;h1&gt;</span><br><span class="line">你是第&lt;%=i%&gt;个访问本站的用户</span><br><span class="line">&lt;/h1&gt;</span><br><span class="line">&lt;br&gt;&lt;%=jerry%&gt;</span><br><span class="line">&lt;br&gt;&lt;%=tom%&gt;</span><br><span class="line">&lt;br&gt;&lt;%=date%&gt;</span><br><span class="line">&lt;br&gt;&lt;%=sayHello(<span class="string">"zhangsan"</span>)%&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h5 id="有个问题："><a href="#有个问题：" class="headerlink" title="有个问题："></a>有个问题：</h5><p>当两个或多个线程同时访问同一个共享的变量时，并且一个线程需要修改这个变量时，我们应对这样的问题做出处理，否则可能发生混乱。根据Tomcat服务器的机制，Tomcat会为每个访问网站的用户开启一个新的线程，当多个用户同时请求一个JSP页面时，JSP页面的程序片（&lt;%  %&gt;中的）就会被多次调用运行，分别运行在不同的线程中，JSP页面的成员变量和方法（&lt;%! %&gt;）会被多个用户共享和访问。有些JSP页面的方法在操作成员变量，可能不需要被其他用户影响，以免产生不利影响，那么就要使用线程同步了，相当于加了一把锁。这样就能保障不同的用户对于同一个数据的处理时，不会发生混乱。</p><p>######synchronized修饰的作用为：当一个线程在执行被synchronized修饰的方法时，其他线程想在程序片中调用这个synchronized修饰的方法时就必须等待，直等到方法执行完。</p><p>关于线程同步的方法有两种：</p><p>1.使用同步块进行线程同步：<code>synchronized(object){要同步的语句}</code>，实例代码如下：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">"java"</span> <span class="keyword">import</span>=<span class="string">"java.util.*"</span> pageEncoding=<span class="string">"UTF-8"</span>%&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">  &lt;title&gt;关于线程同步的两个措施&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;p&gt;synchronized块 实现线程同步&lt;/p&gt;</span><br><span class="line">&lt;%! </span><br><span class="line"><span class="comment">//声明Integer 对象</span></span><br><span class="line">Integer number=<span class="keyword">new</span> Integer(<span class="number">0</span>);</span><br><span class="line">%&gt;</span><br><span class="line">&lt;% </span><br><span class="line"><span class="comment">//把i++操作放入synchronized块中</span></span><br><span class="line"><span class="keyword">synchronized</span>(number)</span><br><span class="line">&#123;</span><br><span class="line">Thread.sleep(<span class="number">5000</span>);</span><br><span class="line"><span class="keyword">int</span> i=number.intValue();</span><br><span class="line">i++;</span><br><span class="line">number=<span class="keyword">new</span> Integer(i);</span><br><span class="line">&#125;</span><br><span class="line">%&gt;</span><br><span class="line">&lt;P&gt;您是第&lt;%=number.intValue()%&gt;个访问本站的客户。</span><br><span class="line">  &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><p>2.使用同步方法进行线程同步：<code>synchronized void methodA(){}</code>,实例代码如下：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">"java"</span> <span class="keyword">import</span>=<span class="string">"java.util.*"</span> pageEncoding=<span class="string">"UTF-8"</span>%&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;title&gt;jspDemo3_2.jsp&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;h1&gt;方法前加synchronized关键字 实现线程同步&lt;/h1&gt;</span><br><span class="line">&lt;%!<span class="comment">//定义变量</span></span><br><span class="line"><span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="comment">//声明方法 </span></span><br><span class="line"><span class="comment">//public void countPeople() </span></span><br><span class="line"><span class="comment">//synchronized修饰方法</span></span><br><span class="line"><span class="function"><span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">countPeople</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">Thread.sleep(<span class="number">5000</span>);</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">try&#123;</span></span><br><span class="line"><span class="comment">Thread.sleep(5000);</span></span><br><span class="line"><span class="comment">&#125;catch(Exception e)&#123;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">i++;</span><br><span class="line">&#125;%&gt;</span><br><span class="line">&lt;%</span><br><span class="line">countPeople();</span><br><span class="line">%&gt;</span><br><span class="line">&lt;P&gt;</span><br><span class="line">您是第&lt;%=i%&gt;个访问本站的客户。</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h3 id="JSP指令："><a href="#JSP指令：" class="headerlink" title="JSP指令："></a>JSP指令：</h3><h4 id="简介：-1"><a href="#简介：-1" class="headerlink" title="简介："></a>简介：</h4><p>(编译指令)相当于在编译期间的命令(换句话说jsp转换成Servlet过程期间需要用到的指令)，其中JSP指令包括page、include、taglib等。</p><h4 id="page指令："><a href="#page指令：" class="headerlink" title="page指令："></a>page指令：</h4><p>Page指令用来定义整个JSP页面的一些属性和这些属性的值。属性值总是用单引号或双引号括起来，可以用一个page指令制定多个属性的值，也可以用多个page为单个属性指定值。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//一个page指令制定多个属性的值</span></span><br><span class="line">&lt;%@ page 属性<span class="number">1</span>=<span class="string">"属性一的值"</span> 属性<span class="number">2</span>=<span class="string">"属性二的值"</span> ... %&gt;</span><br><span class="line"><span class="comment">//多个page指令制定多个属性的值</span></span><br><span class="line">&lt;%@ page 属性<span class="number">1</span>=<span class="string">"属性一的值"</span>%&gt;</span><br><span class="line">&lt;%@ page 属性<span class="number">2</span>=<span class="string">"属性二的值"</span>%&gt;</span><br></pre></td></tr></table></figure><p>另外page指令常用与进行错误页面的跳转，这其中一定会包括两个页面，一个是出错的页面，一个是出错后要跳转到的页面，部分代码如下：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//出错页面的page指令信息</span></span><br><span class="line">&lt;%@ page language=<span class="string">"java"</span> <span class="keyword">import</span>=<span class="string">"java.util.*"</span> pageEncoding=<span class="string">"UTF-8"</span> errorPage=<span class="string">"error.jsp"</span> %&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">//出错后跳转到的页面的page指令信息</span></span><br><span class="line">&lt;%@ page language=<span class="string">"java"</span> <span class="keyword">import</span>=<span class="string">"java.util.*"</span> pageEncoding=<span class="string">"UTF-8"</span> isErrorPage=<span class="string">"true"</span> %&gt;</span><br></pre></td></tr></table></figure><h4 id="include指令："><a href="#include指令：" class="headerlink" title="include指令："></a>include指令：</h4><p>include指令用于在运行时将html文件或者jsp页面嵌入到另一个jsp页面(为了代码的复用，写好的jsp页面可以被所有的其他jsp页面进行银行用)，部分代码如下所示：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%--</span><br><span class="line">  include指令包含,静态包含特点:</span><br><span class="line">  <span class="number">1</span>,先包含后处理,生成一个servlet类.</span><br><span class="line">    即当前jsp页面和插入的文件合并成一个新的jsp文件,然后JSP引擎再将这个新的JSP页面转译成java文件</span><br><span class="line">  <span class="number">2</span>,只生成一个<span class="class"><span class="keyword">class</span>文件</span></span><br><span class="line"><span class="class">  3,不能传参数</span></span><br><span class="line"><span class="class">  4,同一个<span class="title">request</span>对象</span></span><br><span class="line"><span class="class">  --%&gt;</span></span><br><span class="line"><span class="class">  &lt;<span class="title">body</span>&gt;</span></span><br><span class="line"><span class="class">    &lt;<span class="title">h1</span>&gt;<span class="title">includeDemo1</span>.<span class="title">jsp</span>&lt;/<span class="title">h1</span>&gt;</span></span><br><span class="line">    &lt;!-- 特点1:包含文件定义变量i,被包含文件定义相同变量i时,报错 原因:先包含后处理,定义变量重复--&gt;</span><br><span class="line">    &lt;%</span><br><span class="line">       <span class="keyword">int</span> i=<span class="number">100</span>;</span><br><span class="line">     %&gt;</span><br><span class="line">     i=&lt;%=i %&gt;</span><br><span class="line">    &lt;br&gt;</span><br><span class="line">    &lt;!--  特点<span class="number">2</span>:查看work下项目的临时文件--&gt;</span><br><span class="line">&lt;!-- 特点<span class="number">3</span>:不能传参数 --&gt;</span><br><span class="line">    &lt;!--  特点<span class="number">4</span>:包含页面和被包含页面访问的是同一个request内嵌对象--&gt;</span><br><span class="line">&lt;%request.setAttribute(<span class="string">"username"</span>,<span class="string">"xiaoming"</span>);%&gt;</span><br><span class="line">&lt;%System.out.println(<span class="string">"includeDemo1.jsp中request="</span>+request); %&gt;</span><br><span class="line">&lt;%<span class="meta">@include</span> file=<span class="string">"include1.jsp"</span> %&gt; </span><br><span class="line"></span><br><span class="line">  &lt;/body&gt;</span><br></pre></td></tr></table></figure><h4 id="taglib指令："><a href="#taglib指令：" class="headerlink" title="taglib指令："></a>taglib指令：</h4><p>taglib指令的作用是在jsp页面中，将<code>标签库描述符文件</code>引入到该页面中，并设置前缀，而去利用标签的<code>前缀</code>去使用标签库表述文件中的标签。部分代码如下所示：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;!-- 引入JSTL标签 --&gt;</span><br><span class="line">&lt;%@ taglib prefix=<span class="string">"c"</span> uri=<span class="string">"http://java.sun.com/jstl/core"</span> %&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">  &lt;head&gt;</span><br><span class="line">    &lt;title&gt;taglib标签引用&lt;/title&gt;</span><br><span class="line">  &lt;/head&gt;</span><br><span class="line">  &lt;body&gt;</span><br><span class="line">    &lt;c:out value=<span class="string">"欢迎测试你的第一个使用到JSTL的网页"</span>/&gt;</span><br><span class="line">  &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h3 id="JSP动作"><a href="#JSP动作" class="headerlink" title="JSP动作"></a>JSP动作</h3><p>jsp动作包括include，param，forward，plugin，useBean等动作，详细介绍如下所示：</p><h4 id="param动作："><a href="#param动作：" class="headerlink" title="param动作："></a>param动作：</h4><p>param标签以“名字–值”对的形式为其他标签提供附加信息，这个标签与jsp:include、jsp:forward、jsp:plugin标签一起使用。param动作标记语法格式如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;jsp:param name=<span class="string">"名字"</span> value=<span class="string">"指定param的值"</span> /&gt;</span><br></pre></td></tr></table></figure></p><h4 id="include动作："><a href="#include动作：" class="headerlink" title="include动作："></a>include动作：</h4><p>include动作的特点为：</p><ol><li>先处理后包含,生成多个servlet类。即能自动区分包含的文件是静态文件还是动态文件；执行时,如果为静态文件,直接将资源包含处理,与静态包含相同；如果为动态文件,则选各自处理资源,之后将处理后的结果包含在一起。</li><li>生成多个class文件</li><li>能传参数</li><li>不同一个request对象,可以取得包含它的页面的参数,并添加了自己的参数</li></ol><p>include动作的标记语法格式如下：<br><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;jsp:include page=<span class="string">"文件的URL"</span> /&gt;</span><br><span class="line">或者</span><br><span class="line">&lt;jsp:include page=<span class="string">"文件的URL"</span>&gt;</span><br><span class="line">&lt;/jsp:include&gt;</span><br></pre></td></tr></table></figure></p><p>######动态include和静态include的区别如下：</p><ol><li><code>&lt;%@ include file=&quot;date.jsp&quot; %&gt;</code><ul><li>include编译指令是在jsp程序的转换时期就将file属性所制定的程序内容嵌入，然后再编译执行；</li><li>只生成一个class文件</li><li>include不能带参数</li><li>同一个request对象</li></ul></li><li><code>&lt;jsp:include page=&quot;date.jsp&quot; flush=&quot;true&quot; /&gt;</code><ul><li>而include指令在转换期间是不会被编译的，只有在客户端请求时期如果被执行到才会被动态的编译载入。</li><li>生成多个class文件</li><li><a href="jsp:include" target="_blank" rel="noopener">jsp:include</a>可以</li><li>不同的request对象，可以去的包含他的页面的参数，并添加了自己的参数。</li></ul></li></ol><h4 id="forward动作："><a href="#forward动作：" class="headerlink" title="forward动作："></a>forward动作：</h4><p>forward的动作标记语法格式：<br><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;jsp:forward page=<span class="string">"文件的URL"</span> /&gt;</span><br><span class="line">或者</span><br><span class="line">&lt;jsp:forward page=<span class="string">"文件的URL"</span>&gt;</span><br><span class="line"><span class="comment">//可以在forward中进行传参操作</span></span><br><span class="line">&lt;jsp:param name=<span class="string">"参数名称"</span> value=<span class="string">"参数值"</span>/&gt;</span><br><span class="line">&lt;/jsp:forward&gt;</span><br></pre></td></tr></table></figure></p><h4 id="plugin动作："><a href="#plugin动作：" class="headerlink" title="plugin动作："></a>plugin动作：</h4><p>plugin标签只是jsp文件加载java plugin，该插件由客户负责下载，并使用该插件负责下载，并使用该插件来运行java applet，其中plugin动作标记语法格式如下：<br><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;jsp:plugin type=<span class="string">"applet"</span> code=<span class="string">"小应用程序的字节码文件"</span> jreversion=<span class="string">"java虚拟机版本号"</span> width=<span class="string">"小程序宽度值"</span> height=<span class="string">"小程序口高度值"</span>&gt;</span><br><span class="line">  &lt;jsp:fallback&gt;</span><br><span class="line">     提示信息：用来提示用户的浏览器是否支持插件下载。</span><br><span class="line">  &lt;/jsp:fallback&gt;</span><br><span class="line">&lt;/jsp:plugin&gt;</span><br></pre></td></tr></table></figure></p><h4 id="useBean动作："><a href="#useBean动作：" class="headerlink" title="useBean动作："></a>useBean动作：</h4><p>useBean标签用来创建并使用一个java beans，是非常重要的一个动作标签。sun公司的倡导是：用html完成jsp页面的静态部分，用javabeans完成动态部分，实现真正意义上的静态和动态分割。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP使用中问题汇总</title>
      <link href="/2016/12/01/jsp-problem/"/>
      <url>/2016/12/01/jsp-problem/</url>
      
        <content type="html"><![CDATA[<h3 id="说明："><a href="#说明：" class="headerlink" title="说明："></a>说明：</h3><p>本篇主要说明在我学习JSP的相关过程中，遇到的最凸显的一些问题，肯定附带一些IDE工具(这里主要是Eclipse)的一些问题，为了方便查询，特此记录。我自身软件环境如下：</p><ol><li>Eclipse Java EE IDE for Web Developers.<br>Version: Neon.1a Release (4.6.1)；</li><li>Apache Tomcat version: 8.5.8</li></ol><blockquote><p>#####进入Servlet映射的虚拟路径的404错误问题</p></blockquote><p>我发现在使用Eclipse利用servlet3.0及以上的版本写虚拟路径映射的时候，默认没有web.xml的时候(当然与这无关)，在路径都书写正确的情况下始终无法进入对应的路径，Eclipse的控制台提示错误为：</p><p><code>[SetPropertiesRule]{Server/Service/Engine/Host/Context} Setting property &#39;source&#39; to &#39;org.eclipse.jst.jee.server:Test&#39; did not find a matching property.</code></p><p>浏览器会提示404错误，我系统默认浏览器与Eclipse的Internal Web Browser都是这个错误。错误如下图所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9c38kniyj30f506z0sw.jpg" alt="404 Error in Chrome"><br><img src="http://cdn.bugwz.com/006qpCDTjw1fa9c6a1qpej30cp06s3ym.jpg" alt="404 Error in Self Browser"></p><p>然后我尝试选择附带web.xml的情况下重建工程，依旧会出现这个错误，最后经过一番查找，发现原因应该是Tomcat6.0及以后的版本中的server.xml的context节点中不支持source属性，我在官网找到如下信息：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9ce8nzxyj30vo0f1tbr.jpg" alt="Tomcat6.0 Not Support &quot;Source&quot;"></p><p>其中最主要的一句好像是：</p><p><code>It is NOT recommended to place &lt;Context&gt; elements directly in the server.xml file. This is because it makes modifying the Context configuration more invasive since the main conf/server.xml file cannot be reloaded without restarting Tomcat.</code></p><p>解决方法为：<br>这里通过修改Tomcat server配置，首先关闭server，然后在server视图中双击server，打开配置界面，选中下面的”Publish module contexts to separate XML files”，保存退出。如下图所示，重启Eclipse即可!</p><p>解决办法的网络参考：<a href="http://siruoxian.iteye.com/blog/1103347" target="_blank" rel="noopener">http://siruoxian.iteye.com/blog/1103347</a></p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9citv12aj30ld0aywf9.jpg" alt="Change Tomcat Server Config"></p><hr><blockquote><p>#####在Servlet使用过程中的编码问题</p></blockquote><p>这里不讨论各种编码方式的优缺点，这里只是说明我们在日常使用过程中出现的编码问题，这也是最让人头痛的问题。我使用的UTF-8编码。<br>我们在使用Eclipse进行项目的时候，比如说吧，我说一下我曾记出现的一个问题。</p><p>响应为http的post响应请求，当然这里http的响应请求方式无所谓，然后是我的页面显示编码全部规定是UTF-8，即<code>resp.setHeader(&quot;Content-type&quot;, &quot;text/html;charset=UTF-8&quot;);</code>，resp为HttpServletResponse对象类型，但是我并没有设置<code>req.setCharacterEncoding(&quot;UTF-8&quot;);</code>，此处的req为HttpServletRequest对象，然后我发现，页面的显示就会出现乱码。</p><p>这是我遇到的问题一，还有一个更严重的问题就是页面实际使用的编码与你要求页面显示的编码方式不统一，由于Eclipse本身默认的文本编码为GBK，并且我们在Eclipse中建立的各种工程全部是继承系统默认的文本编码方式，但是我经常使用的页面编码显示为UTF-8，这就会出现意想不到的问题，为了解决这一问题，我强烈建议，你想让页面以什么编码方式显示，一定要用该编码编辑该文本，其中Eclipse的编码设置位置，如下图所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fabgla91ifj30m20imabn.jpg" alt="Change Text Coding"></p><hr><blockquote><p>#####Eclipse的汉化方法</p></blockquote><p>虽然说使用英文的IDE可以不断的锻炼自己的英文水平，并且能够避免很多错译，或者语言文件本身存在的bug导致软件无法正常的运行等等问题。</p><p>但是，但是，因为Eclipse本身软件的影响力，其官方的汉化文件已经做得非常好了，IDE本身的汉化比已经达到80%多，加上英文IDE本身给人的恐惧心理，让我们没法深入探索这个IDE工具的有趣之处，我们可能只是按需的在这款软件内依次点击那几个按钮，完成老师交代的那一丁丁任务而已，我们也只是稍微碰过这款软件，也许是因为英文而没有那种想去探索的勇气，一个英文不好的人去用，的却会浪费很多时间，最起码我是因为英文不想去接触，说实话，我有点怕麻烦，进入正题，汉化过程如下：</p><p>访问Eclipse的官网语言下载链接：<a href="https://www.eclipse.org/babel/downloads.php" target="_blank" rel="noopener">这里</a>；然后我们选择对应我们IDE的语言包进行下载，如下图所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fabhmpqvgjj30oh09x75b.jpg" alt="IDE Language Pack"></p><p>下载后，接下，记住解压后的文件的目录绝对路径，然后我们打开Eclipse这个IDE工具，依次选择Help-&gt;Install New Software-&gt;Add…-&gt;Local…；然后添加那个解压的文件的文件夹，然后确定后，进行等待，然后展开下图中的一个语言包：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fabht9088lj30t70jrgnb.jpg" alt="Install Language Pack"></p><p>此处切记哈，不要安装这个语言包中的所有插件，因为有一些汉化的极其不好，并且你全部安装后会发现Eclipse的有些功能莫名地无法使用，就是因为有一些语言包插件有问题，为了能最大的汉化并不影响我们的日常使用，我推荐选择其中一款名字接近于”for eclipse…”的插件，目前该插件汉化Eclipse的汉化率在80%以上，其他的操作一次进行即可。</p><hr><blockquote><p>#####Eclipse中导入其他的工程时出错，无法运行</p></blockquote><p>当我们从别人的机器中拷贝出一个工程项目并在我们的电脑中导入该工程后，很容易出现的问题就是出错，无法运行，这其中有很大的原因在于工程项目的构建路径中的JAR和类文件夹出错。</p><p>我们知道，每个工程的构建依赖于电脑自身的Java与Tomcat环境，如果一个人的电脑配置的环境是Java1.7，并且它的Tomcat环境是7.0.73,他新建一个工程的时候Eclipse自然在构建路径中配置这些信息，但是由于我的系统环境为Java1.8,Tomcat8.5.8，这样就会因为找不到对应的配置环境而出错，还有一种可能性是，你环境的安装路径不对，这里仅是推测。出错可能如下图所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fach5an9r3j30t60ce0u8.jpg" alt="Path Error"></p><p>然后我们的纠错步骤为，修改该工程的构建路径中的的JAR和类文件夹信息，删除出错的，添加我们本机的Java环境以及Tomcat环境，如下图所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fach7tx90lj30p60b3wfa.jpg" alt="Library Error"><br><img src="http://cdn.bugwz.com/006qpCDTjw1fach9xe23dj30v40f7abu.jpg" alt="Runtime Error"></p><p>这样的话，该工程的错误就回消失，当然还有一点是可能是该工程的原始环境较现在的环境较新，因此出错，这样的话就具体针对错误提示就行排除。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP作业-2-利用Servlet完成表单提交验证</title>
      <link href="/2016/11/30/jsp-homework-2/"/>
      <url>/2016/11/30/jsp-homework-2/</url>
      
        <content type="html"><![CDATA[<h3 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h3><ol><li>Eclipse Java EE IDE for Web Developers.<br>Version: Neon.1a Release (4.6.1)</li><li>Apache Tomcat version: 8.0.39</li></ol><h3 id="作业内容："><a href="#作业内容：" class="headerlink" title="作业内容："></a>作业内容：</h3><p>利用三个servlet，实现表单页面的输出，表单提交的信息的验证以及验证信息后的跳转页面。</p><h3 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h3><p>我们一共需要建立三个servlet类，他们分别是：Login.java，LoginContral.java和Welcome.java，他们的具体代码如下所示：</p><blockquote><p>1.Login()类代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/Login"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Login</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@see</span> HttpServlet#HttpServlet()</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Login</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="comment">// TODO Auto-generated constructor stub</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="comment">//super.doGet(req, resp);</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 解决页面显示的中文乱码问题</span></span><br><span class="line">resp.setContentType(<span class="string">"text/html;charset=UTF-8"</span>);</span><br><span class="line"><span class="comment">//解决中文在传输过程中的乱码问题</span></span><br><span class="line">req.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line">PrintWriter out = resp.getWriter();</span><br><span class="line">out.println(<span class="string">"&lt;html&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;head&gt;&lt;title&gt;登陆页面&lt;/title&gt;&lt;/head&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;body&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;h1&gt;登陆页面&lt;/h1&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;form action='LoginControl'&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"用户名：&lt;input type='text' name='username' /&gt;&lt;br&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"密码：&lt;input type='password' name='password' /&gt;&lt;br&gt;"</span>);</span><br><span class="line"><span class="comment">// 添加隐藏input元素（这种类型元素在页面上不显示，但是可以传递到其他的页面）</span></span><br><span class="line">out.println(<span class="string">"&lt;input type='hidden' name='sex' value='man' /&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;input type='submit' value='登陆'&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;/form&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;/body&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;/html&gt;"</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>2.LoginControl()类代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/LoginControl"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LoginControl</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@see</span> HttpServlet#HttpServlet()</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">LoginControl</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="comment">// TODO Auto-generated constructor stub</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="comment">// super.doGet(req, resp);</span></span><br><span class="line"><span class="comment">// 获取请求的信息（方法的参数需要与input元素中name属性值相同，否则取不到值）</span></span><br><span class="line"><span class="comment">// 解决页面显示的中文乱码问题</span></span><br><span class="line">resp.setContentType(<span class="string">"text/html;charset=UTF-8"</span>);</span><br><span class="line"><span class="comment">//解决中文在传输过程中的乱码问题</span></span><br><span class="line">req.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line">String username = req.getParameter(<span class="string">"username"</span>);</span><br><span class="line">String password = req.getParameter(<span class="string">"password"</span>);</span><br><span class="line">String sex = req.getParameter(<span class="string">"sex"</span>);</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (username.equals(<span class="string">"admin"</span>) &amp;&amp; password.equals(<span class="string">"123"</span>)) &#123;<span class="comment">// 合法</span></span><br><span class="line"><span class="comment">// 页面的跳转（注意：这个方法的参数是url-pattern里面配置的）</span></span><br><span class="line"><span class="comment">// 当用户合法后，把用户名和密码写到cookie，供welcome页面取出并显示</span></span><br><span class="line"><span class="comment">// a.用cookie实现不同页面之间数据的共享</span></span><br><span class="line"><span class="comment">// 1.创建cookie</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Cookie c1 = new Cookie("username",username); Cookie c2 = new</span></span><br><span class="line"><span class="comment"> * Cookie("password",password);</span></span><br><span class="line"><span class="comment"> * //设定cookie在客户端存在的时间,单位为s（如果没有设定的话，当关闭浏览器时，cookie消失或者过期）</span></span><br><span class="line"><span class="comment"> * c1.setMaxAge(10); c2.setMaxAge(10); //2.向客户端写cookie</span></span><br><span class="line"><span class="comment"> * response.addCookie(c1); response.addCookie(c2);</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">// c.利用session阻止用户的非法登录</span></span><br><span class="line"><span class="comment">// 如果用户名合法，则把用户名放到session里面</span></span><br><span class="line">HttpSession hs = req.getSession();</span><br><span class="line"><span class="comment">// 设置session的生命时间（单位s）</span></span><br><span class="line">hs.setMaxInactiveInterval(<span class="number">30</span>);</span><br><span class="line"><span class="comment">// 向session中放置属性</span></span><br><span class="line">hs.setAttribute(<span class="string">"username"</span>, username);</span><br><span class="line">hs.setAttribute(<span class="string">"password"</span>, password);</span><br><span class="line"><span class="comment">// b.通过sendRedirect实现不同页面之间数据的共享</span></span><br><span class="line">resp.sendRedirect(<span class="string">"Welcome?username="</span> + username + <span class="string">"&amp;password="</span> + password + <span class="string">"&amp;sex="</span> + sex);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 不合法</span></span><br><span class="line"><span class="comment">// 这个方法的参数是url-pattern里面配置的</span></span><br><span class="line">resp.sendRedirect(<span class="string">"Login"</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>3.LoginControl()类代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/Welcome"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Welcome</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@see</span> HttpServlet#HttpServlet()</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Welcome</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="comment">// TODO Auto-generated constructor stub</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="comment">// super.doGet(req, resp);</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line"><span class="comment">// 解决页面显示的中文乱码问题</span></span><br><span class="line">resp.setContentType(<span class="string">"text/html;charset=UTF-8"</span>);</span><br><span class="line"><span class="comment">//解决中文在传输过程中的乱码问题</span></span><br><span class="line">req.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line">PrintWriter out = resp.getWriter();</span><br><span class="line"><span class="comment">// a.通过cookie来获取共享信息；</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * //3.获取cookie Cookie[] cs=request.getCookies();</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * if(cs!=null)&#123; for(int i=0;i&lt;cs.length;i++)&#123; Cookie temp = cs[i];</span></span><br><span class="line"><span class="comment"> * out.println(temp.getName()+":"+temp.getValue()); &#125; &#125;else&#123;</span></span><br><span class="line"><span class="comment"> * out.println("cookie不存在或者已过期！"); &#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">// b.通过sendRedirect来获取共享信息</span></span><br><span class="line"><span class="comment">// String username = request.getParameter("username");</span></span><br><span class="line"><span class="comment">// String password = request.getParameter("password");</span></span><br><span class="line"><span class="comment">// String sex = request.getParameter("sex");</span></span><br><span class="line"><span class="comment">// out.println("username=="+username+" password=="+password+"</span></span><br><span class="line"><span class="comment">// sex=="+sex);</span></span><br><span class="line"><span class="comment">// c.通过session 阻止非法用户登录网站内部</span></span><br><span class="line"><span class="comment">// 获取session</span></span><br><span class="line">HttpSession hs = req.getSession();</span><br><span class="line"><span class="comment">// 根据session中的属性名获取相应的属性值</span></span><br><span class="line">String usernameSession = (String) hs.getAttribute(<span class="string">"username"</span>);</span><br><span class="line">String passwordSession = (String) hs.getAttribute(<span class="string">"password"</span>);</span><br><span class="line"><span class="keyword">if</span> (usernameSession != <span class="keyword">null</span>) &#123;</span><br><span class="line"><span class="comment">// 如果从session当中能够获取用户名，则可以进入我的网站</span></span><br><span class="line">out.println(<span class="string">"欢迎登陆我们的网站！用户名："</span> + usernameSession + <span class="string">" 密码："</span>+ passwordSession);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// 否则显示空白页面</span></span><br><span class="line">&#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP笔记-1-初次使用Servlet</title>
      <link href="/2016/11/29/jsp-1/"/>
      <url>/2016/11/29/jsp-1/</url>
      
        <content type="html"><![CDATA[<h3 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h3><ol><li>Eclipse Java EE IDE for Web Developers. Version: Mars.1 Release (4.5.1)</li><li>Apache Tomcat version: 8.0.39</li></ol><h3 id="课堂学习目标"><a href="#课堂学习目标" class="headerlink" title="课堂学习目标"></a>课堂学习目标</h3><p>了解servlet的虚拟路径使用方法，这里具体讲解使用Eclipse的servlet3.0之前的版本(使用servlet2.5版本)中关于WebContent/WEB-INF/web.xml的使用方法以及servlet3.0及之后的版本的虚拟路径使用方法。</p><h3 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h3><blockquote><p>测试一：利用servlet3.0之前的版本进行测试</p></blockquote><p>利用Eclipse新建一个Dynamin Web Project,调用安装的Apache Tomcat V8.0版本，然后选择2.5的servlet版本，该IDE会自动新建一个WebContent/WEB-INF/web.xml文件。</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa8yo0l2hnj30hy0jmmy9.jpg" alt="New Dynamic Web Project"></p><p>之后我们新建两个servlet，一个直接全部使用默认设置，另一个我们点击下一步后不使用默认自带的doPost()和doGet()方法。</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa8ytojjcyj30e60e7wf1.jpg" alt="Create Servlet"></p><p>新建一个index.jsp文件后，软件视图如图所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa8z11vt9wj311q0k1gog.jpg" alt="index.jsp"></p><p>因为本次我们需要利用servlet配置虚拟路径，所以我们直接利用未初始化重写方法doPost()和doGet()的Servlet_2.java进行操作，在Servlet_2.java中的Servlet_2类中空白处点击右键，选择”源码”,然后选择”覆盖/实现方法”中选择对应的doGet()，然后我们进行重写操作，输出一行数据。</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa8z899s4gj30rb0brt9h.jpg" alt="Servlet2.java"></p><p>然后我们就需要修改web.xml文件进行虚拟路径的部署，这里由于我们提到了web.xml文件，所以，我们需要介绍一下什么是web.xml文件。</p><h3 id="题外话：什么是web-xml文件？"><a href="#题外话：什么是web-xml文件？" class="headerlink" title="题外话：什么是web.xml文件？"></a>题外话：什么是web.xml文件？</h3><p>web.xml文件可以简单的理解成servlet的一个配置文件,通过这个配置文件来寻找对应的servlet处理业务。配置web.xml的目的就是让容器知道你的请求是由那个servlet处理，然后把httprequest和httpresponse对象注入到该servlet中。一个web.xml中当然可以存在多个servlet规则，具体的使用方法下面将进行介绍。</p><p>需要注意的是，所有部署描述符文件的顶层（根）元素为web-app。而XML元素不像HTML，他们是大小写敏感的。因此，web-App和WEB-APP都是不合法的，web-app必须用小写。 </p><h3 id="继续"><a href="#继续" class="headerlink" title="继续"></a>继续</h3><p>web.xml中配置映射servlet的规则十分简单，如下列代码所示：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;servlet&gt;</span><br><span class="line">    &lt;servlet-name&gt;Servlet_2&lt;/servlet-name&gt;</span><br><span class="line">    &lt;servlet-class&gt;com.bugwz.servlet.Servlet_2&lt;/servlet-class&gt;</span><br><span class="line">  &lt;/servlet&gt;</span><br><span class="line">  &lt;servlet-mapping&gt;</span><br><span class="line">    &lt;servlet-name&gt;Servlet_2&lt;/servlet-name&gt;</span><br><span class="line">    &lt;url-pattern&gt;/Servlet_2&lt;/url-pattern&gt;</span><br><span class="line">  &lt;/servlet-mapping&gt;</span><br></pre></td></tr></table></figure></p><p>如果你使用的servlet的版本是3.0之前的版本，如果你想要成功映射的话，你必须将这些代码(上面的代码是必须的，最简化)正确的在web.xml文件的web-app标签中书写，上述代码具体讲解如下：</p><ol><li>servlet标签<ul><li>servlet-name标签内的信息对应servlet-mapping标签内的servlet-name的标签内的信息，推荐其内的信息与servlet的类名相同。</li><li>servlet-class标签内的信息为映射对象的信息，具体格式为”servlet所属包名.servlet的类名”，这样才能找到具体的类，并执行。</li></ul></li><li>servlet-mapping标签<ul><li>servlet-name标签内的信息内容上面已经说了。</li><li>url-pattern标签内的信息为该项目下的虚拟路径，也就是说项目下的/Servlet_2就会展示类Servlet_2中的信息，这里的虚拟路径可以根据需求修改。实践可知，url-pattern中信息若为/*，则该项目的所有子栏目，全部指向该处对应的类，是否可以通过某种方式存放正则表达式待考证。</li></ul></li><li>执行过程为，根据servlet-mapping标签的子标签url-pattern的虚拟路径，按照servlet-mapping标签内的子标签servlet-name的信息，去寻找servlet标签中的子标签servlet-name中相同的信息，然后展示对应的servlet-class标签中的类中的可展示内容。</li></ol><blockquote><p>测试二：利用servlet3.0及其之后的版本进行测试</p></blockquote><p>这里需要说明的是如果你的项目目前使用的是servlet3.0及之后的版本，那么你的项目中不会直接创建web.xml，servlet3.0 及之后的版本中新增的注解支持简化了 Servlet/ 过滤器 / 监听器的声明，从而使得 web.xml 变为可选配置。这里我们使用servlet3.1版本，目前最新的版本。</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa90a1a2m7j30hu0jjq46.jpg" alt="New Dynamic Web Project - Version"></p><p>我们依次新建两个<code>Servlet_1.java</code>和<code>Servlet_2.java</code>，然后我们会发现在新建的servlet中带了下面这一行代码，如下图所示：</p><p><code>@WebServlet(&quot;/Servlet_2&quot;)</code></p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa90hj12e6j30su0d9mye.jpg" alt="Servlet_2"></p><p>上面那一行代码其实就充当了web.xml中的定义的虚拟路径的那几行代码的功能，因此servlet3.0及之后的版本简化了代码的书写量，更加简单易用。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP笔记-2-理解Servlet的init()、service()与destroy()</title>
      <link href="/2016/11/29/jsp-2/"/>
      <url>/2016/11/29/jsp-2/</url>
      
        <content type="html"><![CDATA[<h3 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h3><ol><li>Eclipse Java EE IDE for Web Developers. Version: Mars.1 Release (4.5.1)</li><li>Apache Tomcat version: 8.0.39</li></ol><h3 id="课堂学习目标"><a href="#课堂学习目标" class="headerlink" title="课堂学习目标"></a>课堂学习目标</h3><p>实践servlet的init()、service()与destroy()方法，理解servlet生命周期的三个阶段。</p><h3 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h3><p>我们还是新建一个名为Test的新工程，然后新建一个不带doGet()与doPost()方法的一个基本的servlet，然后我们依次循规蹈矩的手动覆盖重写三个方法，他们分别是init()、service()和destroy()方法，重写后截图如下：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9cyhh80xj30rj0bgwfq.jpg" alt="Servlet_1"></p><p>我们在浏览器地址栏中输入进入Servlet_1的虚拟路径后，控制台下便出现了，调用init()方法的输出信息，然后调用了service()方法的输出信息，这两个方法的调用就是servlet的前两个生命周期，初始化阶段，调用init()方法；响应客户请求阶段，调用service()方法，之后每次刷新浏览器界面会发现只出现调用service()方法的输出结果，如下所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9d42n3glj30kv08vaaj.jpg" alt="service()输出结果"></p><blockquote><p>提示：</p></blockquote><p>如果你发现你打不开映射的虚拟路径，浏览器并提示了404错误，请尝试移步<a href="http://www.bugwz.com/jsp1/#jsp_1_trouble" target="_blank" rel="noopener">这里</a> ，尝试解决问题。</p><p>之后，当我们停止服务器的时候，注意，这不是终止进程，还是有点区别的。</p><ol><li>停止服务器：web应用被终止，或Servlet容器终止运行，或Servlet容器重新装载Servlet新实例时，Servlet容器会先调用Servlet的destroy()方法，在destroy()方法中可以释放掉Servlet所占用的资源。</li><li>终止进程：servlet容器被强行终止，来不及调用Servlet的destroy()方法。</li></ol><p>我们点击停止服务器后效果图如下所示：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9dbaxac7j30sn0d4wgu.jpg" alt="Stop Server"></p><h3 id="来些总结"><a href="#来些总结" class="headerlink" title="来些总结"></a>来些总结</h3><p>Servlet生命周期分为三个阶段：</p><ol><li>初始化阶段，调用init()方法</li><li>响应客户请求阶段，调用service()方法</li><li>终止阶段，调用destroy()方法</li></ol><p>Servlet初始化阶段：(在下列时刻Servlet容器装载Servlet)</p><ol><li>Servlet容器启动时自动装载某些Servlet。</li><li>在Servlet容器启动后，客户首次向Servlet发送请求。</li><li>Servlet类文件被更新后，重新装载Servlet。</li></ol><blockquote><p>Servlet被装载后，Servlet容器创建一个Servlet实例并且调用Servlet的init()方法进行初始化。在Servlet的整个生命周期内，init()方法只被调用一次。</p></blockquote><p>Servlet响应请求阶段：</p><p>　　对于用户到达Servlet的请求，Servlet容器会创建特定于这个请求的ServletRequest对象和ServletResponse对象，然后调用Servlet的service方法。service方法从ServletRequest对象获得客户请求信息，处理该请求，并通过ServletResponse对象向客户返回响应信息。</p><p>Servlet终止阶段：</p><p>　　当web应用被终止，或Servlet容器终止运行，或Servlet容器重新装载Servlet新实例时，Servlet容器会先调用Servlet的destroy()方法，在destroy()方法中可以释放掉Servlet所占用的资源。</p><blockquote><p>关于Servlet生命周期与工作原理的更详细的信息，推荐访问<a href="http://www.cnblogs.com/cuiliang/archive/2011/10/21/2220671.html" target="_blank" rel="noopener">这里</a></p></blockquote>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP笔记-3-Servlet API</title>
      <link href="/2016/11/29/jsp-3/"/>
      <url>/2016/11/29/jsp-3/</url>
      
        <content type="html"><![CDATA[<h3 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h3><ol><li>Eclipse Java EE IDE for Web Developers.<br>Version: Neon.1a Release (4.6.1)</li><li>Apache Tomcat version: 8.0.39</li></ol><h3 id="总结知识点"><a href="#总结知识点" class="headerlink" title="总结知识点"></a>总结知识点</h3><h4 id="认识Servlet-API"><a href="#认识Servlet-API" class="headerlink" title="认识Servlet API:"></a>认识Servlet API:</h4><p>Servlet API由两个软件包组成：Javax.servlet包和Javax.servlet.http包。其中Javax.servlet包主要存放与http协议无关的一般性的servlet类，Javax.servlet.http包主要存放与http协议相关的功能的类。(两个软件包都位于Tomcat的servlet-api.jar中)</p><h4 id="本次主要说明的东西如下："><a href="#本次主要说明的东西如下：" class="headerlink" title="本次主要说明的东西如下："></a>本次主要说明的东西如下：</h4><ol><li>Javax.servlet.http包中的接口：<ul><li>HttpServletRequest接口：提供http请求信息。</li><li>HttpServletResponse接口：提供http响应。</li></ul></li><li>Javax.servlet包中的接口：<ul><li>ServletConfig接口：在初始化的过程中由Servlet容器使用。</li><li>ServletContext接口：定义Servlet用于获取来自其容器的信息的方法。</li><li>ServletRequest接口：向服务器提交请求。</li><li>ServletResponse接口：响应客户端请求。</li></ul></li><li>Javax.servlet包中的类：<ul><li>ServletInputStream类：用于从客户端读取二进制数据。</li><li>ServletOutputStream类：用于将二进制数据发送到客户端。</li><li>ServletException类：用于异常处理的类。</li><li>UnavailableException类：当servlet或filter不能用时，处理异常的类。</li></ul></li></ol><blockquote><p>#####HttpServletRequest接口</p></blockquote><p>HttpServletRequest接口对应客户端http的请求。当客户端通过http访问服务器时，http请求中的所有信息都封装在HttpServletRequest对象中，我们可以通过该对象一些方法获取很多信息，其中获取客户机信息，获取请求头信息，获取请求参数等，这主要说明这三种。</p><p>######1.获取客户机信息：</p><ul><li>getRequestURL方法返回客户端发出完整请求的URL</li><li>getRequestURI方法返回请求行中的资源名部分</li><li>getQueryString方法返回请求行中的参数部分</li><li>getRemoteAddr方法返回发出请求的客户机的IP地址</li><li>getMethod得到客户机请求方式</li><li>getContextPath获得工程虚拟目录名称</li></ul><p>部分代码如下所示：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//URI 和 URL 区别？ </span></span><br><span class="line"><span class="comment">//URI获得以webroot目录开始路径 URL获得以Http协议开始完整路径</span></span><br><span class="line"><span class="comment">//URI 描述路径范围比URL大（URL都是URI ） /lesson9_jsp/request1  ../request1 这些路径都是URI，不是URL</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RequestServletDemo1</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">System.out.println(request.getRequestURI()); </span><br><span class="line"><span class="comment">// 输出：/lesson9_jsp/RequestServletDemo1</span></span><br><span class="line">System.out.println(request.getRequestURL());</span><br><span class="line"><span class="comment">// 输出为：http://localhost:8080/lesson9_jsp/RequestServletDemo1</span></span><br><span class="line">System.out.println(request.getQueryString());</span><br><span class="line"><span class="comment">// 输出：null</span></span><br><span class="line">System.out.println(<span class="string">"您的ip是："</span> + request.getRemoteAddr());</span><br><span class="line"><span class="comment">// 输出：您的ip是：0:0:0:0:0:0:0:1</span></span><br><span class="line">System.out.println(<span class="string">"您的请求方式是： "</span> + request.getMethod());</span><br><span class="line"><span class="comment">// 输出：您的请求方式是： GET</span></span><br><span class="line">System.out.println(<span class="string">"工程虚拟目录:"</span> + request.getContextPath());</span><br><span class="line"><span class="comment">// 输出：工程虚拟目录:/lesson9_jsp</span></span><br><span class="line">System.out.println(<span class="string">"当前资源:"</span> + request.getServletPath());</span><br><span class="line"><span class="comment">// 输出：当前资源:/RequestServletDemo1</span></span><br><span class="line">System.out.println(<span class="string">"uri is:"</span>+request.getRequestURI());</span><br><span class="line"><span class="comment">// 输出：uri is:/lesson9_jsp/RequestServletDemo1</span></span><br><span class="line">System.out.println(<span class="string">"当前资源: "</span>+ request.getRequestURI().substring(request.getContextPath().length()));</span><br><span class="line"><span class="comment">// 输出：当前资源: /RequestServletDemo1</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>######2.获取请求头信息：</p><ul><li>getHeader(name)—String</li><li>getHeaders(String name)方法—Enumeration<string></string></li><li>getHeaderNames方法—Enumeration<string></string></li></ul><p>部分代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RequestServletDemo2</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// 获得所有头信息</span></span><br><span class="line">Enumeration&lt;String&gt; names = request.getHeaderNames();</span><br><span class="line"><span class="keyword">while</span> (names.hasMoreElements()) &#123;</span><br><span class="line"><span class="comment">// 每个头信息 名称</span></span><br><span class="line">String name = names.nextElement();</span><br><span class="line"><span class="comment">// 根据name 获得 value</span></span><br><span class="line">System.out.println(name + <span class="string">":"</span> + request.getHeader(name));</span><br><span class="line">&#125;</span><br><span class="line">System.out.println(<span class="string">"--------------------------------------"</span>);</span><br><span class="line"><span class="comment">// 获得一个指定头信息</span></span><br><span class="line">System.out.println(request.getHeader(<span class="string">"User-Agent"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>######3.获取请求参数：</p><ul><li>getParameter(name)—String 通过name获得值</li><li>getParameterValues—String[]通过name获得多值checkbox</li><li>getParameterNames—Enumeration<string>获得所有name</string></li><li>getParameterMap—Map&lt;String, String[]&gt; key :name value:多值</li></ul><p>部分代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RequestServletDemo3</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// 接收request.html 提交数据</span></span><br><span class="line">String name = request.getParameter(<span class="string">"name"</span>);</span><br><span class="line">String password = request.getParameter(<span class="string">"password"</span>);</span><br><span class="line">System.out.println(<span class="string">"name="</span> + name);</span><br><span class="line">System.out.println(<span class="string">"password="</span> + password);</span><br><span class="line"><span class="comment">// 判断name是否为空</span></span><br><span class="line"><span class="keyword">if</span> (name == <span class="keyword">null</span> || name.trim().length() == <span class="number">0</span>) &#123;</span><br><span class="line"><span class="comment">// 不合法</span></span><br><span class="line">request.getRequestDispatcher(<span class="string">"/request/request3.html"</span>).forward(</span><br><span class="line">request, response);</span><br><span class="line"><span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 获得多值 属性 hobby</span></span><br><span class="line"><span class="comment">// 如果存在多个值 getParameter 只能获得第一个值</span></span><br><span class="line"><span class="comment">// url?hobby=sport&amp;hobby=music ---- 使用getParameterValues</span></span><br><span class="line">System.out.println(request.getParameter(<span class="string">"hobby"</span>));</span><br><span class="line">String[] arr = request.getParameterValues(<span class="string">"hobby"</span>);</span><br><span class="line">System.out.println(Arrays.toString(arr));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 下面通过getParameterNames 和 getParameterMap 遍历所有请求参数</span></span><br><span class="line">System.out.println(<span class="string">"---------------------------------------"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第一种通过getParameterNames</span></span><br><span class="line">Enumeration&lt;String&gt; names = request.getParameterNames();</span><br><span class="line"><span class="keyword">while</span> (names.hasMoreElements()) &#123;</span><br><span class="line">String parameterName = names.nextElement();</span><br><span class="line">String[] parameterValues = request.getParameterValues(parameterName);</span><br><span class="line">System.out.println(parameterName + <span class="string">":"</span>+ Arrays.toString(parameterValues));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第二种通过 getParameterMap 遍历所有数据</span></span><br><span class="line">Map&lt;String, String[]&gt; map = request.getParameterMap();</span><br><span class="line"><span class="comment">// 通过keySet entrySet两种方式遍历map</span></span><br><span class="line">System.out.println(<span class="string">"getParameterMap keySet----------------------"</span>);</span><br><span class="line">Set&lt;String&gt; keySet = map.keySet();</span><br><span class="line"><span class="keyword">for</span> (String key : keySet) &#123;</span><br><span class="line">String[] values = map.get(key);</span><br><span class="line">System.out.println(key + <span class="string">": "</span> + Arrays.toString(values));</span><br><span class="line">&#125;</span><br><span class="line">System.out.println(<span class="string">"getParameterMap entrySet----------------------"</span>);</span><br><span class="line">Set&lt;Entry&lt;String, String[]&gt;&gt; entrySet = map.entrySet();</span><br><span class="line"><span class="keyword">for</span> (Entry&lt;String, String[]&gt; entry : entrySet) &#123;</span><br><span class="line">System.out.println(entry.getKey() + <span class="string">": "</span>+ Arrays.toString(entry.getValue()));</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><blockquote><p>#####HttpServletResponse接口</p></blockquote><p>HttpServletResponse对象提供服务器对客户端的http响应，它封装了向客户端提供数据、发送响应头、发送相应状态码等方法，这里主要说明指定状态码&amp;头信息，状态码和头信息应用以及它生成响应等操作。</p><p>######1.指定状态码&amp;头信息以及头信息应用：</p><p>部分代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ResponseServletDemo1</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// 设置状态码setStatus</span></span><br><span class="line"><span class="comment">// response.setStatus(404);</span></span><br><span class="line"><span class="comment">// response.setStatus(200);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 重定向设置使浏览器去访问另一个WEB资源</span></span><br><span class="line">response.setStatus(HttpServletResponse.SC_FOUND);</span><br><span class="line"><span class="comment">// 浏览器重定向，相对的和绝对的都可以</span></span><br><span class="line"><span class="comment">// response.setHeader("Location", "index.jsp");</span></span><br><span class="line"><span class="comment">// response.setHeader("Location", "/lesson9_jsp/index.jsp");</span></span><br><span class="line"><span class="comment">// 简化重定向</span></span><br><span class="line">response.sendRedirect(<span class="string">"/lesson9_jsp/index.jsp"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3秒后自动跳转 index.jsp</span></span><br><span class="line"><span class="comment">// 如果是客户端访问一个路径，此时必须 /工程名</span></span><br><span class="line">response.setHeader(<span class="string">"refresh"</span>, <span class="string">"3;url=/lesson9_jsp/index.jsp"</span>);</span><br><span class="line"><span class="comment">// 在跳转同时，提示用户页面将要跳转</span></span><br><span class="line">response.setContentType(<span class="string">"text/html;charset=utf-8"</span>);</span><br><span class="line">response.getWriter().println(<span class="string">"页面将在3秒后 自动跳转index.jsp, 如果没有跳转请点击&lt;a href='/lesson9_jsp/index.jsp'&gt;这里&lt;/a&gt;"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置三个禁止缓存头字段</span></span><br><span class="line">response.setHeader(<span class="string">"Cache-Control"</span>, <span class="string">"no-cache"</span>);</span><br><span class="line">response.setHeader(<span class="string">"Pragma"</span>, <span class="string">"no-cache"</span>);</span><br><span class="line"><span class="comment">// 设置Expires时，一般不设置String 日期字符串 书写复杂</span></span><br><span class="line"><span class="comment">// 参数一个毫秒 -1毫秒 -- java中时间 从1970年1月1日 0点开始计时</span></span><br><span class="line"><span class="comment">// 当前网页已经过期</span></span><br><span class="line">response.setDateHeader(<span class="string">"Expires"</span>, -<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>######2.response生成响应：<br>部分代码如下所示：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ResponseServletDemo5</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// 设置浏览器查看编码两种方式</span></span><br><span class="line"><span class="comment">// setContentType 具有 setCharacterEncoding 效果</span></span><br><span class="line">response.setContentType(<span class="string">"text/html;charset=UTF-8"</span>);</span><br><span class="line">response.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line"><span class="comment">// getOutputStream 与 getWriter 不能同时使用</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//字节流数据输出</span></span><br><span class="line"><span class="comment">// 下面语句报错提示：类型不匹配：不能从 HttpServletResponse 转换为 OutputStream</span></span><br><span class="line"><span class="comment">// OutputStream out = response。getOutputStream();</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 字符流输出</span></span><br><span class="line"><span class="comment">// response.getWriter().println("今天天气多云，可能有雨！");</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// tomcat会自动关流</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fadbvx0b5lj30qn0h1mzp.jpg" alt="Response Notice"></p><blockquote><p>#####ServletConfig接口与ServletContext接口</p></blockquote><p>######1. 关于ServletConfig接口：<br>在Servlet的配置文件中，可以使用一个或者多个<init-param>标签为servlet配置一些初始化参数，当servlet配置了初始化参数后，web容器在创建servlet实例对象时，会自动将这些初始化参数封装到ServletConfig对象中，并在调用servlet的init方法时，将ServletConfig的对象传递给servlet，进而，程序员通过ServletConfig对象就可以得到当前servlet的初始化参数信息，从一个servlet实例化之后，对任何客户端的任何时候访问都有效，一个servlet的ServletConfig不能被其他servlet访问。</init-param></p><p>部分代码如下所示:<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ServletConfigDemo1</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="comment">// private ServletConfig config;</span></span><br><span class="line"><span class="comment">// @Override</span></span><br><span class="line"><span class="comment">// public void init(ServletConfig config) throws ServletException &#123;</span></span><br><span class="line"><span class="comment">// // 父类GenericServlet中已经做了this.config=config就不需要重写了,不需要覆盖带参数的初始化方法</span></span><br><span class="line"><span class="comment">// // this.config = config;</span></span><br><span class="line"><span class="comment">// // 传入一个ServletConfig 参数，获得Servlet实例初始化数据</span></span><br><span class="line"><span class="comment">// System.out.println("init " + config.getInitParameter("uname"));</span></span><br><span class="line"><span class="comment">// System.out.println("init " + config.getInitParameter("password"));</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// // 获得所有参数name的集合</span></span><br><span class="line"><span class="comment">// Enumeration&lt;String&gt; names = config.getInitParameterNames();</span></span><br><span class="line"><span class="comment">// // 遍历names 获取所有初始化参数值</span></span><br><span class="line"><span class="comment">// while (names.hasMoreElements()) &#123;</span></span><br><span class="line"><span class="comment">// String name = (String) names.nextElement();</span></span><br><span class="line"><span class="comment">// System.out.println("init " + name + "="</span></span><br><span class="line"><span class="comment">// + config.getInitParameter(name));</span></span><br><span class="line"><span class="comment">// &#125;</span></span><br><span class="line"><span class="comment">// &#125;</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line"><span class="comment">// 覆盖无参的初始化方法,config是私有的参数,就不能用了,通过getServletConfig方法获取config对象</span></span><br><span class="line">System.out.println(<span class="string">"init "</span> + getServletConfig().getInitParameter(<span class="string">"uname"</span>));</span><br><span class="line">System.out.println(<span class="string">"init "</span> + getServletConfig().getInitParameter(<span class="string">"password"</span>));</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// 在doGet方法中 如何获得初始化数据 ？？？？从init 方法传递config 到doGet</span></span><br><span class="line"><span class="comment">// 使用保存成员变量 config 获得初始化信息</span></span><br><span class="line"><span class="comment">// System.out.println("doGet " + config.getInitParameter("uname"));</span></span><br><span class="line"><span class="comment">// System.out.println("doGet " + config.getInitParameter("password"));</span></span><br><span class="line"></span><br><span class="line">System.out.println(<span class="string">"doGet "</span>+ getServletConfig().getInitParameter(<span class="string">"uname"</span>));</span><br><span class="line">System.out.println(<span class="string">"doGet "</span>+ getServletConfig().getInitParameter(<span class="string">"password"</span>));</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>######2. 关于ServletContext接口：</p><p>在WEB容器启动的时候，他会为每一个WEB应用程序都创建一个对应的ServletContext对象，它代表当前web应用。ServletCofig对象中维护了ServletContext对象的引用，开发人员在编写servlet时，可以通过ServletConfig.getServletContext方法获得ServletContext对象。由于一个WEB应用中的所有Servlet共享同一个ServletContext对象来实现通讯。ServletContext对象通常也被称之为context域对象。ServletContext对恩赫servlet，任何人在任何时间都有效，是真正的全部对象。</p><p>部分代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ServletContextCountDemo1</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="comment">// 初始化访问次数0</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line"><span class="comment">// 在ServletContext对象中保存一个访问次数</span></span><br><span class="line">ServletContext context = getServletContext();</span><br><span class="line"><span class="comment">// 保存一个数据</span></span><br><span class="line">context.setAttribute(<span class="string">"count"</span>, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function"><span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// 每次访问获得原来访问次数+1</span></span><br><span class="line">ServletContext context = getServletContext();</span><br><span class="line"><span class="comment">// 或者使用下面这条语句获取context</span></span><br><span class="line"><span class="comment">// ServletContext context = getServletConfig().getServletContext();</span></span><br><span class="line"><span class="comment">// 获得访问次数</span></span><br><span class="line"><span class="keyword">int</span> count = (Integer) context.getAttribute(<span class="string">"count"</span>);</span><br><span class="line"><span class="comment">// 次数+1保存</span></span><br><span class="line">count++;</span><br><span class="line">context.setAttribute(<span class="string">"count"</span>, count);</span><br><span class="line">System.out.println(<span class="string">"网站被访问次数为"</span> + count);</span><br><span class="line"><span class="comment">// 浏览器显示中文</span></span><br><span class="line">response.setContentType(<span class="string">"text/html;charset=utf-8"</span>);</span><br><span class="line">response.getWriter().println(<span class="string">"Demo1网站被访问次数为"</span> + count);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>JSP作业-1-Servlet课后作业</title>
      <link href="/2016/11/29/jsp-homework-1/"/>
      <url>/2016/11/29/jsp-homework-1/</url>
      
        <content type="html"><![CDATA[<h3 id="作业要求："><a href="#作业要求：" class="headerlink" title="作业要求："></a>作业要求：</h3><ol><li><a href="#1-1" id="1">编写一个Serlvet以便显示该Servlet被访问的次数。</a></li><li><a href="#2-2" id="2">编写一个Servlet程序，在doGet方法中显示一个Form表单，用户可以输入姓名和电子邮件地址，用户提交该表单后，doPost方法动态读出请求参数，并输出这些参数。提示：使用request对象的getParameterNames（）方法。</a></li><li><a href="#3-3" id="3">描述Serlvet的生命周期。</a></li><li><a href="#4-4" id="4">实现Servlet有几种方法，每种方法都有哪些特点。</a></li><li><a href="#5-5" id="5">如何配置Serlvet。</a></li></ol><h3 id="开始作业"><a href="#开始作业" class="headerlink" title="开始作业"></a>开始作业</h3><blockquote><p><a id="1-1"></a>作业 1：<a href="#1">点此查看要求</a></p></blockquote><p>仅贴出Servlet的主体代码如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/Servlet_1"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Servlet_1</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@see</span> HttpServlet#HttpServlet()</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Servlet_1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="comment">// TODO Auto-generated constructor stub</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> num=<span class="number">1</span>;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="comment">//super.doGet(req, resp);</span></span><br><span class="line">resp.setHeader(<span class="string">"Content-type"</span>, <span class="string">"text/html;charset=UTF-8"</span>);  </span><br><span class="line">PrintWriter pw = resp.getWriter();</span><br><span class="line">pw.println(<span class="string">"Servlet被访问的次数为："</span>+num++);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>运行图示如下：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9e0wjqj1j30el04iwee.jpg" alt="Servlet Num"></p><blockquote><p><a id="2-2"></a>作业 2：<a href="#2">点此查看要求</a></p></blockquote><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/Servlet_2"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Servlet_2</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@see</span> HttpServlet#HttpServlet()</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Servlet_2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="comment">// TODO Auto-generated constructor stub</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// super.doGet(req, resp);</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">req.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line">resp.setHeader(<span class="string">"Content-type"</span>, <span class="string">"text/html;charset=UTF-8"</span>);  </span><br><span class="line">PrintWriter out = resp.getWriter();</span><br><span class="line">out.println(<span class="string">"&lt;html&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;head&gt;&lt;title&gt;表单提交&lt;/title&gt;&lt;/head&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;body&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;h1&gt;表单提交页面&lt;/h1&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;form action='Servlet_2' method='post'&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"姓名：&lt;input type='text' name='name' /&gt;&lt;br&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"邮箱：&lt;input type='mail' name='mail' /&gt;&lt;br&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;input type='submit' value='提交'&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;/form&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;/body&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"&lt;/html&gt;"</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// super.doPost(req, resp);</span></span><br><span class="line">req.setCharacterEncoding(<span class="string">"UTF-8"</span>);</span><br><span class="line">resp.setHeader(<span class="string">"Content-type"</span>, <span class="string">"text/html;charset=UTF-8"</span>);  </span><br><span class="line">String name = req.getParameter(<span class="string">"name"</span>);</span><br><span class="line">String mail = req.getParameter(<span class="string">"mail"</span>);</span><br><span class="line">PrintWriter out = resp.getWriter();</span><br><span class="line">out.println(<span class="string">"提交的姓名为："</span>+name+<span class="string">"&lt;br /&gt;"</span>);</span><br><span class="line">out.println(<span class="string">"提交的邮箱为："</span>+mail+<span class="string">"&lt;br /&gt;"</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行图示如下：</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9euf82olj308e04o3yf.jpg" alt="Form Page"><br><img src="http://cdn.bugwz.com/006qpCDTjw1fa9eufmacjj308q03njr8.jpg" alt="Form Submit"></p><blockquote><p><a id="3-3"></a>作业 3：<a href="#3">点此查看要求</a></p></blockquote><p>  具体答案内容，请查看<a href="http://bugwz.com/jsp_2/#jsp_2_lifecycle" target="_blank" rel="noopener">这里</a></p><blockquote><p><a id="4-4"></a>作业 4：<a href="#4">点此查看要求</a></p></blockquote><p>Servlet有三种实现方式：</p><ul><li>继承原生Servlet接口</li><li>继承GenericServlet类</li><li>继承HttpServlet类</li></ul><h3 id="Servlet的这三种实现方式的具体讲解如下："><a href="#Servlet的这三种实现方式的具体讲解如下：" class="headerlink" title="Servlet的这三种实现方式的具体讲解如下："></a>Servlet的这三种实现方式的具体讲解如下：</h3><h4 id="1-继承原生Servlet接口："><a href="#1-继承原生Servlet接口：" class="headerlink" title="1.继承原生Servlet接口："></a>1.继承原生Servlet接口：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/Servlet_1_1"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Servlet_1_1</span> <span class="keyword">implements</span> <span class="title">Servlet</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line">        System.out.println(<span class="string">"destroy...."</span>);  </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ServletConfig <span class="title">getServletConfig</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub  </span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;  </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getServletInfo</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub  </span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;  </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//该 函数用于初始化，只会被调用一次（当用户第一次访问Servlet时，被调用）  </span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(ServletConfig arg0)</span> <span class="keyword">throws</span> ServletException </span>&#123;  </span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub  </span></span><br><span class="line">        System.out.println(<span class="string">"init...."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//用于处理业务逻辑，应该把业务逻辑代码写在该方法中  </span></span><br><span class="line">    <span class="comment">//会被多次调用，当用户每访问一次时就会被调用一次  </span></span><br><span class="line">    <span class="comment">//request用于获取客户端的信息  </span></span><br><span class="line">    <span class="comment">//response用户向客户端返回信息  </span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">service</span><span class="params">(ServletRequest request, ServletResponse response)</span>  </span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> ServletException, IOException </span>&#123;  </span><br><span class="line">        <span class="comment">// TODO Auto-generated method stub  </span></span><br><span class="line">        System.out.println(<span class="string">"service...."</span>);       </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="注解："><a href="#注解：" class="headerlink" title="注解："></a>注解：</h5><p>这种实现方法是新建一个类，该类继承了Servlet接口，因为新建类的缘故，我们需要手动添加<code>@WebServlet(&quot;/Servlet_1_1&quot;)</code>代码，然后重写Servlet接口的五个方法。</p><p>########## 题外话：Servlet接口的五个方法</p><ol><li>init()</li><li>service()</li><li>destroy()</li><li>getServletConfig()</li><li>getServletInfo()</li></ol><h5 id="init-方法："><a href="#init-方法：" class="headerlink" title="init()方法："></a>init()方法：</h5><p>在Servlet实例化后，Servlet容器会调用init()方法来初始化该对象，主要是为了让Servlet对象在处理客户请求前可以完成一些初始化工作，例如：建立数据库的连接，获取配置信息等。对于每一个Servlet实例，init()方法只能被调用一次。init()方法有一个类型为ServletConfig的参数，Servlet容器通过这个参数向Servlet传递配置信息。Servlet使用ServletConfig对象从Web应用程序的配置信息中获取以名-值对形式提供的初始化参数。另外，在Servlet中，还可以通过ServletConfig对象获取描述Servlet运行环境的ServletContext对象，使用该对象，Servlet可以和它的Servlet容器进行通信。</p><h5 id="Service-方法："><a href="#Service-方法：" class="headerlink" title="Service()方法："></a>Service()方法：</h5><p>容器调用service()方法来处理客户端的请求。要注意的是，在service()方法被容器调用之前，必须确保init()方法正确完成。容器会构造一个表示客户端请求信息的请求对象（类型为ServletRequest）和一个用于对客户端进行响应的响应对象（类型为ServletResponse）作为参数传递给service()。在service()方法中，Servlet对象通过ServletRequest对象得到客户端的相关信息和请求信息，在对请求进行处理后，调用ServletResponse对象的方法设置响应信息。</p><h5 id="destroy-方法："><a href="#destroy-方法：" class="headerlink" title="destroy()方法："></a>destroy()方法：</h5><p>当容器检测到一个Servlet对象应该从服务中被移除的时候，容器会调用该对象的destroy()方法，以便让Servlet对象可以释放它所使用的资源，保存数据到持久存储设备中，例如将内存中的数据保存到数据库中，关闭数据库的连接等。当需要释放内存或者容器关闭时，容器就会调用Servlet对象的destroy()方法，在Servlet容器调用destroy()方法前，如果还有其他的线程正在service()方法中执行容器会等待这些线程执行完毕或者等待服务器设定的超时值到达。一旦Servlet对象的destroy()方法被调用，容器不回再把请求发送给该对象。如果需要改Servlet再次为客户端服务，容器将会重新产生一个Servlet对象来处理客户端的请求。在destroy()方法调用之后，容器会释放这个Servlet对象，在随后的时间内，该对象会被java的垃圾收集器所回收。</p><h5 id="getServletConfig-方法："><a href="#getServletConfig-方法：" class="headerlink" title="getServletConfig()方法："></a>getServletConfig()方法：</h5><p>该方法返回容器调用init()方法时传递给Servlet对象的ServletConfig对象，ServletConfig对象包含了Servlet的初始化参数。</p><h5 id="getServletInfo-方法："><a href="#getServletInfo-方法：" class="headerlink" title="getServletInfo()方法："></a>getServletInfo()方法：</h5><p>返回一个String类型的字符串，其中包括了关于Servlet的信息，例如，作者、版本和版权。该方法返回的应该是纯文本字符串，而不是任何类型的标记。</p><p>关于Servlet接口的五个方法的精彩文字转载自<a href="http://www.cnblogs.com/freeabyss/archive/2013/07/11/3187047.html" target="_blank" rel="noopener">这里</a>。</p><h4 id="2-继承GenericServlet类："><a href="#2-继承GenericServlet类：" class="headerlink" title="2.继承GenericServlet类："></a>2.继承GenericServlet类：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/Servlet_1_2"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Servlet_1_2</span> <span class="keyword">extends</span> <span class="title">GenericServlet</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">//重写service方法即可</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">service</span><span class="params">(ServletRequest arg0, ServletResponse arg1)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="keyword">try</span>&#123;  </span><br><span class="line">            System.out.println(<span class="string">"service...."</span>);</span><br><span class="line">        &#125;<span class="keyword">catch</span>(Exception e)&#123;  </span><br><span class="line">            e.printStackTrace();  </span><br><span class="line">        &#125;  </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="注解：-1"><a href="#注解：-1" class="headerlink" title="注解："></a>注解：</h5><p>这里的实现方法也是新建一个类，手动添加<code>@WebServlet(&quot;/Servlet_1_2&quot;)</code>这一行代码，然后只重写service()方法即可，如果我们直接通过实现Servlet接口来编写一个Servlet类，就需要实现Servlet接口中定义的5种方法，为了简化Servlet的编写，在javax.servlet包中，给我们提供了一个抽象的类GenericServlet，它提供了除service()方法外的其他4种方法的简单实现。GenericServlet类定义了一个通用的，不依赖具体协议的Servlet。实践出现的一个有意思的事情是，我进入映射的虚拟路径/Servlet_1_2后，发现控制台输出的信息如下，这里我并没有修改init()方法:</p><p><img src="http://cdn.bugwz.com/006qpCDTjw1fa9wwqs4wqj30ld0a9wfk.jpg" alt="控制台"></p><h4 id="2-继承HttpServlet类："><a href="#2-继承HttpServlet类：" class="headerlink" title="2.继承HttpServlet类："></a>2.继承HttpServlet类：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@WebServlet</span>(<span class="string">"/Servlet_1"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Servlet_1</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span> </span>&#123;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">1L</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">service</span><span class="params">(HttpServletRequest arg0, HttpServletResponse arg1)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="comment">// super.service(arg0, arg1);</span></span><br><span class="line">System.out.println(<span class="string">"service...."</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="comment">// super.destroy();</span></span><br><span class="line">System.out.println(<span class="string">"destroy...."</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line"><span class="comment">// TODO 自动生成的方法存根</span></span><br><span class="line"><span class="comment">// super.init();</span></span><br><span class="line">System.out.println(<span class="string">"init....."</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="注解：-2"><a href="#注解：-2" class="headerlink" title="注解："></a>注解：</h5><p>新建一个Servlet类时的默认构建方法，也是最常用的构建方法。由于大多数网络应用中，都是浏览器通过HTTP协议去访问服务器资源，而我们编写的Servlet也主要是应用于HTTP协议的请求和响应，为了快速开发应用于HTTP协议的Servlet，在javax.servlet.http包中给我们提供了一个抽象的类HttpServlet，他继承自GenericServlet类，用于创建适合Web站点的HTTP Servlet。</p><blockquote><p><a id="5-5"></a>作业 5：<a href="#5">点此查看要求</a></p></blockquote><p>配置Serlvet的过程可分为两种，一种是Servlet3.0版本之前的配置，相对来说比较麻烦，还有一种是Servlet3.0版本之后的配置，相对来说简单很多，具体的配置方法，我在<a href="http://bugwz.com/jsp_1/" target="_blank" rel="noopener">JSP笔记_1_初次使用Servlet</a>中已经详细说明过了，这里就不做具体说明了。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> JSP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>Markdown语法大全</title>
      <link href="/2016/02/28/markdown/"/>
      <url>/2016/02/28/markdown/</url>
      
        <content type="html"><![CDATA[<h3 id="欢迎使用Markdown"><a href="#欢迎使用Markdown" class="headerlink" title="欢迎使用Markdown"></a>欢迎使用Markdown</h3><p>@(示例笔记本)[Markdown|帮助|Markdown]</p><ul><li><strong>功能丰富</strong> ：支持高亮代码块、<em>LaTeX</em> 公式、流程图，本地图片以及附件上传，甚至截图粘贴，工作学习好帮手；</li><li><strong>得心应手</strong> ：简洁高效的编辑器，提供[桌面客户端][1]以及[离线Chrome App][2]，支持移动端 Web；</li><li><strong>深度整合</strong> ：支持选择笔记本和添加标签，支持从印象笔记跳转编辑，轻松管理。</li></ul><h3 id="Markdown简介"><a href="#Markdown简介" class="headerlink" title="Markdown简介"></a>Markdown简介</h3><blockquote><p>Markdown 是一种轻量级标记语言，它允许人们使用易读易写的纯文本格式编写文档，然后转换成格式丰富的HTML页面。    —— <a href="https://zh.wikipedia.org/wiki/Markdown" target="_blank" rel="noopener">维基百科</a></p></blockquote><p>正如您在阅读的这份文档，它使用简单的符号标识不同的标题，将某些文字标记为<strong>粗体</strong>或者<em>斜体</em>，创建一个<a href="http://www.example.com" target="_blank" rel="noopener">链接</a>或一个脚注[^demo]。下面列举了几个高级功能，更多语法请按<code>Ctrl + /</code>查看帮助。 </p><h3 id="代码块"><a href="#代码块" class="headerlink" title="代码块"></a>代码块</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@requires_authorization</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">somefunc</span><span class="params">(param1=<span class="string">''</span>, param2=<span class="number">0</span>)</span>:</span></span><br><span class="line">    <span class="string">'''A docstring'''</span></span><br><span class="line">    <span class="keyword">if</span> param1 &gt; param2: <span class="comment"># interesting</span></span><br><span class="line">        <span class="keyword">print</span> <span class="string">'Greater'</span></span><br><span class="line">    <span class="keyword">return</span> (param2 - param1 + <span class="number">1</span>) <span class="keyword">or</span> <span class="keyword">None</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SomeClass</span>:</span></span><br><span class="line">    <span class="keyword">pass</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>message = <span class="string">'''interpreter</span></span><br><span class="line"><span class="string"><span class="meta">... </span>prompt'''</span></span><br></pre></td></tr></table></figure><h3 id="LaTeX-公式"><a href="#LaTeX-公式" class="headerlink" title="LaTeX 公式"></a>LaTeX 公式</h3><p>可以创建行内公式，例如 $\Gamma(n) = (n-1)!\quad\forall n\in\mathbb N$。或者块级公式：</p><p>$$    x = \dfrac{-b \pm \sqrt{b^2 - 4ac}}{2a} $$</p><h3 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h3><table><thead><tr><th style="text-align:left">Item</th><th style="text-align:right">Value</th><th style="text-align:center">Qty</th></tr></thead><tbody><tr><td style="text-align:left">Computer</td><td style="text-align:right">1600 USD</td><td style="text-align:center">5</td></tr><tr><td style="text-align:left">Phone</td><td style="text-align:right">12 USD</td><td style="text-align:center">12</td></tr><tr><td style="text-align:left">Pipe</td><td style="text-align:right">1 USD</td><td style="text-align:center">234</td></tr></tbody></table><h3 id="流程图"><a href="#流程图" class="headerlink" title="流程图"></a>流程图</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">st=&gt;start: Start</span><br><span class="line">e=&gt;end</span><br><span class="line">op=&gt;operation: My Operation</span><br><span class="line">cond=&gt;condition: Yes or No?</span><br><span class="line"></span><br><span class="line">st-&gt;op-&gt;cond</span><br><span class="line">cond(yes)-&gt;e</span><br><span class="line">cond(no)-&gt;op</span><br></pre></td></tr></table></figure><p>以及时序图:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Alice-&gt;Bob: Hello Bob, how are you?</span><br><span class="line">Note right of Bob: Bob thinks</span><br><span class="line">Bob--&gt;Alice: I am good thanks!</span><br></pre></td></tr></table></figure><blockquote><p><strong>提示：</strong>想了解更多，请查看<strong>流程图</strong>[语法][3]以及<strong>时序图</strong>[语法][4]。</p></blockquote><h3 id="复选框"><a href="#复选框" class="headerlink" title="复选框"></a>复选框</h3><p>使用 <code>- [ ]</code> 和 <code>- [x]</code> 语法可以创建复选框，实现 todo-list 等功能。例如：</p><ul><li style="list-style: none"><input type="checkbox" checked> 已完成事项</li><li style="list-style: none"><input type="checkbox"> 待办事项1</li><li style="list-style: none"><input type="checkbox"> 待办事项2</li></ul><blockquote><p><strong>注意：</strong>目前支持尚不完全，在印象笔记中勾选复选框是无效、不能同步的，修改 Markdown 原文才可生效。</p></blockquote><h3 id="印象笔记相关"><a href="#印象笔记相关" class="headerlink" title="印象笔记相关"></a>印象笔记相关</h3><h3 id="笔记本和标签"><a href="#笔记本和标签" class="headerlink" title="笔记本和标签"></a>笔记本和标签</h3><p><strong>增加</strong>了<code>@(笔记本)[标签A|标签B]</code>语法, 以选择笔记本和添加标签。 <strong>绑定账号后</strong>， 输入<code>(</code>自动会出现笔记本列表，请从中选择。</p><h3 id="笔记标题"><a href="#笔记标题" class="headerlink" title="笔记标题"></a>笔记标题</h3><p>会自动使用文档内出现的<strong>第一个标题</strong>作为笔记标题。例如本文，就是第一行的 <code>欢迎使用</code>。</p><h3 id="快捷编辑"><a href="#快捷编辑" class="headerlink" title="快捷编辑"></a>快捷编辑</h3><p>保存在印象笔记中的笔记，右上角会有一个红色的编辑按钮，点击后会回到中打开并编辑该笔记。</p><blockquote><p><strong>注意：</strong>目前用户在印象笔记中单方面做的任何修改。</p></blockquote><h3 id="数据同步"><a href="#数据同步" class="headerlink" title="数据同步"></a>数据同步</h3><p>通过<strong>将Markdown原文以隐藏内容保存在笔记中</strong>的精妙设计，实现了对Markdown的存储和再次编辑。既解决了其他产品只是单向导出HTML的单薄，又规避了服务端存储Markdown带来的隐私安全问题。这样，服务端仅作为对印象笔记 API调用和数据转换之用。</p><blockquote><p>**隐私声明：用户所有的笔记数据，均保存在印象笔记中。</p></blockquote><h3 id="离线存储"><a href="#离线存储" class="headerlink" title="离线存储"></a>离线存储</h3><p>使用浏览器离线存储将内容实时保存在本地，不必担心网络断掉或浏览器崩溃。为了节省空间和避免冲突，已同步至印象笔记并且不再修改的笔记将删除部分本地缓存，不过依然可以随时通过<code>文档管理</code>打开。</p><blockquote><p><strong>注意：</strong>虽然浏览器存储大部分时候都比较可靠，但印象笔记作为专业云存储，更值得信赖。以防万一，<strong>请务必经常及时同步到印象笔记</strong>。</p></blockquote><h3 id="编辑器相关"><a href="#编辑器相关" class="headerlink" title="编辑器相关"></a>编辑器相关</h3><h3 id="设置"><a href="#设置" class="headerlink" title="设置"></a>设置</h3><p>右侧系统菜单（快捷键<code>Ctrl + M</code>）的<code>设置</code>中，提供了界面字体、字号、自定义CSS、vim/emacs 键盘模式等高级选项。</p><h3 id="快捷键"><a href="#快捷键" class="headerlink" title="快捷键"></a>快捷键</h3><p>帮助    <code>Ctrl + /</code><br>同步文档    <code>Ctrl + S</code><br>创建文档    <code>Ctrl + Alt + N</code><br>最大化编辑器    <code>Ctrl + Enter</code><br>预览文档 <code>Ctrl + Alt + Enter</code><br>文档管理    <code>Ctrl + O</code><br>系统菜单    <code>Ctrl + M</code> </p><p>加粗    <code>Ctrl + B</code><br>插入图片    <code>Ctrl + G</code><br>插入链接    <code>Ctrl + L</code><br>提升标题    <code>Ctrl + H</code></p><p>本文转载自<a href="https://maxiang.io/" target="_blank" rel="noopener">https://maxiang.io/</a>.</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Markdown </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>基于SS-Panel V2的SS面板式管理系统</title>
      <link href="/2016/02/01/ss-panel-v2/"/>
      <url>/2016/02/01/ss-panel-v2/</url>
      
        <content type="html"><![CDATA[<h3 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h3><p>本人之前接触过Shadowsocks这个软件，感觉其实是挺好用的，因为这款软件最初的着力点是便捷的连接，通信的速度，所以使用起来的感受相比之前使用的VPN还是好了太多太多。如果是针对于像我一样有些固执的人来说，我是绝对会为了个人的方便以及使用的稳定性而购置一台服务器并进行搭建Shadowsocks服务器的，但是针对于那些Shadowsocks服务商来说，租赁出去的服务器的限制条件无非是使用时间以及使用流量。因此，针对于限制流量这一方面的话，Shadowsocks自身就无法做到了，这也是本文的初衷，可视化的界面管理工具的出现以及流量监控等功能的出现也会减少维护的繁琐程度，OK，进入正文。</p><p>注：这里所说的前端与后端是指用户看到的部分以及用户看不到的部分，看到的部分就是SS-Panel V2的界面(即前端)，看不到的部分就是运行在后面的Shadowsocks等部分(即后端)。</p><h3 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h3><blockquote><h4 id="1-ShadowsocksR多用户版服务端安装教程-后端"><a href="#1-ShadowsocksR多用户版服务端安装教程-后端" class="headerlink" title="1. ShadowsocksR多用户版服务端安装教程(后端)"></a>1. ShadowsocksR多用户版服务端安装教程(后端)</h4></blockquote><h4 id="①-安装基本库："><a href="#①-安装基本库：" class="headerlink" title="① . 安装基本库："></a>① . 安装基本库：</h4><ol><li><p>CentOS系统:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install python-setuptools</span><br><span class="line">easy_install pip</span><br><span class="line">yum install git</span><br></pre></td></tr></table></figure></li><li><p>Ubuntu/Debian系统(推荐)：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt-get update</span><br><span class="line">apt-get install python-pip git -y</span><br></pre></td></tr></table></figure></li></ol><p>注：如果要使用 salsa20 和 chacha20 算法，请安装 <a href="https://github.com/jedisct1/libsodium" target="_blank" rel="noopener">libsodium</a>，最新版本请点击<a href="https://github.com/jedisct1/libsodium/releases" target="_blank" rel="noopener">这里</a>。</p><h4 id="2-安装cymysql"><a href="#2-安装cymysql" class="headerlink" title="2. 安装cymysql"></a>2. 安装cymysql</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install cymysql</span><br></pre></td></tr></table></figure><blockquote><h4 id="3-获取ShadowsocksR源代码"><a href="#3-获取ShadowsocksR源代码" class="headerlink" title="3. 获取ShadowsocksR源代码"></a>3. 获取ShadowsocksR源代码</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> -b manyuser https://github.com/breakwa11/shadowsocks.git</span><br></pre></td></tr></table></figure></blockquote><p>执行完毕后此目录会新建一个shadowsocks目录，其中根目录(当前目录)的是多用户版（用户数据存放在数据库中），子目录(当前目录下的shadowsocks目录)中的是单用户版(用户数据存放在文件中)。</p><blockquote><h4 id="4-服务端配置"><a href="#4-服务端配置" class="headerlink" title="4. 服务端配置"></a>4. 服务端配置</h4></blockquote><p>在多用户版的根目录中，复制<code>mysql.json</code>文件为<code>usermysql.json</code>，然后修改<code>usermysql.json</code>并找到对应的信息栏目(一定要记住!!!)：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cp mysql.json usermysql.json</span><br><span class="line">vi usermysql.json</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="string">"host"</span>: <span class="string">"127.0.0.1"</span>, //前端mysql域名/IP</span><br><span class="line"><span class="string">"port"</span>: 3306, //mysql端口</span><br><span class="line"><span class="string">"user"</span>: <span class="string">"ss"</span>, //mysql用户名</span><br><span class="line"><span class="string">"password"</span>: <span class="string">"pass"</span>, //mysql密码</span><br><span class="line"><span class="string">"db"</span>: <span class="string">"shadowsocks"</span>, //数据库名</span><br></pre></td></tr></table></figure><blockquote><h4 id="5-配置文件config-json"><a href="#5-配置文件config-json" class="headerlink" title="5. 配置文件config.json"></a>5. 配置文件config.json</h4></blockquote><p>在多用户版的根目录将文件config.json复制一份到user-config.json，然后编辑：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cp config.json user-config.json</span><br><span class="line">vi user-config.json</span><br></pre></td></tr></table></figure></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="string">"method"</span>:<span class="string">"aes-256-cfb"</span>, //修改成您要的加密方式的名称</span><br><span class="line"><span class="string">"protocol"</span>: <span class="string">"auth_sha1_compatible"</span>, //修改成您要的协议插件名称</span><br><span class="line"><span class="string">"obfs"</span>: <span class="string">"tls1.0_session_auth_compatible"</span>, //修改成您要的混淆插件名称</span><br></pre></td></tr></table></figure><blockquote><p>本文的主要内容来源于<a href="https://www.dou-bi.co/ss-jc30/" target="_blank" rel="noopener">逗比根据地</a></p></blockquote>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Shadowsocks </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>PHP与正则爬虫实践</title>
      <link href="/2016/01/26/php-regular/"/>
      <url>/2016/01/26/php-regular/</url>
      
        <content type="html"><![CDATA[<h3 id="0-前言"><a href="#0-前言" class="headerlink" title="0.前言"></a>0.前言</h3><p>之前听说过我们学校有一个学生信息的接口，最近这几天闲来无事，打算用PHP做一个爬虫，爬一下数据。由于我对数据的情有独钟，因此，弄完后打算写个文章记录下我的过程，也算是温习一下（这其中有个小插曲：本来我都写完了，可是因手贱原因，重新安装系统了，又手贱把D盘的一些软件全删了，重新安了一遍，结果把写好的爬虫文件删除了。。。。。。尝试用Recuva软件恢复，可是还是没恢复成功，没办法了，周六中午10点又写了一遍，两个小时吧，才弄好。。。真是累呀<del>~</del>）。推荐几个关于正则表达式的链接：<a href="http://tool.lu/regex/" target="_blank">正则表达式在线工具</a> —-<a href="http://www.jb51.net/shouce/jquery1.82/regexp.html" target="_blank">正则表达式速查表</a>—-<a href="http://www.yiifcms.com/soft/9/" target="_blank">下载正则表达式CHM</a></p><h3 id="1-正文"><a href="#1-正文" class="headerlink" title="1.正文"></a>1.正文</h3><p>这里只用PHP写了爬虫，毕竟其他的我也不会。考虑到需要使用正则，因此我又去温习了一遍正则，网上有很多正则学习的地方，这里就不举例了。因为我是爬的校园内的接口，因此我会隐去网址的一部分。</p><p>首先需要获取目标网址，下面 <code>$url</code> 为目标地址，<code>$num</code> 为学号，这个接口是使用学号查询的，因此把学号弄成变量。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$html=file_get_contents($url.$num);</span><br><span class="line">$html=str_replace(<span class="keyword">array</span>(<span class="string">"/r"</span>,<span class="string">"/n"</span>,<span class="string">"/t"</span>,<span class="string">"/s"</span>), <span class="string">''</span>, $html);</span><br></pre></td></tr></table></figure><p>为了获取该网页中的数据信息，必须对存储着网页代码的$html文件进行正则查询，因此：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">preg_match_all(<span class="string">'/&lt;div[^&gt;]*&gt;(.*?)&lt;\/div&gt;/si'</span>,$html,$match);</span><br></pre></td></tr></table></figure></p><p>然后我们通过 <code>print_r($match);</code> 会发现，<code>$match</code> 为一个二维数组，其中一个数组中存储着匹配的所有数据，包括学生姓名，性别，学号，学院，校园邮箱，其实还有一个隐藏的信息，那就是身份证号，通过查看网页源代码可以发现，但是 <code>$match</code> 数组中并没有这个数据，为止，我们可以使用下面的代码正则匹配身份证号码，其中$html为保存的网页源码变量，将匹配的身份证号码存入 <code>$idcard</code> ，但是如果想要输出身份证号码的话，要echo $idcard[0];<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">preg_match(<span class="string">'/\d&#123;18&#125;|\d&#123;17&#125;[0-9Xx]/'</span>,$html,$idcard);</span><br></pre></td></tr></table></figure></p><p>还有，注意看的话，<code>$match</code> 数组中还没有学生图片的 <code>src</code> 地址，为此我们需要分析网页源码，发现只有一个 <code>img</code> 标签，那就好办了，直接利用下面的正则代码，获取 <code>src</code> ,使用 <code>echo $link[1];</code> 输出图片链接地址，<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">preg_match(<span class="string">'/&lt;[img|IMG].*?src=[\'|\"](.*?(?:[\.gif|\.jpg]))[\'|\"].*?[\/]?&gt;/'</span>,$html,$link);</span><br></pre></td></tr></table></figure></p><p>这里输出的图片地址为相对地址，为了正常访问需要在前面加上前缀，具体前缀，可以在网页源码中点击图片链接后查看。</p><p>这样，图片地址，学号，姓名，学院，年级，身份证号都可以打印出来了，为了方便建议新建数组，存放这些信息。</p><h3 id="2-一些问题"><a href="#2-一些问题" class="headerlink" title="2.一些问题"></a>2.一些问题</h3><p>我在爬的过程中遇到很多小问题，下面具体说一说：</p><ul><li><p>要看清 <code>$match</code> 数组中哪些是自己需要的信息，不要弄错了。</p></li><li><p>这里是查询一个人的信息，如果多人可以弄个 <code>while(1)</code> 的循环。</p></li><li><p>查询完毕后可以把数据存入数据库或者本地文件，不过建议本地文件，毕竟这样会效率快一些。为了方便导入数据库，在存入文件时需要做一些必要的格式化处理，比如，学生的各个信息之间使用 <code>Tab</code> 分割，一个学生数据存入后，录入文件中一个回车符，这样也可以方便导入数据库。</p></li><li><p>因为学号的命名规则，前四位代表入学年份，接着两位代表学院编码，接着两位为专业编码，接着三位为专业内自己的编号，如果要是之间不作处理从最开始到最后的话，会浪费很多无用的时间，并且专业内人员的人数大多数都不会超过 <code>500</code> 人，这样每一千里面就会浪费 <code>500</code> 数据的查询时间，建议：先爬一下学院与专业的这四位数组成的学号末尾为 <code>001</code> 的数据，本地记录下，这样在进行爬虫时会节省不少时间，并且如果查询时连续出现 <code>50</code> 个空白数据，然后跳转到下一个专业或者学院代码继续爬，节省时间，等等其他方法。</p></li><li><p>注意有的学生的查询页中没有隐藏的身份证信息，这样就需要自己加个判断条件，防止写入文件时格式乱了，不利于录入数据库。</p></li><li><p>该接口使用了安全狗的检测功能，如果频繁查询，系统会屏蔽一段时间，不过这也就是 <code>10</code> 分钟左右的事，这也是需要考虑的，要知道，屏蔽后也会出现界面，这样程序在这个页面捕捉的信息也会写入文件，需要加个判断，比如，个人信息查询界面存在邮箱格式，而安全狗提示界面没有邮箱信息，可以用正则验证是否有邮箱信息，如果有的话，那就 <code>10</code> 分钟之后再试，并且不将获得的数据写入文件。</p></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>Shadowsocks一键安装脚本</title>
      <link href="/2016/01/23/shadowsocks-script/"/>
      <url>/2016/01/23/shadowsocks-script/</url>
      
        <content type="html"><![CDATA[<h3 id="Shadowsocks介绍"><a href="#Shadowsocks介绍" class="headerlink" title="Shadowsocks介绍"></a>Shadowsocks介绍</h3><p>Shadowsocks（中文名称：影梭）是使用Python等语言开发的、基于Apache许可证开源的代理软件。Shadowsocks使用socks5代理，用于保护网络流量。Shadowsocks分为服务器端和客户端。在使用之前，需要先将服务器端部署在支持Python等的服务器上面，然后通过客户端连接并创建本地代理。此外用户也可以选择购买基于Shadowsocks的商业服务，以获得更加稳定可靠的服务，或者免去自行部署的麻烦。目前开发者Clowwindy由于一些原因已宣布停止维护，shadowsocksR 的作者已表示会继续开发新版本。</p><p>对于想要使用SS的用户来说，相比在网路上买一些月供几十的SS服务商的出售的SS账户，自己倒不如买个便宜点的VPS自己构建一个SS服务器，为防止VPS资源过剩，自己也可以干点别的事情，这里给出了使用三种语言搭建SS服务器的一键安装脚本，供大家使用。</p><h3 id="Python一键脚本"><a href="#Python一键脚本" class="headerlink" title="Python一键脚本"></a>Python一键脚本</h3><ol><li><p>适用条件：系统为CentOS 6，7，Debian，Ubuntu 、内存≥128M</p></li><li><p>配置：服务器端口默认为8585 ，客户端端口为1080 ，密码默认为password</p></li><li><p>一键脚本（使用root用户）：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget --no-check-certificate https://raw.githubusercontent.com/CUBEGWZ/Shadowsocks/master/Python/shadowsocks.sh</span><br><span class="line">chmod +x shadowsocks.sh</span><br><span class="line">./shadowsocks.sh 2&gt;&amp;1 | tee shadowsocks.log</span><br></pre></td></tr></table></figure></li><li><p>卸载与其他配置：<br>卸载方式： <code>./shadowsocks.sh uninstall</code>  </p><ul><li>单用户配置文件 Sample：<br>配置文件路径：<code>/etc/shadowsocks.json</code>  </li></ul><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">"server"</span>:<span class="string">"0.0.0.0"</span>,</span><br><span class="line">    <span class="string">"server_port"</span>:8585,</span><br><span class="line">    <span class="string">"local_address"</span>:<span class="string">"127.0.0.1"</span>,</span><br><span class="line">    <span class="string">"local_port"</span>:1080,</span><br><span class="line">    <span class="string">"password"</span>:<span class="string">"password"</span>,</span><br><span class="line">    <span class="string">"timeout"</span>:300,</span><br><span class="line">    <span class="string">"method"</span>:<span class="string">"aes-256-cfb"</span>,</span><br><span class="line">    <span class="string">"fast_open"</span>: <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>多用户多端口配置文件 Sample：<br>配置文件路径：<code>/etc/shadowsocks.json</code></li></ul><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">"server"</span>:<span class="string">"0.0.0.0"</span>,</span><br><span class="line">    <span class="string">"local_address"</span>:<span class="string">"127.0.0.1"</span>,</span><br><span class="line">    <span class="string">"local_port"</span>:1080,</span><br><span class="line">    <span class="string">"port_password"</span>:&#123;</span><br><span class="line">    <span class="string">"8585"</span>:<span class="string">"password0"</span>,</span><br><span class="line">    <span class="string">"9001"</span>:<span class="string">"password1"</span>,</span><br><span class="line">    <span class="string">"9002"</span>:<span class="string">"password2"</span>,</span><br><span class="line">    <span class="string">"9003"</span>:<span class="string">"password3"</span>,</span><br><span class="line">    <span class="string">"9004"</span>:<span class="string">"password4"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">"timeout"</span>:300,</span><br><span class="line">    <span class="string">"method"</span>:<span class="string">"aes-256-cfb"</span>,</span><br><span class="line">    <span class="string">"fast_open"</span>: <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="GO一键脚本"><a href="#GO一键脚本" class="headerlink" title="GO一键脚本"></a>GO一键脚本</h3><ol><li><p>适用条件：系统为CentOS，Debian，Ubuntu 、内存≥128M</p></li><li><p>配置：服务器端口默认为8585 ，客户端端口为1080 ，密码默认为password</p></li><li><p>一键脚本（使用root用户）：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget --no-check-certificate https://raw.githubusercontent.com/CUBEGWZ/Shadowsocks/master/Go/shadowsocks-go.sh</span><br><span class="line">chmod +x shadowsocks-go.sh</span><br><span class="line">./shadowsocks-go.sh 2&gt;&amp;1 | tee shadowsocks-go.log</span><br></pre></td></tr></table></figure></li><li><p>卸载与其他配置：<br>卸载方式： <code>./shadowsocks-go.sh uninstall</code></p><ul><li>多用户多端口配置文件 Sample：<br>配置文件路径：<code>/etc/shadowsocks.json</code></li></ul><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">"port_password"</span>:&#123;</span><br><span class="line">    <span class="string">"8585"</span>:<span class="string">"password0"</span>,</span><br><span class="line">    <span class="string">"9001"</span>:<span class="string">"password1"</span>,</span><br><span class="line">    <span class="string">"9002"</span>:<span class="string">"password2"</span>,</span><br><span class="line">    <span class="string">"9003"</span>:<span class="string">"password3"</span>,</span><br><span class="line">    <span class="string">"9004"</span>:<span class="string">"password4"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">"method"</span>:<span class="string">"aes-256-cfb"</span>,</span><br><span class="line">    <span class="string">"timeout"</span>:600</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="Libev一键脚本"><a href="#Libev一键脚本" class="headerlink" title="Libev一键脚本"></a>Libev一键脚本</h3><ol><li><p>适用条件：系统为CentOS 32或64位、内存≥128M</p></li><li><p>配置：服务器端口默认为8585 ，客户端端口为1080 ，密码默认为password</p></li><li><p>一键脚本（使用root用户）：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget --no-check-certificate https://raw.githubusercontent.com/CUBEGWZ/Shadowsocks/master/Libev/shadowsocks-libev.sh</span><br><span class="line">chmod +x shadowsocks-libev.sh</span><br></pre></td></tr></table></figure></li><li><p>卸载与其他配置：<br>卸载方式： <code>./shadowsocks-libev.sh uninstall</code></p><ul><li><p>修改配置文件 <code>/etc/shadowsocks-libev/config.json</code>,同时启用 IPv4 与 IPv6 支持：</p> <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">"server"</span>:[<span class="string">"[::0]"</span>,<span class="string">"0.0.0.0"</span>],</span><br><span class="line">    <span class="string">"server_port"</span>:your_server_port,</span><br><span class="line">    <span class="string">"local_address"</span>:<span class="string">"127.0.0.1"</span>,</span><br><span class="line">    <span class="string">"local_port"</span>:1080,</span><br><span class="line">    <span class="string">"password"</span>:<span class="string">"password"</span>,</span><br><span class="line">    <span class="string">"timeout"</span>:600,</span><br><span class="line">    <span class="string">"method"</span>:<span class="string">"aes-256-cfb"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul></li></ol><h3 id="Shadowsocks操作命令"><a href="#Shadowsocks操作命令" class="headerlink" title="Shadowsocks操作命令"></a>Shadowsocks操作命令</h3><ul><li>启动：<code>/etc/init.d/shadowsocks start</code></li><li>停止：<code>/etc/init.d/shadowsocks stop</code></li><li>重启：<code>/etc/init.d/shadowsocks restart</code></li><li>状态：<code>/etc/init.d/shadowsocks status</code></li></ul><hr>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Shadowsocks </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>Linux下有趣的东西</title>
      <link href="/2016/01/22/linux-interesting/"/>
      <url>/2016/01/22/linux-interesting/</url>
      
        <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>Linux就是一个高大上的系统，如果你习惯使用Linux的Shell方式，而不是经常用Linux的界面形式，那你一定会学到更多东西，更能体会在门外汉看起来的高大上，但是，不要以为命令行的形式有多么枯燥，命令行中也有很多有意思的东西哦，话不多说，直入主题。</p><h3 id="1-可爱的小火车sl"><a href="#1-可爱的小火车sl" class="headerlink" title="1. 可爱的小火车sl"></a>1. 可爱的小火车<code>sl</code></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install sl</span><br></pre></td></tr></table></figure><p>当输入<code>sl</code>时就会有小火车路过哦!输入<code>sl -l</code>会出现加长版的小火车，输入<code>sl -F</code>就会出现晃动的小火车<br>再来个晃动的小火车：<br>其实，这个也可以用来恶搞别人，如果你输入：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> ls=sl</span><br></pre></td></tr></table></figure><p>因为<code>ls</code>是<code>Linux命令</code>中比较频繁的使用命令之一，当别人是输入<code>ls</code>的时候，小火车就出现了。。。</p><h3 id="2-程序猿的愤怒yes"><a href="#2-程序猿的愤怒yes" class="headerlink" title="2. 程序猿的愤怒yes"></a>2. 程序猿的愤怒<code>yes</code></h3><p>这个不需要安装什么东西，直接输入<code>yes</code>，然后回车，系统上面就会不断出现y，想停止的话，按<kbd>ctrl+z</kbd>。</p><h3 id="3-会说话的ASCII动物cowsay-cowthink"><a href="#3-会说话的ASCII动物cowsay-cowthink" class="headerlink" title="3. 会说话的ASCII动物cowsay/cowthink"></a>3. 会说话的ASCII动物<code>cowsay/cowthink</code></h3><p>首先需要安装<code>cowsay</code>,输入下面命令进行安装：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install cowsay</span><br></pre></td></tr></table></figure></p><p>安装后，当我们输入：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cowsay hello everyone！</span><br></pre></td></tr></table></figure><p>就出现了，一头会说话的奶牛（它说的话就是我们指定的内容）;也可以用下面的命令输出：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cowthink hello everyone!</span><br></pre></td></tr></table></figure></p><p>其实不光可以用<code>cow</code>(奶牛)，也可以用其他的动物，你可以使用下面命令，查看可以使用的动物,下图有实例：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cowsay -l</span><br></pre></td></tr></table></figure></p><p>使用的话，代码如下(<code>name</code>为动物名称；<code>words</code>为动物要说的内容)：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cowsay -f name words</span><br></pre></td></tr></table></figure></p><p>还可以使用这段代码，这样每次说话的动物都不同（下图有实例）：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cowsay -f <span class="string">"<span class="variable">$(ls /usr/share/cowsay/cows | sort -R | head -1)</span>"</span> hello everyone!</span><br></pre></td></tr></table></figure></p><h3 id="4-随机输出一句话fortune"><a href="#4-随机输出一句话fortune" class="headerlink" title="4. 随机输出一句话fortune"></a>4. 随机输出一句话<code>fortune</code></h3><p>如果仅安装上<code>fortune</code>就可以输出一句英文名言，或者英文的一句话，如果再安装上<code>fortune-zh</code>就可以使用fortune输出英文的和中文的，输出中文的是古诗词。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install fortune</span><br><span class="line">sudo apt-get install fortune-zh</span><br></pre></td></tr></table></figure></p><p>如果想输出一句话，直接使用<code>fortune</code>输出即可。<br>另一个有意思的方法是和<code>cowsay</code>结合起来的话，代码为:<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fortune | cowsay</span><br></pre></td></tr></table></figure></p><p>也可以使用随机动物输出一句话，代码为：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cowsay -f <span class="string">"<span class="variable">$(ls /usr/share/cowsay/cows | sort -R | head -1)</span>"</span> <span class="string">"<span class="variable">$(fortune -s)</span>"</span></span><br></pre></td></tr></table></figure></p><h3 id="5-让字符变为彩色的lolcat"><a href="#5-让字符变为彩色的lolcat" class="headerlink" title="5. 让字符变为彩色的lolcat"></a>5. 让字符变为彩色的<code>lolcat</code></h3><p>我们需要安装名为<code>lolcat</code>的软件，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install lolcat</span><br></pre></td></tr></table></figure></p><p>安装完成之后，可以与前面讲的<code>fortune</code>结合使用，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fortune | lolcat</span><br></pre></td></tr></table></figure></p><p>这两个小软件还可以与<code>cowsay</code>结合使用，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cowsay -f <span class="string">"<span class="variable">$(ls /usr/share/cowsay/cows | sort -R | head -1)</span>"</span> <span class="string">"<span class="variable">$(fortune -s)</span>"</span> | lolcat</span><br></pre></td></tr></table></figure></p><h3 id="6-用符号拼凑字母banner和figlet"><a href="#6-用符号拼凑字母banner和figlet" class="headerlink" title="6. 用符号拼凑字母banner和figlet"></a>6. 用符号拼凑字母<code>banner</code>和<code>figlet</code></h3><p>首先我们安装<code>bnner</code>：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install sysvbanner</span><br></pre></td></tr></table></figure></p><p>然后我们可以直接输入<code>banner</code>进行使用。<br>然后我们来安装<code>figlet</code>，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install figlet</span><br></pre></td></tr></table></figure></p><p>然后我们可以直接输入<code>figlet</code>进行使用。<br>可以看出，这两个拼的样式不同的哈~个人比较喜欢后面的<del>~</del></p><h3 id="7-终端上网w3m"><a href="#7-终端上网w3m" class="headerlink" title="7.终端上网w3m"></a>7.终端上网<code>w3m</code></h3><p>因为我用的是服务器环境，虽然说我都是SSH它，浏览信息都在我本地电脑上，可是真有个终端上网的工具，相信用处还是挺大的。安装代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install w3m w3m-img</span><br></pre></td></tr></table></figure></p><p>如果只是用来上个网，直接使用下面的代码，下面有实例（浏览时图片显示不出来，因为我用的server版本），如果想看别的<code>options</code>，直接<code>help</code>查看一下（想要退出的话，按<kbd>ctrl+z</kbd>）；<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">w3m cn.bing.com</span><br></pre></td></tr></table></figure></p><h3 id="8-屏幕录制script-amp-scriptreplay"><a href="#8-屏幕录制script-amp-scriptreplay" class="headerlink" title="8. 屏幕录制script&amp;scriptreplay"></a>8. 屏幕录制<code>script</code>&amp;<code>scriptreplay</code></h3><p>使用<code>script</code>命令可是将你在屏幕上的操作录制下来（结束录制的话，输入<code>exit</code>并<kbd>回车</kbd>）。使用<code>scriptreplay</code>可以查看你的录制。<br>使用下面的代码开始录制（实例如下）：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">script -t 2&gt;example.time -a example.txt</span><br></pre></td></tr></table></figure><p>注解上面的代码：</p><ol><li><code>-t</code> 是把时间数据输出到标准错误<code>(standard error)</code>，所以这里使用<code>2&gt;example.time</code>把数据转向到<code>example.txt</code>这个文件当中。</li><li><code>-a</code>选项是指定输出录制的文件</li></ol><p>输入<code>exit</code>并按<kbd>回车</kbd>来结束录制。<br>查看录制的代码的代码为：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scriptreplay example.time example.txt</span><br></pre></td></tr></table></figure></p><h3 id="9-显示logo的linuxlogo"><a href="#9-显示logo的linuxlogo" class="headerlink" title="9. 显示logo的linuxlogo"></a>9. 显示logo的<code>linuxlogo</code></h3><p><code>linuxlogo</code>(或<code>linux_logo</code>)是一款在<code>Linux</code>命令行下用彩色 ANSI 代码生成附带有系统信息的发行版徽标的工具。</p><p>首先安装这个小工具：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install linuxlogo</span><br></pre></td></tr></table></figure></p><p>安装完成后，我们可以使用<code>linuxlogo</code>命令查看你当前使用的发行版的默认徽标,我们可以查看它的<code>help</code>，里面有很多<code>options</code>，这里只介绍一些：<br>输入下面的代码表示仅输出徽标而不包含系统信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">linuxlogo -l</span><br></pre></td></tr></table></figure></p><p>输入下面的代码表示输出灰色的信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">linuxlogo -a</span><br></pre></td></tr></table></figure></p><p>其实这个工具内部还有很多徽标，你可以使用 <code>[-L list]</code> 选项查看在这些徽标的列表，代码及实例如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">linuxlogo -L list</span><br></pre></td></tr></table></figure></p><p>关于这个小工具更详细的介绍，大家可以去 <a href="https://linux.cn/article-5838-1.html" target="_blank" rel="noopener">这里</a></p><h3 id="10-分解因数factor"><a href="#10-分解因数factor" class="headerlink" title="10. 分解因数factor"></a>10. 分解因数<code>factor</code></h3><p><code>factor</code>不需要安装，可以直接使用，代码格式如下(number可以为任意整数):<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">factor number</span><br></pre></td></tr></table></figure></p><h3 id="11-屏保cmatrix"><a href="#11-屏保cmatrix" class="headerlink" title="11. 屏保cmatrix"></a>11. 屏保<code>cmatrix</code></h3><p>从上往下不断的输出字符串，和很多黑客电影中的场景差不多，估计那些电影也是用的这个吧.安装代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install cmatrix</span><br></pre></td></tr></table></figure></p><p>启动代码为<code>cmatrix</code>,退出的话直接<kbd>ctrl+z</kbd>即可。</p><h3 id="12-屏幕燃烧aafire"><a href="#12-屏幕燃烧aafire" class="headerlink" title="12. 屏幕燃烧aafire"></a>12. 屏幕燃烧<code>aafire</code></h3><p>该命令会输出由<code>ASCII</code>组成的字符，创造仿佛屏幕燃烧的效果。下面是安装，启动代码及实例：<br>安装代码：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install libaa-bin</span><br></pre></td></tr></table></figure></p><p>启动代码：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">aafire</span><br></pre></td></tr></table></figure></p><h3 id="13-输出艺术字toilet"><a href="#13-输出艺术字toilet" class="headerlink" title="13. 输出艺术字toilet"></a>13. 输出艺术字<code>toilet</code></h3><p>还是由ASCII组成的艺术字。安装代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install toilet</span><br></pre></td></tr></table></figure></p><h3 id="14-不一样的音乐播放器bb"><a href="#14-不一样的音乐播放器bb" class="headerlink" title="14. 不一样的音乐播放器bb"></a>14. 不一样的音乐播放器<code>bb</code></h3><p>安装代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install bb</span><br></pre></td></tr></table></figure></p><p>启动代码(这款软件是有声音的哦!)：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bb</span><br></pre></td></tr></table></figure></p><h3 id="15-观看星球大战telnet"><a href="#15-观看星球大战telnet" class="headerlink" title="15. 观看星球大战telnet"></a>15. 观看星球大战<code>telnet</code></h3><p>telnet是基于Telnet协议的远程登陆客户端程序，经常用来远程登录服务器，但是现在，我们却用它来看星球大阵，哈哈哈~</p><p>观看命令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">telnet towel.blinkenlights.nl</span><br></pre></td></tr></table></figure></p><p>这个有点长，并且，我好像不知道怎么退出去了。。。</p><hr>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>vsftpd搭建FTP服务器</title>
      <link href="/2016/01/22/vsftpd-ftp/"/>
      <url>/2016/01/22/vsftpd-ftp/</url>
      
        <content type="html"><![CDATA[<h3 id="一、要求"><a href="#一、要求" class="headerlink" title="一、要求"></a>一、要求</h3><p>现在我们需要在<code>Ubuntu14.04</code>上利用<code>vsftpd</code>创建<code>ftp</code>服务器环境，然后在禁止匿名访问的前提下，创建以下四个虚拟用户并配权：</p><ol><li>一个用户为：<code>tea1</code>，密码为：<code>tea1pass</code>，进入<code>tea1</code>目录（对该目录及其子目录拥有所有权限，不可进入上级目录）。</li><li>一个用户为：<code>tea2</code>，密码为：<code>tea2pass</code>，进入<code>tea2</code>目录（对该目录及其子目录拥有所有权限，不可进入上级目录）。</li><li>一个用户为：<code>stu</code>，密码为：<code>stupass</code>，进入<code>ftp</code>总目录（只拥有上传文件权限，不可进入上级目录）。</li><li>一个用户名为：<code>admin</code>，密码为：<code>admin</code>，进入<code>ftp</code>总目录（拥有ftp的全部权限）。</li></ol><p>注：<code>ftp</code>的主目录为<code>/home/ftp/ftp</code>，如果需要创建其他用户，或者修改用户权限，使用SSH修改。</p><h3 id="二、vsftpd安装："><a href="#二、vsftpd安装：" class="headerlink" title="二、vsftpd安装："></a>二、vsftpd安装：</h3><p>在配置<code>vsftpd</code>之前，我们先安装<code>vsftpd</code>，<code>vsftpd</code>的安装比较简单。我们直接使用<code>apt-get</code>进行安装，如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get -y install vsftpd</span><br></pre></td></tr></table></figure></p><p>安装步骤很简单，这一个命令就ok了，不过配置才是大头。你可以使用下面的命令尝试去看<code>vsftpd</code>安装的一些文件：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dpkg -L vsftpd |tac</span><br></pre></td></tr></table></figure></p><p>（这里只说明<code>/etc/init/vsftpd.conf</code>是<code>vsftpd</code>的初始化文件，而<code>/etc/vsftpd.conf</code>是<code>vsftpd</code>的配置文件）</p><p>vsftpd的启动，停止，重启方式：</p><ol><li><p>在ubuntu下要启动、停止、重启vsftpd，我们必须使用以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo service vsftpd stop</span><br><span class="line">sudo service vsftpd start</span><br><span class="line">sudo service vsftpd restart</span><br></pre></td></tr></table></figure></li><li><p>在centos下，我们可以使用以下命令：  </p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">service vsftpd stop</span><br><span class="line">/etc/init.d/vsftpd stop</span><br></pre></td></tr></table></figure></li></ol><h3 id="三、配置"><a href="#三、配置" class="headerlink" title="三、配置"></a>三、配置</h3><p>由于我们已经安装完了，所以，接下来我们就要进行一些配置，其实在我们安装完vsftpd之后，这个程序就会自动创建一个账户为ftp的账户，大家可以用下面的命令查看创建好的ftp用户：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /etc/passwd</span><br></pre></td></tr></table></figure></p><p>不过我不想使用它，我们就先把它删除了吧，反正以后也用不到，然后我们再创建一个用户ftp，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">userdel -r ftp </span><br><span class="line">sudo useradd -m -s /bin/bash ftp</span><br></pre></td></tr></table></figure></p><p>创建后，可以查看一下刚创建的用户（注意：这个用户是无法登录系统的，只是用来作后面虚拟用户的载体）：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /etc/passwd |grep ftp</span><br></pre></td></tr></table></figure></p><p>创建完成新用户后，我们来创建该用户的对应的目录并修改用户之前的对应目录：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo chown -R ftp:ftp /home/ftp/</span><br></pre></td></tr></table></figure></p><p>由于我们需要使用虚拟用户登录系统，所以我们接下来设置虚拟用户的账户名和密码的文件<code>login.txt</code>，如下代码所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo mkdir /etc/vsftpd/</span><br><span class="line">sudo vim /etc/vsftpd/login.txt</span><br></pre></td></tr></table></figure></p><p>然后在文件中，输入我们想要创建的四个用户的用户名及其密码并保存,需要注意的是一个账户一行，一个密码一行，一共四个用户和密码，也就是需要占用八行。<br>之后，我们需要用到<code>db_load</code>进行加密处理，所以我们还需要下载并安装<code>db-util</code>，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get -y install db-util</span><br></pre></td></tr></table></figure></p><p>安装成功后，使用<code>db_load</code>对<code>loginx.txt</code>进行加密处理：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo db_load -T -t <span class="built_in">hash</span> -f /etc/vsftpd/login.txt /etc/vsftpd/login.db</span><br></pre></td></tr></table></figure></p><p>将<code>loginx.txt</code>加密处理后，我们接下来配置<code>vsftpd</code>的PAM验证。<br>创建验证文件，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/pam.d/vsftpd.virtual</span><br></pre></td></tr></table></figure></p><p>在打开的文件中输入下面的代码（注意：下面代码中的<code>pam_userdb.so</code>的路径根据具体的路径进行填写，如果不知道，请find一下。下面的<code>/etc/vsftpd/login</code>等同于<code>/etc/vsftpd/login.db</code>文件，后面不需要写后缀。）： </p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">auth required /lib/x86_64-linux-gnu/security/pam_userdb.so db=/etc/vsftpd/login</span><br><span class="line">account required /lib/x86_64-linux-gnu/security/pam_userdb.so db=/etc/vsftpd/login</span><br></pre></td></tr></table></figure><p>接下来我们开始进行用户权限的分配！使用下面代码打开配置文件：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/vsftpd.conf</span><br></pre></td></tr></table></figure></p><p>确保该文件中下面的代码正确并且已经启用（如果没有请手动添加）：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">listen=YES</span><br><span class="line">listen_ipv6=NO</span><br><span class="line">anonymous_enable=NO</span><br><span class="line">local_enable=YES</span><br><span class="line">write_enable=YES</span><br><span class="line">local_umask=022</span><br><span class="line">dirmessage_enable=YES</span><br><span class="line">use_localtime=YES</span><br><span class="line">xferlog_enable=YES</span><br><span class="line">connect_from_port_20=YES</span><br><span class="line">xferlog_file=/var/<span class="built_in">log</span>/vsftpd.log</span><br><span class="line">xferlog_std_format=YES</span><br><span class="line">chroot_local_user=YES</span><br><span class="line">chroot_list_enable=NO</span><br><span class="line">allow_writeable_chroot=YES</span><br><span class="line">secure_chroot_dir=/var/run/vsftpd/empty</span><br><span class="line">pam_service_name=vsftpd</span><br><span class="line">rsa_cert_file=/etc/ssl/certs/ssl-cert-snakeoil.pem</span><br><span class="line">rsa_private_key_file=/etc/ssl/private/ssl-cert-snakeoil.key</span><br><span class="line">ssl_enable=NO</span><br><span class="line">guest_enable=YES</span><br><span class="line">pam_service_name=vsftpd.virtual</span><br><span class="line">user_config_dir=/etc/vsftpd/vu</span><br><span class="line">pasv_enable=YES</span><br><span class="line">pasv_min_port=30000</span><br><span class="line">pasv_max_port=31000</span><br></pre></td></tr></table></figure></p><p>上面这些代码的具体含义请自行查询，这里不做解释。<br>首先由于这些虚拟用户还没有对应的文件夹，所以我们需要在<code>/home/ftp/ftp</code>中创建对应的文件夹，并且将文件夹所有者设置为<code>ftp</code>，由于<code>stu</code>用户和<code>admin</code>用户都指向总目录，所以不需要额外创建目录，因此代码如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo mkdir /home/ftp/ftp</span><br><span class="line">sudo chown -R ftp:ftp /home/ftp/ftp/</span><br><span class="line">sudo mkdir /home/ftp/ftp/tea1</span><br><span class="line">sudo chown -R ftp:ftp /home/ftp/ftp/tea1/</span><br><span class="line">sudo mkdir /home/ftp/ftp/tea2</span><br><span class="line">sudo chown -R ftp:ftp /home/ftp/ftp/tea2/</span><br></pre></td></tr></table></figure></p><h3 id="虚拟用户的权限的配置"><a href="#虚拟用户的权限的配置" class="headerlink" title="虚拟用户的权限的配置"></a>虚拟用户的权限的配置</h3><p>创建完虚拟用户对应的目录后，我们需要建立虚拟用户对应的配置文件，对虚拟用户的权限进行配置，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo mkdir /etc/vsftpd/vu</span><br><span class="line">sudo vim /etc/vsftpd/vu/tea1</span><br></pre></td></tr></table></figure></p><p>在打开的窗口中输入<code>tea1</code>用户的权限设置并保存：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">guest_username=ftp</span><br><span class="line">local_root=/home/ftp/ftp/tea1/</span><br><span class="line">virtual_use_local_privs=YES</span><br><span class="line">anon_umask=133</span><br><span class="line">cmds_allowed=ABOR,ACCT,APPE,CWD,CDUP,DELE,HELP,LIST,MODE,MDTM,MKD,NOOP,NLST,PASS,PASV,PORT,PWD,QUIT,REIN,RETR,RMD,RNFR,RNTO,SITE,SIZE,STOR,STAT,STOU,STRU,SYST,TYPE,USER</span><br></pre></td></tr></table></figure></p><p>然后建立<code>tea2</code>的配置文件并输入配置信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/vsftpd/vu/tea2</span><br></pre></td></tr></table></figure></p><p>配置信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">guest_username=ftp</span><br><span class="line">local_root=/home/ftp/ftp/tea2/</span><br><span class="line">virtual_use_local_privs=YES</span><br><span class="line">anon_umask=133</span><br><span class="line">cmds_allowed=ABOR,ACCT,APPE,CWD,CDUP,DELE,HELP,LIST,MODE,MDTM,MKD,NOOP,NLST,PASS,PASV,PORT,PWD,QUIT,REIN,RETR,RMD,RNFR,RNTO,SITE,SIZE,STOR,STAT,STOU,STRU,SYST,TYPE,USER</span><br></pre></td></tr></table></figure></p><p>建立<code>stu</code>的配置文件并输入配置信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/vsftpd/vu/stu</span><br></pre></td></tr></table></figure></p><p>配置信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">guest_username=ftp</span><br><span class="line">local_root=/home/ftp/ftp/</span><br><span class="line">virtual_use_local_privs=YES</span><br><span class="line">anon_umask=133</span><br><span class="line">cmds_allowed=FEAT,REST,CWD,LIST,MDTM,MKD,NLST,PASS,PASV,PORT,PWD,QUIT,RMD,SIZE,STOR,TYPE,USER,ACCT,APPE,CDUP,HELP,MODE,NOOP,REIN,STAT,STOU,STRU,SYST</span><br></pre></td></tr></table></figure></p><p>建立<code>admin</code>的配置文件并输入配置信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/vsftpd/vu/admin</span><br></pre></td></tr></table></figure></p><p>配置信息：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">guest_username=ftp</span><br><span class="line">local_root=/home/ftp/ftp/</span><br><span class="line">virtual_use_local_privs=YES</span><br><span class="line">anon_umask=133</span><br><span class="line">cmds_allowed=ABOR,ACCT,APPE,CWD,CDUP,DELE,HELP,LIST,MODE,MDTM,MKD,NOOP,NLST,PASS,PASV,PORT,PWD,QUIT,REIN,RETR,RMD,RNFR,RNTO,SITE,SIZE,STOR,STAT,STOU,STRU,SYST,TYPE,USER</span><br></pre></td></tr></table></figure></p><blockquote><p>关于<code>cmds_allowed</code>的一些知识</p></blockquote><p>以逗号分隔的方式指定可用的FTP命令(post　login. USER, PASS and QUIT 是始终可用的命令)。在vsftpd中可以使用默认的一些方法配置来调节用户的对文件操作的权限，但是，对于有些权限的配置的效果却不尽人意，为此<code>cmds_allowed</code>就可以发挥它巨大的作用。这是一个强有力的locking down一个FTP服务器的手段。下面是它的实例和一些配置解释：</p><ol><li>只能上传。不能下载、删除、重命名。  <code>cmds_allowed=FEAT,REST,CWD,LIST,MDTM,MKD,NLST,PASS,PASV,PORT,PWD,QUIT,RMD,SIZE,STOR,TYPE,USER,ACCT,APPE,CDUP,HELP,MODE,NOOP,REIN,STAT,STOU,STRU,SYST</code></li><li>只能下载、删除、重命名。不能上传。<br><code>cmds_allowed=FEAT,REST,CWD,LIST,MDTM,MKD,NLST,PASS,PASV,PORT,PWD,QUIT,RMD,RNFR,RNTO,RETR,DELE,SIZE,TYPE,USER,ACCT,APPE,CDUP,HELP,MODE,NOOP,REIN,STAT,STOU,STRU,SYST</code></li><li>配置解释：<br><code>cmds_allowed=ABOR,ACCT,APPE,CWD,CDUP,DELE,HELP,LIST,MODE,MDTM,MKD,NOOP,NLST,PASS,PASV,PORT,PWD,QUIT,REIN,RETR,RMD,RNFR,RNTO,SITE,SIZE,STOR,STAT,STOU,STRU,SYST,TYPE,USER</code><br>其中：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">CWD - change working directory 更改目录</span><br><span class="line">DELE - delete a remote file 删除文件</span><br><span class="line">LIST - list remote files 列目录</span><br><span class="line">MKD - make a remote directory 新建文件夹</span><br><span class="line">NLST - name list of remote directory</span><br><span class="line">PWD - <span class="built_in">print</span> working directory 显示当前工作目录</span><br><span class="line">RETR - retrieve a remote file 下载文件</span><br><span class="line">RMD - remove a remote directory 删除目录</span><br><span class="line">RNFR - rename from 重命名</span><br><span class="line">RNTO - rename to 重命名</span><br><span class="line">STOR - store a file on the remote host 上传文件</span><br><span class="line">ABOR - abort a file transfer 取消文件传输</span><br><span class="line">CWD - change working directory 更改目录</span><br><span class="line">DELE - delete a remote file 删除文件</span><br><span class="line">LIST - list remote files 列目录</span><br><span class="line">MDTM - <span class="built_in">return</span> the modification time of a file 返回文件的更新时间</span><br><span class="line">MKD - make a remote directory 新建文件夹</span><br><span class="line">NLST - name list of remote directory</span><br><span class="line">PASS - send password</span><br><span class="line">PASV - enter passive mode</span><br><span class="line">PORT - open a data port 打开一个传输端口</span><br><span class="line">PWD - <span class="built_in">print</span> working directory 显示当前工作目录</span><br><span class="line">QUIT - terminate the connection 退出</span><br><span class="line">RETR - retrieve a remote file 下载文件</span><br><span class="line">RMD - remove a remote directory</span><br><span class="line">RNFR - rename from</span><br><span class="line">RNTO - rename to</span><br><span class="line">SITE - site-specific commands</span><br><span class="line">SIZE - <span class="built_in">return</span> the size of a file 返回文件大小</span><br><span class="line">STOR - store a file on the remote host 上传文件</span><br><span class="line">TYPE - <span class="built_in">set</span> transfer <span class="built_in">type</span></span><br><span class="line">USER - send username</span><br><span class="line">ACCT* - send account information</span><br><span class="line">APPE - append to a remote file</span><br><span class="line">CDUP - CWD to the parent of the current directory</span><br><span class="line">HELP - <span class="built_in">return</span> <span class="built_in">help</span> on using the server</span><br><span class="line">MODE - <span class="built_in">set</span> transfer mode</span><br><span class="line">NOOP - <span class="keyword">do</span> nothing</span><br><span class="line">REIN* - reinitialize the connection</span><br><span class="line">STAT - <span class="built_in">return</span> server status</span><br><span class="line">STOU - store a file uniquely</span><br><span class="line">STRU - <span class="built_in">set</span> file transfer structure</span><br><span class="line">SYST - <span class="built_in">return</span> system <span class="built_in">type</span></span><br></pre></td></tr></table></figure></li></ol><h3 id="两个问题说明："><a href="#两个问题说明：" class="headerlink" title="两个问题说明："></a>两个问题说明：</h3><blockquote><p>#####问题一：vsftpd搭建的FTP服务器存在的中文编码问题</p></blockquote><p>事先说明清楚的是，我的服务器环境是Ubuntu14.04，而FTP的访问者们大多都是使用Windows的系统，并且他们没有多少人使用一些比较专业的FTP客户端软件进行访问，他们大多数都是使用的Windows自带的资源管理器进行访问。</p><p>Linux中使用UTF-8作为locale已经成为理所当然的事，然而windows资源管理器却只支持GBK这一系列的编码，恰巧vsftpd并不会处理文件名的编码，这就会出现很不友好的问题，因此针对vsftpd的这个缺陷，网上之前也有过针对它的补丁，使得vsftpd可以进行编码转换，然而这个补丁的版本较老，针对新版本的vsftpd可能还会出现一系列的错误，这里就不对补丁进行介绍了。</p><p>之后通过一番查询，发现可以使用基于文件系统的fuse-convmvfs创建一个目录的镜像，一不用打补丁，二是所有的ftp服务器都可以使用，它的原理是：fuse-convmvfs 可以创建一个目录的镜像，在这个镜像里面的文件名都是经过 iconv 进行编码转换的结果，在底层则完全是同一个目录。例如下面代码所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">convmvfs /home/ftp/ftpgbk -o srcdir=/home/ftp/ftp,icharset=utf8,ocharset=gbk,allow_other,uid=0,gid=11</span><br></pre></td></tr></table></figure></p><p>上面代码的含义是将 <code>/home/ftp/ftpgbk</code> 作为 <code>/home/ftp/ftp</code> 的一个镜像，只是在 <code>/home/ftp/ftpgbk</code> 里面这些文件的名字“看起来是 GBK 编码的”。执行完上面语句之后，为使客户端使用时，查看的文件不会出现乱码，我们还需要将系统ftp的目录改为我们映射的<code>/home/ftp/ftpgbk</code>，然后还需要把ftp中的各个虚拟用户中的指定的路径中的<code>/home/ftp/ftp</code>改为<code>/home/ftp/ftpgbk</code>，这样就ok了。</p><blockquote><p>#####问题二：基于问题一成功后的仍旧的编码问题。</p></blockquote><p>说起来很有意思，大家如果全部读完了上文，应该会知道如果我们没有在tea1，tea2以及admin的目录权限配置文件中书写<code>cmds_allowed</code>,它们还是会对该目录及目录下的所有文件拥有全部权限，但是，如果，你没在它们的配置文件中加上<code>cmds_allowed</code>配置项的话，即使你完成了问题一的解决方式，仍然会出现编码问题，因为我之前写的博客中没有在它们三个的配置文件中加上<code>cmds_allowed</code>配置项，之后调试的时候烦恼了我很长时间，特此记录。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> FTP </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>pptpd搭建VPN服务器</title>
      <link href="/2016/01/01/pptpd-vpn/"/>
      <url>/2016/01/01/pptpd-vpn/</url>
      
        <content type="html"><![CDATA[<h3 id="正题"><a href="#正题" class="headerlink" title="正题"></a>正题</h3><ol><li>安装pptpd<br>安装之前，先更新一下源，<code>sudo apt-get update</code><br>然后安装：<code>sudo apt-get install pptpd</code></li><li><p>配置内网ip策略<br><code>sudo vim /etc/pptpd.conf</code><br>在文件中加入下面2行代码（其实文件末尾有这段代码，不过前面有注释，你也可以直接去掉注释）：<br><code>localip 192.168.0.1remoteip 192.168.0.234-238,192.168.0.245</code></p><blockquote><p>注意：<br>localip 自己服务器的IP（pptpd中设置的，并不是你的服务器真正的ip，此处可以理解为使用路由器时，路由器的管理ip，remoteip 分配给客户端的IP ，所有使用该vpn服务器都会获得该ip段内的一个IP）</p></blockquote></li><li><p>配置dns<br><code>sudo vim /etc/ppp/pptpd-options</code><br>在该文中加入下面2行，也可以配置其他dns:<br><code>ms-dns 8.8.8.8ms-dns 8.8.4.4</code></p></li><li>配置VPN帐号<br><code>sudo vim /etc/ppp/chap-secrets</code><br>添加vpn账号、服务器名、vpn密码和IP限制，一个帐号一行，每个参数中间用空格间隔。如：  <code>test * testpass *</code></li><li><p>TCP/IP策略配置:<br><code>sudo vim /etc/sysctl.conf</code><br>插入下面这一行代码（下面该段代码这个文件中也有，也可以取消注释，我这里是取消注释）,保存并退出：<br><code>net.ipv4.ip_forward=1</code><br>然后使用下面这段代码，可以看到出现的代码中出现刚才添加的或者取消注释的那段代码：<br><code>sudo sysctl -p</code>  </p><blockquote><p>注意：一定要注意“-”号的使用，一定要注意，下面也有很多，遇到在提。如果提示<code>net.ipv4.ip_forward = 1</code>，说明配置生效，如下图所示：</p></blockquote></li><li><p>开放网络端口<br>安装iptables(已安装请忽略，不知道安没安装，那就再安一遍，如果按了会提示)<br><code>sudo apt-get install iptables</code><br>然后开放1723端口：<br><code>sudo iptables -I INPUT -p tcp --dport 1723 -j ACCEPT</code></p></li><li><p>配置NAT网络地址转换:<br>下面的更有意思，记住哈，所有的“-”号都有两个，但是interface前面的“-号”只有一个，还有就是下面代码中的eth0是你的网卡名称，如果不知道你的网卡名称是什么，请使用ifconfig命令查看一下你的网卡名称，一定要注意，如果你的ubuntu有多个网卡的话，一定要查看对应网卡的对应ip，我这里因为有两个网卡，一个拥有内网的ip，一个拥有外网的ip，当然了，我肯定要使用外网的，因此，我使用的是eth1，截图如下（一定要注意结合实际！！）<br><code>sudo iptables --table nat --append POSTROUTING --out-interface eth1 --jump MASQUERADE</code></p></li><li><p>为确保系统重启后VPN可直接使用我们需要进行如下操作：</p><blockquote><p>#####1.让之前配置的iptables信息保存并设置为开机启动：</p></blockquote><ul><li>创建存储iptables信息的文件：<br><code>touch /etc/iptables</code></li><li>将上面的关于iptables的两个操作信息保存到该文件中：<br><code>iptables-save &gt; /etc/iptables</code>  </li><li>创建自启动配置文件，并授予可执行权限：<br><code>touch /etc/network/if-pre-up.d/iptables</code><br><code>chmod +x /etc/network/if-pre-up.d/iptables</code></li><li>编辑自启动配置文件，内容为启动网络时恢复的iptables配置信息：<br><code>vi /etc/network/if-pre-up.d/iptables</code><br>文件中内容为：<br><code>#!/bin/sh</code><br><code>/sbin/iptables-restore &lt; /etc/iptables</code>  </li><li>保存该信息后即可，下面是iptables的两个命令解析：<ul><li>保存iptables信息命令： iptables-save&gt;/etc/iptables  </li><li>恢复iptables信息命令： iptables-restore &lt;/etc/iptables</li></ul></li></ul><blockquote><p>#####2.开机后自动开启pptpd服务：</p></blockquote><ul><li>在/etc/rc.local文件的exit 0之前添加软件启动命令:<br><code>/etc/init.d/pptpd start</code></li></ul></li><li>最后重启pptpd<br><code>sudo /etc/init.d/pptpd restart</code><br>接下来就可以使用测试的用户名与密码连接你的vpn服务器了。即使系统重启后，我们任然可以直接使用VPN.</li></ol><blockquote><p>#####一个问题：连接后部分网站无法访问解决方法</p></blockquote><p>在我们的使用过程中发现百度的相关网站无法访问，当时也并未找到一些方法去解决，昨日，烫烫烫发来消息说原来是虚拟网卡的MTU值影响了部分网站的访问，在他修改后确实成功访问百度，所以贴出解决方案，避免更多人苦恼（我们是将虚拟网卡<code>ppp0</code>的<code>MTU</code>改为<code>1400</code>）。</p><ul><li>临时修改方法：<br> <code>ifconfig ppp0 mtu 1400</code>  </li><li>(推荐)永久修改方法:<br> 避免以后重启机器需重新配置，打开<code>/etc/ppp/ip-up</code>文件，并在<code>exit 0</code>前面（如果没有<code>exit 0</code>的话，就定位到文件末尾）加上如下代码：<br> <code>/sbin/ifconfig $1 mtu 1400</code></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>Red Hat课堂学习知识点笔记</title>
      <link href="/2016/01/01/redhat-note/"/>
      <url>/2016/01/01/redhat-note/</url>
      
        <content type="html"><![CDATA[<h3 id="0-环境及目录"><a href="#0-环境及目录" class="headerlink" title="0.环境及目录"></a>0.环境及目录</h3><p>课堂讲述及联想所记，便于自己后来查阅同时与他人便利。</p><ul><li>两款虚拟机：VMware Workstation 12 Pro、VirtualBox 5.1.22</li><li>Rad Hat镜像：RedHat-Server-6.4-i386</li><li>其他一些工具：XShell 5、Putty</li></ul><h4 id="目录列表"><a href="#目录列表" class="headerlink" title="目录列表"></a>目录列表</h4><blockquote><h4 id="1-VMware的VMware-Tools安装与VirtualBox的增强工具"><a href="#1-VMware的VMware-Tools安装与VirtualBox的增强工具" class="headerlink" title="1.VMware的VMware Tools安装与VirtualBox的增强工具"></a><a href="#redhat_1">1.VMware的VMware Tools安装与VirtualBox的增强工具</a></h4><h4 id="2-RedHat的root用户的密码初始化"><a href="#2-RedHat的root用户的密码初始化" class="headerlink" title="2.RedHat的root用户的密码初始化"></a><a href="#redhat_2">2.RedHat的root用户的密码初始化</a></h4><h4 id="3-RedHat无法使用yum安装软件解决方法"><a href="#3-RedHat无法使用yum安装软件解决方法" class="headerlink" title="3.RedHat无法使用yum安装软件解决方法"></a><a href="#redhat_3">3.RedHat无法使用yum安装软件解决方法</a></h4><h4 id="4-添加KDE桌面-中文支持和VNC设置体验"><a href="#4-添加KDE桌面-中文支持和VNC设置体验" class="headerlink" title="4.添加KDE桌面/中文支持和VNC设置体验"></a><a href="#redhat_4">4.添加KDE桌面/中文支持和VNC设置体验</a></h4><h4 id="5-XShell和X11转发"><a href="#5-XShell和X11转发" class="headerlink" title="5. XShell和X11转发"></a><a href="#redhat_5">5. XShell和X11转发</a></h4></blockquote><p><a id="redhat_1"></a></p><h4 id="1-VMware的VMware-Tools安装与VirtualBox的增强工具-1"><a href="#1-VMware的VMware-Tools安装与VirtualBox的增强工具-1" class="headerlink" title="1. VMware的VMware Tools安装与VirtualBox的增强工具"></a>1. VMware的VMware Tools安装与VirtualBox的增强工具</h4><p>为了加强同时使用虚拟机与主机的用户体验，像是实现虚拟机和主机图形用户界面之间平滑移动鼠标光标等的需求，我们在两款虚拟机软件中安装对应的增强工具。</p><h5 id="1-1-安装VMware的VMware-Tools："><a href="#1-1-安装VMware的VMware-Tools：" class="headerlink" title="1.1 安装VMware的VMware Tools："></a>1.1 安装VMware的VMware Tools：</h5><ul><li>在虚拟机中进入桌面后，点击VMware菜单栏的”虚拟机”，然后点击”安装VMware Tools”，在RedHat中确定VMware Tools的挂载点位置。</li><li>将VMware Tools中的类似于”VMwareTools-xxxxxx.tar.gz”的文件解压到另一个文件夹(例如tmp)中，解压，并运行安装，一路确定加回车即可。示例代码如下：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar zxvf VMwareTools-xxxxxx.tar.gz -C /tmp/</span><br><span class="line"><span class="built_in">cd</span> vmware-tools-distrib/</span><br><span class="line">./vmware-install.pl</span><br></pre></td></tr></table></figure></li></ul><h5 id="1-2-安装VirtualBox的增强工具："><a href="#1-2-安装VirtualBox的增强工具：" class="headerlink" title="1.2 安装VirtualBox的增强工具："></a>1.2 安装VirtualBox的增强工具：</h5><ul><li><p>首先确保本机已经安装kernel-devel和gcc两个依赖包，如果没有可以使用下面代码进行安装(如果下面代码使用后出错，请参考<a href="#redhat_3">RedHat无法使用yum在线安装软件解决方法</a>)：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install kernel-devel</span><br><span class="line">yum install gcc</span><br></pre></td></tr></table></figure></li><li><p>在虚拟机中进入桌面后，查看该虚拟机的界面右下角，会有一排小图标，其中有个光驱图标，确保它是灰色的，即确保该虚拟机的光驱没有被其他占用，然后点击VirtualBox窗口的”设备”，然后点击”安装增强工具”。</p></li><li>虚拟机系统界面就会弹出安装提示，点击”OK”，然后点击”RUN”继续即可,最后重启生效。类似图如下：<br><img src="http://cdn.bugwz.com/006qpCDTly1ffxq4t0txlj30sa0nawyk.jpg" alt="RedHat"><br><img src="http://cdn.bugwz.com/006qpCDTly1ffxq4thuugj30s30n17p5.jpg" alt="RedHat"></li></ul><p><a id="redhat_2"></a></p><h3 id="2-RedHat的root用户的密码初始化-1"><a href="#2-RedHat的root用户的密码初始化-1" class="headerlink" title="2.RedHat的root用户的密码初始化"></a>2.RedHat的root用户的密码初始化</h3><p>由于在VMware中安装的RedHat并没有提示设置root用户密码，并且系统策略也不和Ubuntu一样：Ubuntu中当root用户未设置密码时，普通用户可以使用 <code>sudo passwd root</code> 初始化root账户密码。所以我们需要如下步骤设置root账户密码：</p><ul><li>重启系统后在引导装载程序菜单上，用上下方向键选择你忘记密码的那个系统键入”e”来进入编辑模式，我的系统是两个选项，不过都是一个系统，区别在于是否有GUI显示；</li><li>接着用上下键选择最新的内核,这里是”kernel /vmlinuz-2.6.32-696.1.1.e16.i686 ro root=UUID=96504b88-d13d-4a”，然后继续按“e”键；</li><li>然后输入空格+single，即” single”，或者直接输入数字”1”,最后回车；</li><li>然后返回了那个内核界面，按下”b”进入单用户模式，在这里修改root的密码，之后重启即可。</li></ul><p><a id="redhat_3"></a></p><h3 id="3-RedHat无法使用yum安装软件解决方法-1"><a href="#3-RedHat无法使用yum安装软件解决方法-1" class="headerlink" title="3.RedHat无法使用yum安装软件解决方法"></a>3.RedHat无法使用yum安装软件解决方法</h3><p>如果我们在虚拟机中使用RedHat去执行 <code>yum install xxx</code> 安装软件的话，系统会给出类似于 “This system is not registered to Red Hat Subscription Management.You can use subscription-manager to register.” 这说明我们是没办法使用yum在线安装RedHat的软件了，这就十分尴尬了。这么好用的yum不能用，尴尬癌都犯了有没有。</p><p>三种办法解决，第一种去用CentOS，第二种我们使用RedHat的光盘ISO镜像作为本地源，第三种我们卸载RedHat自带yum，然后用CentOS的yum进行替换。第二种以及第三种的实现方法具体如下所示：</p><h4 id="3-1-使用RedHat的光盘ISO镜像作为本地源"><a href="#3-1-使用RedHat的光盘ISO镜像作为本地源" class="headerlink" title="3.1 使用RedHat的光盘ISO镜像作为本地源"></a>3.1 使用RedHat的光盘ISO镜像作为本地源</h4><ul><li>首先我们需要挂载本地RedHat的光盘ISO镜像，无论是VMware还是VirtualBox，都是在菜单栏目中将可移动设备光盘中设置为对应的RedHat镜像，并挂在到虚拟机中；</li><li><p>然后我们修改RedHat镜像的挂载位置(一定要记住之前的挂载点)，目的是便于后面使用，防止空格等字符的存在我们无法进入对应目录等，图示及命令如下所示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ffxxatnllej30gw03eaa7.jpg" alt=""></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">umount /dev/sr0</span><br><span class="line">mkdir -p /media/cdrom</span><br><span class="line">mount /dev/sr0 /media/cdrom</span><br></pre></td></tr></table></figure></li><li><p>接着修改系统源设置文件中的信息，将我们新挂载的RedHat镜像源加入改文件中，这个文件位置是 <code>/etc/yum.repos.d/rhel-source.repo</code>，我添加的一个源信息是该文件中最后一个，如下图所示：<br><img src="http://cdn.bugwz.com/006qpCDTly1ffxxd8s1nej30gx09faas.jpg" alt=""></p></li><li>最后我们清楚yum缓存并获取yum列表尝试一下，代码及成功的图示如下所示：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum clean all</span><br><span class="line">yum makecache</span><br></pre></td></tr></table></figure></li></ul><p><img src="http://cdn.bugwz.com/006qpCDTly1ffxxiyhj1tj30gx098my3.jpg" alt=""></p><h4 id="3-2-使用CentOS的yum进行替换RedHat自带的yum"><a href="#3-2-使用CentOS的yum进行替换RedHat自带的yum" class="headerlink" title="3.2 使用CentOS的yum进行替换RedHat自带的yum"></a>3.2 使用CentOS的yum进行替换RedHat自带的yum</h4><ul><li>首先确定系统是32位的还是64位的，可以使用命令 <code>uname -a</code> 查看，根据系统位数，前往<a href="http://mirrors.163.com/centos/6/os/" target="_blank">网易开源镜像站</a>对应目录下的Packages目录中获取文件链接使用 <code>wget</code> 下载相关软件；</li><li>(我RedHat是32位)相关软件如下所示，不过你会发现网站中并没有<br> <code>yum-3.2.29-30.el6.centos.noarch.rpm</code> ，反而有一个 <code>yum-3.2.29-81.el6.centos.noarch.rpm</code>，我想说的是，你下载后者安装会出错，所以你可从<a href="https://github.com/CUBEGWZ/Other/raw/master/yum-3.2.29-30.el6.centos.noarch.rpm" target="_blank" rel="noopener">这里</a>下载 <code>yum-3.2.29-30.el6.centos.noarch.rpm</code> 这个文件<ul><li>python-iniparse-0.3.1-2.1.el6.noarch.rpm</li><li>yum-3.2.29-30.el6.centos.noarch.rpm</li><li>yum-metadata-parser-1.1.2-16.el6.i686.rpm</li><li>yum-plugin-fastestmirror-1.1.30-40.el6.noarch.rpm</li></ul></li><li><p>然后我们卸载RedHat自带的yum，解释及命令如下所示：</p><ul><li>xargs是一条Unix和类Unix操作系统的常用命令。它的作用是将参数列表转换成小块分段传递给其他命令，以避免参数列表过长的问题；</li><li>–nodeps  强制卸载,不管依赖性；<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpm -qa | grep yum | xargs rpm -e --nodeps</span><br></pre></td></tr></table></figure></li></ul></li><li><p>然后安装下载的CentOS的yum包，需要注意的的最后一名命令将两个软件一起安装的原因是防止安装时出现依赖性错误，代码如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpm -ivh python-iniparse-0.3.1-2.1.el6.noarch.rpm</span><br><span class="line">rpm -ivh yum-metadata-parser-1.1.2-16.el6.i686.rpm</span><br><span class="line">rpm -ivh yum-3.2.29-30.el6.centos.noarch.rpm yum-plugin-fastestmirror-1.1.30-40.el6.noarch.rpm</span><br></pre></td></tr></table></figure></li><li><p>然后我们前往<a href="http://mirrors.163.com/.help/centos.html" target="_blank" rel="noopener">CentOS镜像使用帮助</a>下载CentOS 6 的repo文件，并将其放置在 <code>/etc/yum.repo.d</code> 目录中,之后编辑下载的 <code>CentOS6-Base-163.repo</code> 文件，将其中的$releasever更改为centos的版本号,此处为 <code>6</code> ,关于 <code>CentOS6-Base-163.repo</code> 文件说明一下：</p><ul><li>CentOS-Base.repo 是yum 网络源的配置文件</li><li>CentOS-Media.repo 是yum 本地源的配置文件<em>**</em></li></ul></li><li><p>最后我们清楚缓存并获取yum列表，之后就可以使用yum命令在线安装软件了，方便至极，代码如下所示：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum clean all</span><br><span class="line">yum makecache</span><br></pre></td></tr></table></figure></li><li><p>补充：最后比较意思的是，当我们重启服务器的时候，我们会发现，系统启动界面变成了 <code>CentOS</code> 的，然后，让我们利用 <code>cat /etc/issue</code> 查看系统发行版本时还是Red Hat。</p></li></ul><p><a id="redhat_4"></a></p><h3 id="4-添加KDE桌面-中文支持和VNC设置体验-1"><a href="#4-添加KDE桌面-中文支持和VNC设置体验-1" class="headerlink" title="4.添加KDE桌面/中文支持和VNC设置体验"></a>4.添加KDE桌面/中文支持和VNC设置体验</h3><h4 id="4-1-添加KDE桌面-中文支持"><a href="#4-1-添加KDE桌面-中文支持" class="headerlink" title="4.1 添加KDE桌面/中文支持"></a>4.1 添加KDE桌面/中文支持</h4><ul><li><p>在我们可以使用 <code>yum</code>的基础之上，我们需要安装 <code>KDE Desktop</code>这个软件组，我们可以使用下面的命令进行查看与安装：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum grouplist</span><br><span class="line">yum groupinstall <span class="string">"KDE Desktop"</span></span><br></pre></td></tr></table></figure></li><li><p>之后我们可能需要安装中文支持包，其中包括 <code>kde-i18n-Chinese</code> 和 <code>kde-l10n-Chinese</code>，命令如下所示：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install kde-i18n-Chinese</span><br><span class="line">yum install kde-l10n-Chinese</span><br></pre></td></tr></table></figure></li><li><p>然后就需要修改系统默认的语言显示，这里有两种办法，第一种，在GUI界面上修改，第二种通过命令行修改，命令行修改的操作是将 <code>/etc/sysconfig/i18n</code> 中的 <code>LANG=&quot;en_US.UTF-8&quot;</code> 改为 <code>LANG=&quot;zh_CN.UTF-8&quot;</code>,然后 <code>logout</code> 重新登录即可。</p></li><li>关于 <code>i18n</code> 与 <code>l10n</code> 的补充解释：<ul><li><code>i18n</code> 是”Internationalization”的缩写，中间的18代表在首字母”i”和尾字母”n”之间省略了 18 个字母，国际化是指采用国际标准提取或显示信息;</li><li><code>l10n</code> 是”Localization”的缩写，中间的10代表在首字母”l”和尾字母”n”之间省略了10个字母，本地化是在国际化的基础上针对不同地区进行个性化设置，就例如微软的系统安装诗句;</li></ul></li></ul><h4 id="4-2-VNC设置体验"><a href="#4-2-VNC设置体验" class="headerlink" title="4.2 VNC设置体验"></a>4.2 VNC设置体验</h4><ul><li><p>VNC(Virtual Network Computing)是基于RFB（Remote Frame Buffer）协议进行通信的，是一个基于平台无关的简单显示协议的超级瘦客户系统，VNC的缺省端口是main:5900（C/S）和http:5800（B/S）端口。<br>RFB (远程帧缓存) 是一个远程图形用户的简单协议，因为它工作在帧缓存级别上，所以它可以应用于所有的窗口系统，例如：X11,Windows和Mac系统。由于RFB是基于TCP的一个应用层协议，所以VNC也是基于TCP协议的。</p></li><li><p>所需软件包括，RedHat服务器端安装的VNC服务器端软件，以及Windows下需要的VNC客户端软件，这里使用 <code>VNC Viewer</code>，下面安装VNC服务器端软件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install vnc-server</span><br></pre></td></tr></table></figure></li><li><p>然后我们需要修改VNC的配置文件 <code>/etc/sysconfig/vncservers</code> ，根据其中示例设置一个用户认证连接设置，这里我们使用root用户进行远程连接，也可以创建其他用户进行连接，我的配置文件如下所示：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">VNCSERVERS=<span class="string">"1:root"</span></span><br><span class="line">VNCSERVERARGS[1]=<span class="string">"-geometry 1024x768"</span></span><br></pre></td></tr></table></figure></li><li><p>其中关于VNC的配置文件详细解释如下所示：</p><ul><li>数字 <code>1</code> 代表远程连接的实际TCP端口为 <code>5900+1</code> ，这是因为VNC Server 的默认初始端口是 5900，用户设置的端口是和初始端口做加法然后使用的，如果想要修改，可以进入 <code>/usr/bin/vncserver</code> 修改即可；</li><li>这里的 <code>root</code> 是一个系统用户，如果设置了多个用户可以通过自己的专属端口使用VNC，其中VNCSERVERS的内容可以写成”1:root 2:test”，但是必须存在多个VNCSERVERARGS[*] 。</li><li>VNCSERVERARGS的参数 <code>-geometry 1024x768</code> 是设置客户端显示的桌面分辨率大小。</li><li>VNCSERVERARGS的参数 <code>-nohttpd</code> 是不监听HTTP端口。</li><li>VNCSERVERARGS的参数 <code>-nolisten 6000</code> 是不监听TCP 6000端口。</li><li>VNCSERVERARGS的参数 <code>-localhost</code> 指只允许从本机访问</li><li>VNCSERVERARGS的参数 <code>-AlwaysShared</code> 这是默认参数，不写的话也是默认存在的，此参数允许可以多个客户端同时使用一个用户及对应的端口进行连接，但是连接后，所有的VNC界面将显示相同，所有人都会看到相同的界面。</li><li>VNCSERVERARGS的参数 <code>-SecurityTypes  None</code> 是指登录不需要密码认证，如果将 <code>None</code> 换成 <code>VncAuth</code> ,则需要密码认证，默认值是 <code>VncAuth</code> 。</li><li>VNCSERVERARGS的参数 <code>-depth</code> 是色深，参数有8、16、24、32。</li></ul></li><li>需要注意防火墙的影响，如果服务器端开启了 <code>iptables</code> 防火墙，我们可以关闭防火墙，或者添加防火墙规则，允许我们的端口通过<ul><li>关闭防火墙：<code>service iptables stop</code>;</li><li>添加防火墙规则，允许端口通过：<code>iptables -A INPUT -p tcp --dport 5900:5920 -j ACCEPT</code>;</li></ul></li></ul><p><a id="redhat_5"></a></p><h3 id="5-XShell和X11转发-1"><a href="#5-XShell和X11转发-1" class="headerlink" title="5. XShell和X11转发"></a>5. XShell和X11转发</h3><p>X11是X Window系统的简称，由于X Window图形系统是一个经典的Server/Client架构，两者通过正常的网络协议进行通信。可是实现比如：服务器中并未安装桌面环境，通过SSH建立的隧道进行转发后，可以实现在另外一台启动了X Server的机器上，由X Server调用本机硬件资源，在屏幕上显示出界面。在windows下我们可以使用 <code>Xming X Server</code> 这款软件接受远程传输来的绘图的相关指令并在本机绘制。</p><ul><li>前提条件是，服务器端已经开启了X11转发，而系统默认是不开启的，我们需要编辑 <code>/etc/ssh/sshd_config</code> 文件，设置 <code>X11Forwarding yes</code>，然后执行 <code>service sshd restart</code> 命令</li><li>在服务器开启了之后，我们还需要在SSH连接软件中开启类似于 “X11 转移” 的功能，然后打开本地的 <code>X Server</code> 软件，我们在windows下测试使用的软件是 <code>Xming X Server</code> 。</li><li>所有 <code>X Server</code> 启动后默认会监听本地6000端口， <code>X client</code> 通过环境变量DISPLAY获取 <code>X Server</code> 服务地址，例如<br> <code>DISPLAY=localhost:0.0</code> 代表 <code>X Server</code> 在本机6000端口，<br>  <code>DISPLAY=localhost:1.0</code> 代表 <code>X Server</code> 在本机6001端口,依此类推；建立连接之前， <code>X Server</code> 还需要验证 <code>X client</code> 的身份，最常的是基于Cookie机制。</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Redhat </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>Ubuntu搭建OpenVPN服务器</title>
      <link href="/2015/11/08/ubuntu-openvpn/"/>
      <url>/2015/11/08/ubuntu-openvpn/</url>
      
        <content type="html"><![CDATA[<h3 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h3><p>对于vpn以前使用最多的是pptpd这个解决方案，但是pptpd相对于OpenVPN来说，没有OpenVPN安全，而且pptpd在Linux下命令行支持不是很好，稳定性也不如OpenVPN。所以最后就选择OpenVPN来搭建VPN。本文的OpenVPN Server安装在Ubuntu 14.04上安装.有关OpenVPN在CentOS6.6 64bit的配置完全可以使用，已经经过验证。文章后有CentOS详细配置命令及步骤。</p><h3 id="OpenVPN原理"><a href="#OpenVPN原理" class="headerlink" title="OpenVPN原理"></a>OpenVPN原理</h3><ol><li>OpenVPN通过使用公开密钥（非对称密钥，加密解密使用不同的key，一个称为Publice key，另外一个是Private key）对数据进行加密的。这种方式称为TLS加密</li><li>OpenVPN使用TLS加密的工作过程是，首先VPN Sevrver端和VPN Client端要有相同的CA证书，双方通过交换证书验证双方的合法性，用于决定是否建立VPN连接。</li><li>然后使用对方的CA证书，把自己目前使用的数据加密方法加密后发送给对方，由于使用的是对方CA证书加密，所以只有对方CA证书对应的Private key才能解密该数据，这样就保证了此密钥的安全性，并且此密钥是定期改变的，对于窃听者来说，可能还没有破解出此密钥，VPN通信双方可能就已经更换密钥了。</li></ol><h3 id="安装OpenVPN"><a href="#安装OpenVPN" class="headerlink" title="安装OpenVPN"></a>安装OpenVPN</h3><p>OpenVPN的安装我们分为<code>apt-get</code>方式和源码方式，下面我们只讲解<code>apt-get</code>方式的安装。有关源码方式安装OpenVPN，可自行查询。<code>apt-get</code>方式安装的命令如下所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get -y install openvpn libssl-dev openssl</span><br></pre></td></tr></table></figure></p><p>OpenVPN安装完毕后，我们来查看OpenVPN的版本，如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openvpn –version</span><br></pre></td></tr></table></figure></p><p>这里我安装的<code>OpenVPN</code>的版本为<code>2.3.2</code>。<br>我们再来查看下<code>OpenVPN</code>安装时产生的文件，如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dpkg -L openvpn |more</span><br></pre></td></tr></table></figure></p><p><code>OpenVPN</code>安装完毕后，我们再来安装<code>easy-rsa</code>,<code>easy-rsa</code>是用来制作<code>OpenVPN</code>相关证书的。安装<code>easy-rsa</code>，使用如下命令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get -y install easy-rsa</span><br></pre></td></tr></table></figure></p><p>查看<code>easy-rsa</code>安装的文件，代码如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dpkg -L easy-rsa |more</span><br></pre></td></tr></table></figure></p><p>注：在我的服务器上面<code>easy-rsa</code>已经安装到<code>/usr/share/easy-rsa/</code>目录下。</p><h3 id="制作相关证书"><a href="#制作相关证书" class="headerlink" title="制作相关证书"></a>制作相关证书</h3><p>根据第一章节<code>OpenVPN</code>的工作原理，我们可以知道<code>OpenVPN</code>的证书分为三部分：<code>CA证书</code>、<code>Server端证书</code>、<code>Client端证书</code>。下面我们通过<code>easy-rsa</code>分别对其进行制作。</p><ol><li>制作CA证书<br><code>OpenVPN</code>与<code>easy-rsa</code>安装完毕后，我们需要在<code>/etc/openvpn/</code>目录下创建<code>easy-rsa</code>文件夹，如下：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo mkdir /etc/openvpn/easy-rsa/</span><br></pre></td></tr></table></figure></li></ol><p>然后把<code>/usr/share/easy-rsa/</code>目录下的所有文件全部复制到<code>/etc/openvpn/easy-rsa/</code>下，如下代码所示：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo cp -r /usr/share/easy-rsa/* /etc/openvpn/easy-rsa/</span><br></pre></td></tr></table></figure></p><p>当然，我们也可以直接在<code>/usr/share/easy-rsa/</code>制作相关的证书，但是为了后续的管理证书的方便，我们还是把<code>easy-rsa</code>放在了<code>OpenVPN</code>的启动目录下。</p><p>注意：由于我们现在使用的是<code>Ubuntu</code>系统，所以我们必须切换到<code>root</code>用户下才能制作相关证书，否则<code>easy-rsa</code>会报错。如果是<code>Centos</code>系统，则不存在此问题。因为我一直处于<code>root</code>状态，所以不用切换了，当然，这种一直处于<code>root</code>状态的习惯并不是好习惯，毕竟<code>root</code>太强大了。<br>在开始制作CA证书之前，我们还需要编辑<code>vars</code>文件，进入并修改下面相关选项内容即可。如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vi /etc/openvpn/easy-rsa/vars</span><br><span class="line"><span class="built_in">export</span> KEY_COUNTRY=”CN”</span><br><span class="line"><span class="built_in">export</span> KEY_PROVINCE=”SD”</span><br><span class="line"><span class="built_in">export</span> KEY_CITY=”JN”</span><br><span class="line"><span class="built_in">export</span> KEY_ORG=”SOIL<span class="string">"</span></span><br><span class="line"><span class="string">export KEY_EMAIL=”abc@abc.com”</span></span><br><span class="line"><span class="string">export KEY_OU=”test”</span></span><br><span class="line"><span class="string">export KEY_NAME=”test”</span></span><br></pre></td></tr></table></figure></p><p><code>vars</code>文件主要用于设置证书的相关组织信息，红色部分的内容可以根据自己的实际情况自行修改。其中<code>export KEY_NAME=”test”</code>这个要记住下，我们下面在制作<code>Server端证书</code>时，会使用到。以上内容，我们也可以使用系统默认的，也就是说不进行修改也是可以使用的。</p><p>然后使用<code>source vars</code>命令使其生效，如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> vars</span><br><span class="line">./clean-all</span><br></pre></td></tr></table></figure></p><p>注意：执行<code>clean-all</code>命令会删除当前目录下的<code>keys</code>文件夹。</p><p>现在开始正式制作<code>CA证书</code>：<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>然后如果成功的话，会提示类似与下面的提示（我的是windows10，所以提示是这样的）：<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>windows7端的提示可能是这样的：<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>查看了一下ip，的确连上了。<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>注意：上图中的<code>client</code>就是根据<code>client.ovpn</code>，这个文件名来的。</p><p>通过上图，我们可以看到本机确实已经连接到Server端，而且获得的IP地址也确实为<code>10.8.0.6</code>。</p><h3 id="在Linux上"><a href="#在Linux上" class="headerlink" title="在Linux上"></a>在Linux上</h3><p>在<code>Windows</code>上测试完毕后，我们现在在切换到<code>Linux</code>系统。这个<code>Linux</code>系统是我的搬瓦工的一个VPS，之前是用来番茄的，现在用来测试一下。它的系统叫我更改成<code>Ubuntu14.04</code>,。</p><p>要在<code>Ubuntu</code>上连接<code>OpenVPN Server端</code>，我们需要先安装<code>OpenVPN</code>软件，如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get -y install openvpn</span><br></pre></td></tr></table></figure></p><p>安装完毕后，把我们刚刚在Windows系统配置的文件上传到Ubuntu系统中。之前我们先在<code>/etc/openvpn</code>目录下面创建了一个<code>test</code>文件，用于存放上传的那四个文件，然后我们使用<code>FileZilla</code>工具利用SSH上传，上传完成如下：<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>注意：上传完毕后，我们不需要修改任何配置文件。因为这几个文件在Windows下已经可以正确连接<code>OpenVPN Server端</code>。</p><p>注意：在连接<code>Server端</code>之前，一定要切换到root用户下。因为在连接<code>Server端</code>时，<code>OpenVPN</code>会在本机创建一个虚拟网卡，如果使用普通用户的话，是没有权限创建虚拟网卡的。</p><p>切换到<code>root</code>用户，使用<code>sudo su</code>命令，然后切换到我们创建的<code>test</code>目录下，然后使用如下命令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openvpn --config client.ovpn</span><br></pre></td></tr></table></figure></p><p>如果出现下图的信息，说明已经正确连接Server端。<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>现在我们在本机使用<code>ifconfig</code>进行查看，在此建议重新开启一个新的<code>ssh</code>窗口。<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>通过上图，我们可以很明显的看出，本机已经正确连接<code>Server端</code>，并且也在本机虚拟出一个叫<code>tun0</code>的虚拟网卡。</p><p>如果想让<code>Ubuntu</code>开机启动并后台运行的话，可以把这条命令写入<code>rc.local</code>文件中。我们先用下面的命令打开<code>rc.local</code>文件：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/rc.local</span><br></pre></td></tr></table></figure></p><p>然后将下面的代码录入该文件并保存：<br><code>/usr/sbin/openvpn --config /etc/openvpn/test/client.ovpn &gt;/var/log/openvpn.log &amp;</code></p><p>注意，命令末尾的<code>&amp;</code>符号不能省略，否则将可能阻塞系统的正常启动。同时这个时候，<code>client.ovpn</code>文件中有关证书的配置一定要写成绝对路径，要不然系统会报错。如下：<br>[从之前博客找回的部分信息，此处缺图片，待后期重新尝试。]</p><p>如果是<code>CentOS</code>系统的话，我们首先需要安装<code>epel</code>源，然后安装<code>OpenVPN</code>软件包。如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpm -ivh http://dl.fedoraproject.org/pub/epel/epel-release-latest-6.noarch.rpm</span><br><span class="line">yum -y install openvpn</span><br></pre></td></tr></table></figure></p><p>以上安装完毕后，把<code>Windows</code>已经成功连接的<code>Client</code>相关文件上传到<code>CentOS</code>系统中，然后连接方法和<code>Ubuntu</code>系统上一样。</p><p>注意：如果在<code>CentOS</code>系统要开机启动的话，也是和<code>Ubuntu</code>系统是一样的，但是有一点需要指出就是<code>Client</code>相关配置文件不能放在<code>/root</code>目录下。</p><p>因为<code>CentOS</code>的<code>OpenVPN Server</code>配置和<code>Ubuntu</code>基本一样，所以就不再单独写一篇有关<code>CentOS</code>下安装配置<code>OpenVPN Server</code>的文章。下面附上在<code>CentOS</code>下，所有执行的命令。如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpm -ivh http://dl.fedoraproject.org/pub/epel/epel-release-latest-6.noarch.rpm</span><br><span class="line">yum -y install openvpn</span><br><span class="line">rpm -ql openvpn</span><br><span class="line">cat /usr/share/doc/openvpn-2.3.7/sample/sample-config-files/README</span><br><span class="line">yum -y install easy-rsa</span><br><span class="line">rpm -ql easy-rsa</span><br><span class="line"><span class="built_in">cd</span> /usr/share/easy-rsa/2.0/</span><br><span class="line">vim vars</span><br><span class="line"><span class="built_in">export</span> KEY_COUNTRY=”CN”</span><br><span class="line"><span class="built_in">export</span> KEY_PROVINCE=”SD”</span><br><span class="line"><span class="built_in">export</span> KEY_CITY=”JN”</span><br><span class="line"><span class="built_in">export</span> KEY_ORG=”SOIL”</span><br><span class="line"><span class="built_in">export</span> KEY_EMAIL=”abc@abc.com”</span><br><span class="line"><span class="built_in">export</span> KEY_OU=”SOIL”</span><br><span class="line"><span class="built_in">export</span> KEY_NAME=”<span class="built_in">test</span>”</span><br><span class="line"><span class="built_in">source</span> vars</span><br><span class="line">./clean-all</span><br><span class="line">./build-ca</span><br><span class="line">./build-key-server <span class="built_in">test</span></span><br><span class="line">./build-dh</span><br><span class="line">./build-key centos</span><br><span class="line"><span class="built_in">cd</span>  keys</span><br><span class="line">cp ca.crt test.key test.crt dh2048.pem /etc/openvpn/</span><br><span class="line">cp ca.crt centos.key centos.crt /root/</span><br><span class="line">cp /usr/share/doc/openvpn-2.3.7/sample/sample-config-files/client.conf /root</span><br><span class="line">cp /usr/share/doc/openvpn-2.3.7/sample/sample-config-files/server.conf /etc/openvpn/</span><br></pre></td></tr></table></figure><p>服务器端配置文件：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/openvpn/server.conf</span><br><span class="line">grep -vE “;|<span class="comment">#|^$” /etc/openvpn/server.conf</span></span><br><span class="line">port 1194</span><br><span class="line">proto udp</span><br><span class="line">dev tun</span><br><span class="line">ca ca.crt</span><br><span class="line">cert test.crt</span><br><span class="line">key test.key</span><br><span class="line">dh dh2048.pem</span><br><span class="line">server 10.8.0.0 255.255.255.0</span><br><span class="line">ifconfig-pool-persist ipp.txt</span><br><span class="line">keepalive 10 120</span><br><span class="line">comp-lzo</span><br><span class="line">persist-key</span><br><span class="line">persist-tun</span><br><span class="line">status openvpn-status.log</span><br><span class="line">verb 3</span><br></pre></td></tr></table></figure></p><p>客户端配置文件：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep -vE “;|<span class="comment">#|^$” centos.conf</span></span><br><span class="line">client</span><br><span class="line">dev tun</span><br><span class="line">proto udp</span><br><span class="line">remote 182.254.223.140 1194</span><br><span class="line">resolv-retry infinite</span><br><span class="line">nobind</span><br><span class="line">persist-key</span><br><span class="line">persist-tun</span><br><span class="line">ca ca.crt</span><br><span class="line">cert centos.crt</span><br><span class="line">key centos.key</span><br><span class="line">remote-cert-tls server</span><br><span class="line">comp-lzo</span><br><span class="line">verb 3</span><br></pre></td></tr></table></figure></p><p>以上信息参考网络上的信息，自己亲身实践并整理一番，有不对之处，请告知。（测试时间：2015年11月8日）</p><hr>]]></content>
      
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> OpenVPN </tag>
            
        </tags>
      
    </entry>
    
  
  
</search>
